services:
  shrimp:
    build:
      context: .
    restart: "no"
    volumes:
      - "${SHRIMP_DATA_DIR:-./data/shrimp}:/var/lib/shrimp"
    develop:
      watch:
        - action: sync+restart
          path: ./dist
          target: /app/dist
