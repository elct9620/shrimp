services:
  shrimp:
    build:
      context: .
    image: shrimp:latest
    container_name: shrimp
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    restart: unless-stopped
