services:
  shrimp:
    build:
      context: .
    image: shrimp:latest
    container_name: shrimp
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - "${SHRIMP_DATA_DIR:-./data/shrimp}:/var/lib/shrimp"
    restart: unless-stopped
