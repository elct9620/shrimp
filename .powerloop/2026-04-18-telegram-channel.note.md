---
goal: Add Telegram Channel support per SPEC.md, following the implementation direction discussed in the spawning conversation. Every implementation step and every check must be verified against SPEC.md. Tests should prefer integration coverage (MSW at HTTP boundary, tmpdir for filesystem) over fine-grained unit tests and the dead code they produce.
language: en
current_phase: execute
started_at: 2026-04-18T00:00:00Z
interval: 10m
cron_id: 82934375
execute_skills: /coding:write → /coding:review → /coding:refactor → /git:commit
review_skills: /coding:review → /coding:refactor → /git:commit
sample_passes: 0/10
review_cycles: 5
---

# Telegram Channel Implementation

## Implementation Direction (from spawning conversation)

### Endpoint
- `POST /channels/telegram` — validates `X-Telegram-Bot-Api-Secret-Token`; 401 on mismatch, 400 on malformed payload, 200 on accept.
- Only mounted when `CHANNELS_ENABLED=true`.
- Slash commands parsed by adapter; non-command messages enqueue a ChannelJob.

### Refactor of existing code
- Split `src/use-cases/job.ts` Job into `HeartbeatJob` + `ChannelJob` sharing a Job Worker skeleton.
- `HeartbeatRoute` dispatches `HeartbeatJob`; `JobQueue` stays variant-agnostic.
- `JobInput` (ports/shrimp-agent.ts) gains `history: ConversationMessage[]`; `AiSdkShrimpAgent` maps it to AI SDK messages and emits `gen_ai.conversation.id = sessionId` on `shrimp.job` span for ChannelJob only.

### New entities / value objects
- `ConversationMessage` (role + content)
- `ConversationRef` (opaque; Telegram interprets as chatId etc.)
- `Session` (id + messages or handled by repository)

### New ports + use cases
- Port: `SessionRepository` (getCurrent, createNew, append)
- Port: `ChannelGateway` (reply)
- Use case: `ChannelJob`
- Use case: `StartNewSession` (for `/new`, bypasses JobQueue)

### New adapters
- `adapters/http/routes/channels/telegram.ts` — webhook entry, secret validation, slash parsing
- `adapters/tools/built-in/reply.ts` — ReplyTool; no-op when ConversationRef is null (HeartbeatJob compatibility)

### New infrastructure
- `infrastructure/telegram/telegram-channel.ts` — ChannelGateway via Bot API sendMessage
- `infrastructure/session/jsonl-session-repository.ts` — state.json + per-session JSONL under SHRIMP_STATE_DIR
- `infrastructure/config/env-config.ts` — add CHANNELS_ENABLED, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SHRIMP_STATE_DIR (validated only when Channels enabled)
- `container.ts` — Channels-gated registration of Telegram/Session/Reply/ChannelJob

### Filesystem layout
- `~/.shrimp/state.json` — current session pointer
- `~/.shrimp/sessions/<uuid>.jsonl` — append-only per-session
- state.json malformed → fail fast; JSONL corrupt → discard and start fresh; missing state.json → treated as "no current session"

## SPEC.md alignment checklist
- Channel concept (§Channel Integration) generic; Telegram is the first implementation
- Slash Commands bypass Job Queue (§Slash Commands); only `/new` supported
- Single global Session, lazy creation on first non-command message (§Session Lifecycle)
- ChannelJob flow steps 1-7 (§Event-Driven Trigger Flow)
- gen_ai.conversation.id = Session ID on ChannelJob; absent on HeartbeatJob (§Telemetry Emission)
- Fail-Open Recovery for reply failures, JSONL append failures (§Session Lifecycle)
- Fail-fast for state.json malformed, missing required env vars when Channels enabled
- Env vars: CHANNELS_ENABLED, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SHRIMP_STATE_DIR

## Progress Table

Ordering rationale: dependency-root first (env + value objects + ports), then leaf infra (SessionRepository), then core refactor (Job split + ShrimpAgent history), then outbound (ChannelGateway impl + ReplyTool), then inbound (HTTP route + slash dispatch), then wiring + cross-cutting (DI + telemetry conversation.id), then test consolidation and doc alignment. Each Execute row = one /coding:write cycle ending with /git:commit per user's cadence.

| # | Item | Execute | Review | Sample | Notes |
|---|------|---------|--------|--------|-------|
| 1 | Env config: add CHANNELS_ENABLED, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SHRIMP_STATE_DIR with conditional validation (only when Channels enabled); create SHRIMP_STATE_DIR at startup, fail-fast on creation failure | done | done | pending | 0351b62; Haiku PASS cycle 18 |
| 2 | Entities / value objects: ConversationMessage (role + content) and ConversationRef (opaque value object) under src/entities/ | done | done | pending | 3a7d3f0; Haiku PASS cycle 19 |
| 3 | Port: SessionRepository (getCurrent, createNew, append) under use-cases/ports/ | done | done | pending | d976f16; Haiku PASS cycle 19 |
| 4 | Port: ChannelGateway (reply(ref, text)) under use-cases/ports/ | done | done | pending | c1bb916; Haiku PASS cycle 19 |
| 5 | Infrastructure: JsonlSessionRepository — state.json + sessions/<uuid>.jsonl under SHRIMP_STATE_DIR; handles missing/malformed state.json per SPEC (fail-fast on malformed; treat missing as no-session; JSONL corruption → discard + start fresh) | done | done | pending | fe02922; Haiku PASS cycle 18 (v4 UUID fallback acceptable per SPEC) |
| 6 | Refactor Job: split into abstract Job Worker skeleton + HeartbeatJob (current behavior, empty history) + ChannelJob (load Session → assemble prompt with incoming msg → invoke Agent with history → append new ConversationMessage entries); extend JobInput with history: ConversationMessage[]; HeartbeatRoute wires HeartbeatJob | done | done | pending | 85a8575; job.test.ts 52→10 cases; Haiku PASS cycle 17 |
| 7 | Use case: StartNewSession — archives previous Session, creates new, updates state.json; bypasses JobQueue; used by /new | done | done | pending | 96db748; Haiku PASS cycle 20 |
| 8 | Adapter (tool): ReplyTool under adapters/tools/built-in/ — calls ChannelGateway with ConversationRef; no-op when ref is null (HeartbeatJob path) | done | done | pending | 029c439; Haiku PASS cycle 20 |
| 9 | Infrastructure: TelegramChannel — implements ChannelGateway via Bot API sendMessage; Fail-Open on reply errors | done | done | pending | 0130167; Haiku PASS cycle 18 |
| 10 | Adapter (http): POST /channels/telegram — validates X-Telegram-Bot-Api-Secret-Token (401 on mismatch), validates payload (400 on malformed), 200 on accept; parses slash commands → StartNewSession / unknown-reply; non-command → ChannelJob enqueue; construct ConversationRef from update | done | done | pending | 355ba9e + 1ffed15 (zod fix); Haiku PASS cycle 17 (non-text 200 flag resolved) |
| 11 | DI wiring: container.ts Channels-gated registration of SessionRepository, ChannelGateway, ReplyTool, ChannelJob, StartNewSession; route mounted only when CHANNELS_ENABLED | done | done | pending | 1ffed15; Haiku PASS cycle 17 (registration order + fail-fast verified) |
| 12 | Telemetry: ChannelJob sets gen_ai.conversation.id = Session ID on shrimp.job span via AiSdkShrimpAgent; HeartbeatJob omits it | done | done | pending | 6d9a06d; Haiku PASS cycle 20 |
| 13 | Test consolidation — before/during/after implementation: audit tests/ for bloat, merge fine-grained unit tests into integration tests (MSW at HTTP boundary, tmpdir for filesystem, real ChannelJob + JsonlSessionRepository end-to-end), remove dead code paths; ensure Telegram webhook, Session lifecycle, Slash Command, ChannelJob flow all covered by integration tests | done | done | pending | 085b8dc + 7fb0440; Haiku PASS cycle 21 |
| 14 | Documentation: verify docs/architecture.md Component Contracts rows for TelegramChannel / JsonlSessionRepository / ReplyTool / ChannelJob / StartNewSession match implementation; touch up only if drift appears (keep high-level per memory) | done | done | pending | 07691e3; 3 drifts fixed: AiSdkShrimpAgent conversation.id (HARD), TelegramChannel responsibility (HARD), Failure Handling Channel row (SOFT); net +1 LOC; 446/446 pass |
| 15 | Further test consolidation (deferred from item 13): gen-ai-bridge-span-processor.test.ts (968 lines) — well-structured but large; audit whether the two `toGenAiInputMessages` + `toGenAiOutputMessages` pure-transform describe blocks can be condensed; todoist-board-repository.test.ts (788 lines) — check for per-field single-assertion patterns | done | done | pending | 46f8a45; Haiku PASS cycle 21 |
| 16 | .env.example: add CHANNELS_ENABLED, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SHRIMP_STATE_DIR with comments describing conditional validity and defaults (SHRIMP_STATE_DIR default ~/.shrimp). User flagged this was missed during items 1/11 — .env.example currently has NO Channel-related entries. | done | done | pending | 0c63803; doc-only, skipped review (no SPEC behavior at risk) |
| 17 | Reply-delivery redesign fix landed (commit a430705): ChannelJob now delivers each assistant ConversationMessage via ChannelGateway.reply directly; ReplyTool + per-invocation ref-threading removed (architecture mistake per user feedback cycle 22); AiSdkShrimpAgent always uses `messages` shape. 436/436 pass, typecheck clean. | done | in_progress | pending | a430705; fixes two bugs: first-message error + missing replies |
| 18 | SPEC.md review & alignment (per user request cycle 22): run /spec-review → /spec-write → /git:commit. Verify SPEC.md §Channel Integration, §Session Lifecycle, §Slash Commands, §Telemetry Emission are consistent with the reply-delivery redesign (item 17). Specifically: SPEC currently describes "Reply tool" as the mechanism for outbound replies — that tool no longer exists; the delivery is now a direct Job Worker responsibility. Architecture.md row for ReplyTool also needs removing (was added in item 14). | done | in_progress | pending | ed3847b; 6 drifts across SPEC.md + architecture.md fixed; 436/436 pass |
| 19 | Strengthen reply delivery (user-reported cycle 24): Langfuse shows the agent produced a reply but ChannelGateway sometimes never fires. Root cause suspected: AiSdkShrimpAgent's `newMessages` is built from `result.text` only, which is the final step's text in a multi-step tool loop — if the model emitted assistant text in an earlier step and ended on a tool call (or the provider returned empty trailing text), the earlier text is dropped. Fix: collect assistant text from ALL steps via `result.steps` (AI SDK exposes step-level content); or alternatively, iterate `result.response.messages` to pick all assistant-role entries. Verify shape against AI SDK types. Integration test: simulate a multi-step run where the assistant text is in step N-1 rather than step N, assert it still gets delivered. | reverted | pending | pending | dc2a6d0 (reverted by 484dd14): Strategy 1 was wrong — intermediate assistant text is scratch/thinking, must not be relayed. Delivery now mirrors OTel final output (result.text). 437/437 pass. |
| 20 | Delivery bug (user reported cycle 26): Langfuse shows final output, Telegram doesn't. Root cause: TelegramChannel only checked HTTP `resp.ok`; Telegram Bot API returns 200 + `{ok:false, error_code, description}` on app-layer failures (message >4096 chars, bad chat_id, etc.) — we silently dropped. Fix: parse body after 2xx, check `body.ok`, log warn with error_code+description on false; still Fail-Open. +1 MSW case. | done | pending | pending | c4d6ecc; 438/438 pass |
| 21 | Follow-up: long-reply truncation. Current behavior: when `result.text` > 4096 chars Telegram returns `ok:false` and we log+drop. User sees nothing. Consider chunking the text into ≤4096 char messages and sending them sequentially, or truncating with an ellipsis. Not urgent — only affects very long agent replies. | in_progress | pending | pending | Identified by item 20 fix as residual risk |

Completeness check against SPEC.md:
- Endpoint POST /channels/telegram with secret + response table → item 10
- Channel concept (generic, Telegram first) → items 4, 9, 10
- Event types (Message vs Slash Command) → items 10, 7
- Slash Commands §/new only, bypass JobQueue → items 7, 10
- Session Lifecycle (lazy creation, rotation, failure matrix) → items 3, 5, 7
- ChannelJob flow (load → assemble → run → append) → item 6
- JobQueue variant-agnostic acceptance → item 6 (no JobQueue change)
- Telemetry gen_ai.conversation.id = Session ID for ChannelJob → item 12
- Fail-Open Recovery for replies / JSONL append → items 5, 8, 9
- Env vars CHANNELS_ENABLED / TELEGRAM_BOT_TOKEN / TELEGRAM_WEBHOOK_SECRET / SHRIMP_STATE_DIR with conditional validation → item 1
- Webhook registration is external (out of scope) — noted, no item
- DI wiring Channels-gated → item 11
- Test consolidation user requirement → item 13
- Architecture doc alignment → item 14

All SPEC.md Channel/Session/Slash-Command/Telemetry clauses and Implementation Direction bullets map to at least one row.

## Log Table

New entries MUST be appended at the bottom (ascending Cycle order). Do not prepend.

| Cycle | Phase | Summary | Decision | Handoff |
|-------|-------|---------|----------|---------|
| 1 | plan | Decomposed goal into 14 items ordered dependency-root-first (env/ports/entities → session repo → job split → outbound adapters → inbound route → DI/telemetry → test consolidation → doc alignment). Mapped every SPEC.md Channel/Session/Slash-Command/Telemetry clause to at least one item. | Treat test consolidation (item 13) as a parallel concern: consolidate tests as each earlier item lands rather than as a separate big-bang pass, so dead-code tests get removed as their subjects change. | Next cycle enters execute phase with item 1 (env config). |
| 2 | execute | Item 1 complete. Added channelsEnabled + telegramBotToken? + telegramWebhookSecret? + shrimpStateDir to EnvConfig; mkdirSync on enable with fail-fast; 444/444 tests pass, typecheck clean. Commit 0351b62. | Kept shrimpStateDir always-present (with default) rather than optional — downstream code simpler; one-line comment explains the choice. | Next cycle: item 2 (entities — ConversationMessage + ConversationRef). Pure value objects under src/entities/; no runtime deps. ConversationMessage has role + content at minimum; ConversationRef is opaque to use-cases. |
| 3 | execute | Item 2 complete. Added ConversationMessage (role: "user"|"assistant" + content: string) and ConversationRef ({channel, payload} with opacity comment). No tests — pure types. 444/444 pass. Commit 3a7d3f0. | Role union of user/assistant only (SPEC says "message history"; system/tool entries live inside AI SDK loop). ConversationRef shape (A) channel+payload over branded opaque string — Telegram needs multi-field payload. | Next cycle: item 3 (Port: SessionRepository). Thin interface. Methods per SPEC §Session Lifecycle: getCurrent/createNew/append. Session shape: { id; messages }. No infrastructure code yet. |
| 4 | execute | Item 3 complete. SessionRepository port with Session type and three methods (getCurrent / createNew / append). JSDoc encodes SPEC failure semantics. 444/444 pass. Commit d976f16. | Kept `append` returning void even though SPEC says Fail-Open; intent documented not typed. | Next cycle: item 4 (Port: ChannelGateway). Single method `reply(ref, text)`. Fail-Open in JSDoc. Imports only ConversationRef. No tests. |
| 5 | execute | Item 4 complete. ChannelGateway port with single `reply(ref, text)` method; Fail-Open semantics in JSDoc. 444/444 pass. Commit c1bb916. | — | Next cycle: item 5 (Infrastructure: JsonlSessionRepository). Real impl under src/infrastructure/session/. Full failure matrix per SPEC §Session Lifecycle. Tmpdir integration test, ~6 focused cases. |
| 6 | execute | Item 5 complete. JsonlSessionRepository with state.json + sessions/<id>.jsonl; write-then-rename atomicity; append() is Fail-Open; getCurrent handles missing/malformed/corrupt per SPEC matrix. 6-case tmpdir integration test (107 lines). 450/450 pass. Commit fe02922. | Constructor-injection of { stateDir, logger } (plain class, no decorators). Write-then-rename for state.json to protect fail-fast on malformed from partial writes. | Next cycle: item 6 — the big structural refactor. Split Job into HeartbeatJob + ChannelJob. Extend JobInput with history + optional sessionId. ChannelJob loads Session, appends user msg, invokes agent, appends assistant newMessages back. Don't touch container.ts Channels wiring yet. Consolidate job.test.ts. |
| 7 | execute | Item 6 complete. Deleted src/use-cases/job.ts, added heartbeat-job.ts and channel-job.ts. JobInput extended with `history` + optional `sessionId`; ShrimpAgentResult extended with `newMessages`. AiSdkShrimpAgent converts history to ModelMessage and prepends; TODO(item 12) comment for sessionId stamping. TOKENS.Job → TOKENS.HeartbeatJob. 52-case job.test.ts consolidated into 10 integration cases across two files. 430/430 pass. Commit 85a8575. | Duplication over abstract base class (each Job's unique work dominates shared boilerplate). ChannelJob appends incoming user msg before agent call so transcript is preserved even if agent fails. history = pre-append snapshot; userPrompt = incoming message. | Next cycle: item 7 (StartNewSession use case). Thin class delegating to SessionRepository.createNew(). Invoked by Telegram webhook on `/new` (item 10 wires it). Keep trivial; no tests. |
| 8 | execute | Item 7 complete. StartNewSession use case: thin class delegating to SessionRepository.createNew(); one info log; returns new Session. No tests — no branching; path is integration-tested by JsonlSessionRepository + to-be item-10 webhook test. 430/430 pass. Commit 96db748. | Method named `execute()` (generic use-case verb) to distinguish from Job Worker `run()`, since StartNewSession bypasses the Job Queue per SPEC §Slash Commands. | Next cycle: item 8 (ReplyTool). Built-in tool under src/adapters/tools/built-in/reply.ts. Calls ChannelGateway.reply(ref, text). No-op when ref is null (HeartbeatJob path). Needs per-invocation ref injection — update ToolProviderFactory.create(context?) to forward ref. |
| 9 | execute | Item 8 complete. ReplyTool built-in tool with message:string input; threads ConversationRef through ToolProviderFactory.create(context?) → BuiltInToolFactory → createBuiltInTools. NoopChannelGateway registered as default stub so BuiltInToolFactory can resolve ChannelGateway unconditionally; item 11 will swap in TelegramChannel when CHANNELS_ENABLED. 3-case reply.test.ts (56 lines) + 1 extension to channel-job.test.ts. 434/434 pass. Commit 029c439. | Option (b) Noop default gateway (precedent: NoopTelemetry in architecture.md) so BuiltInToolFactory stays injection-clean — rejected lazy resolution or conditional injection as extra mechanism for same outcome. | Next cycle: item 9 (TelegramChannel — real ChannelGateway impl). Under src/infrastructure/channel/telegram-channel.ts (co-located with noop-channel-gateway.ts). Constructor: { botToken, logger }. reply(ref, text) calls Telegram Bot API sendMessage with chat_id from ref.payload; Fail-Open: log and swallow on error. Use fetch (no extra deps). MSW integration test ~4 cases: happy path, network error swallowed, upstream 400 swallowed, ref.channel≠"telegram" behavior. ref.payload shape: `{ chatId: number }`. |
| 10 | execute | Item 9 complete. TelegramChannel adapter at src/infrastructure/channel/telegram-channel.ts; fetch-based POST to api.telegram.org/bot<token>/sendMessage; Fail-Open on both upstream non-2xx and network errors (log warn + return, never throw). Exports `TELEGRAM_CHANNEL_NAME = "telegram"` for item 10 to reuse. 4-case MSW integration test (94 lines) following existing todoist test pattern. 438/438 pass. Commit 0130167. | Guard `ref.channel !== TELEGRAM_CHANNEL_NAME` returns early with warn — protects against misrouting if future adapters share the same ChannelGateway registration. Do not parse Telegram response body (we only care about request success). | Next cycle: item 10 — POST /channels/telegram webhook. New adapter src/adapters/http/routes/channels/telegram.ts. Validate `X-Telegram-Bot-Api-Secret-Token` header (401 on mismatch); validate payload shape (400 on malformed); 200 on accept. Parse Telegram update JSON → extract message text + chat.id. If text begins with `/`: Slash Command path (bypass Job Queue) — `/new` → invoke StartNewSession + send confirmation via ChannelGateway; unknown command → send short error reply; both use ConversationRef `{channel: TELEGRAM_CHANNEL_NAME, payload: {chatId}}`. Non-command → tryEnqueue(() => channelJob.run({message, ref})) via JobQueue; always 200 regardless of enqueue acceptance (matches SPEC: silent drop when busy). Integration test with supertest-style Hono `app.request` + MSW for bot API; ~5-6 cases (happy non-command, /new success, /new failure, unknown command, missing secret → 401, malformed payload → 400). Route is NOT mounted yet on main app (item 11 gates). |
| 11 | execute | Item 10 complete. POST /channels/telegram webhook at src/adapters/http/routes/channels/telegram.ts (96 lines); 8-case integration test via Hono `app.request` (183 lines). Covers 401/400/200 response table + /new + unknown command + non-command enqueue + non-text skip. SubAgent self-reported 446/446 pass. Commit 355ba9e. Route NOT mounted in app.ts yet — item 11 gates on CHANNELS_ENABLED. | SubAgent treated absent-but-parseable `message` (no text/chat) as "acknowledge and skip" (200). BUT their justification claimed zod "strips invalid nested objects by default" — that's not zod behavior; safeParse should FAIL on partial message. Means a non-text-but-has-chat update (e.g. photo-only) may currently hit 400 rather than 200. FLAG for review: confirm SPEC-preferred behavior + fix schema to use `z.object({...}).partial()` or `passthrough` or handle the branch explicitly. | Next cycle: item 11 (DI wiring — Channels-gated registration). Update src/container.ts: when `envConfig.channelsEnabled === true`, register SessionRepository (JsonlSessionRepository with env.shrimpStateDir), ChannelGateway (TelegramChannel with env.telegramBotToken) — **replacing** the NoopChannelGateway default; register ChannelJob; register StartNewSession. In src/adapters/http/app.ts: when channelsEnabled, mount createTelegramRoute with webhookSecret=env.telegramWebhookSecret. Keep HeartbeatJob flow untouched. No new token symbols beyond those already added (ChannelGateway token exists from item 8; add SessionRepository / ChannelJob / StartNewSession tokens if missing in infrastructure/container/tokens.ts). Fail-fast at startup if registrations blow up when channelsEnabled. Integration test: container.test.ts or app.test.ts — when channelsEnabled=true, /channels/telegram responds; when false, /channels/telegram → 404. |
| 12 | execute | Item 11 complete. Channel-feature DI wiring behind CHANNELS_ENABLED: bootstrap() registers JsonlSessionRepository/ChannelJob/StartNewSession and swaps NoopChannelGateway → TelegramChannel only when enabled; app.ts accepts optional `channels` deps and mounts /channels/telegram conditionally; server.ts resolves and passes through. NoopChannelGateway registration moved from module-level into bootstrap() to avoid double-registration. Also fixed the item-10 zod concern inline: `text: z.string().optional()` + `!msg?.text` guard → photo-only messages now 200 (new test case added). 450/450 pass. Commit 1ffed15. | Chose option (a) — remove module-level Noop registration and handle both branches inside bootstrap — cleaner than runtime-override. Verified BuiltInToolFactory's @inject(ChannelGateway) still resolves (bootstrap registers gateway before ToolProviderFactory step 8). | Next cycle: item 12 (telemetry gen_ai.conversation.id for ChannelJob). AiSdkShrimpAgent already accepts optional `sessionId` (plumbed in item 6 with TODO comment). Now stamp `gen_ai.conversation.id = sessionId` on the `shrimp.job` span when present. HeartbeatJob passes no sessionId → attribute omitted. Check how AiSdkShrimpAgent currently annotates the span (the agent-level gen_ai.* attrs are set there). Update GenAiBridgeSpanProcessor if that's where conversation.id should live — SPEC §Telemetry Emission says `shrimp.job` span carries gen_ai.conversation.id; confirm which layer stamps it. Integration test: extend aiSdkShrimpAgent test (or a light telemetry-focused one) to assert the attribute is set when sessionId is present and absent when not. Small change — likely ~30 LOC + 2 test cases. |
| 13 | execute | Item 12 complete. Fixed SPEC-violating telemetry in AiSdkShrimpAgent: replaced unconditional `gen_ai.conversation.id = jobId` with conditional `sessionId`-based stamping. ChannelJob (sessionId present) → attribute set to Session ID. HeartbeatJob (sessionId absent) → attribute omitted. Preserved per-invocation correlation as new `shrimp.job.id` attribute. Removed TODO(item 12) comment; updated block comment to cite SPEC §Telemetry Emission. Replaced 2 stale tests asserting old jobId behavior; added 3 new test cases: ChannelJob path, HeartbeatJob path, shrimp.job.id always-present. 451/451 pass. | No design decisions needed — SPEC was explicit. The only subtlety: the old "shrimp.job.id" attr is the first `shrimp.*` span attr in this repo; it's SPEC-compatible per §Job §Job ID "domain-scoped correlation key" wording. | Next cycle: item 13 (Test consolidation). |
| 14 | execute | Item 13 complete. Audited all test files for bloat. Targeted 3 files: env-config (5 per-var throw tests → it.each; TELEMETRY_RECORD_INPUTS + OUTPUTS twin blocks → describe.each); get-tasks (3 per-section call tests + 3 schema-accept tests → it.each; merged return-results assertion into call assertion); move-task (same per-section + schema pattern → it.each). -136 LOC across 3 files (7064→6928 total); 446/446 pass (was 451; -5 from collapsed cases with no coverage loss). Channel test files untouched. Deferred gen-ai-bridge-span-processor + todoist-board-repository to item 15. | Consolidation scope: only clear per-attribute single-assertion patterns and near-identical sibling blocks. Did not touch gen-ai-bridge (968 lines, well-structured, each case tests distinct behavior). did not touch todoist-board-repository (requires deeper audit). | Next cycle: item 14 (Documentation alignment). |
| 15 | execute | Item 14 complete. Audited docs/architecture.md end-to-end against landed code (items 1-13). Found 3 drifts: (1) HARD — AiSdkShrimpAgent row cited `conversation.id` unconditionally; updated to reflect conditional stamping (ChannelJob only) and always-present shrimp.job.id per SPEC §Telemetry Emission. (2) HARD — TelegramChannel row incorrectly claimed it validates webhooks and normalises updates; that work belongs to TelegramRoute (HTTP adapter); row now describes only sendMessage + Fail-Open. (3) SOFT — Failure Handling table had no row for Channel reply / JSONL append Fail-Open; added one row. All other Channel rows (HeartbeatJob/ChannelJob, StartNewSession, JsonlSessionRepository, ReplyTool, SessionRepository port, ChannelGateway port, Use Cases layer description) matched code — no change needed. Net +1 LOC on architecture.md (formatter reflowed table widths). 446/446 pass; typecheck clean. Commit 07691e3. | No decisions needed — all edits were in-place row rewrites or one new Failure Handling row within the existing table. | Item 15 (further test consolidation) is the remaining pending item. |
| 16 | execute | Item 15 complete. Outcome A (partial) + B (partial). todoist-board-repository.test.ts consolidated via it.each: -80 LOC net (298→218 equivalent changes); per-priority mapping + per-section patterns merged, all SPEC-driven error/success branches preserved. gen-ai-bridge-span-processor.test.ts left untouched (item-13's verdict confirmed — each case asserts a distinct attribute-translation rule; consolidating would lose coverage). Dispatcher had to repair SubAgent's incomplete edit (stale `vi` import + it.each signature missing third tuple element `_label`); followed by commit 46f8a45. 446/446 pass; typecheck clean. ALL Execute items (1-15) now done → current_phase = review. | SubAgent was truncated mid-run; dispatcher applied the final two type-safety fixes and committed the consolidation work that was already complete. Preserved the "breadth not bloat" ruling on gen-ai-bridge. | Next cycle enters the review phase. Per phase rules: pick 2-3 pending Review items, two-stage scan (Haiku broad → Sonnet verify), spawn fixer SubAgent (Sonnet) on any FAIL. 15 items to work through; at 2-3 per cycle, review phase spans ~6 cycles. Priority order suggestion: start with the highest-risk items — 6 (Job split), 10 (webhook), 11 (DI wiring) — since those have the most surface area for SPEC drift. |
| 17 | review | Review batch 1: items 6/10/11 (highest-risk). 3 Haiku broad scans in parallel, ALL PASS. Item 6 (Job split): HeartbeatJob + ChannelJob lifecycles align with SPEC §Event-Driven Trigger Flow; ShrimpAgentResult.newMessages properly flows; no decorators on use cases. Item 10 (Telegram webhook): 200/400/401 response table correct; non-text 200 acknowledge works (zod fix from cycle 12); /new bypasses JobQueue; silent-drop on enqueue refusal. Item 11 (DI wiring): CHANNELS_ENABLED gating works (404 when off); fail-fast when required Telegram env vars missing; ChannelGateway registered before ToolProviderFactory so BuiltInToolFactory @inject resolves cleanly. No SUSPECTs → no Stage 2 Sonnet verify or fixer spawned. review_cycles = 1. | No fixes required this batch — implementation held up under SPEC-focused scan. | Next review cycle: items 1 (env config), 5 (JsonlSessionRepository), 9 (TelegramChannel adapter). After that: items 2/3/4 (ports + entities — trivial, can bundle with 7/8/12/13/14/15 in one or two later cycles since they are small). Total remaining review items: 12. Projected 4-5 more review cycles. |
| 18 | review | Review batch 2: items 1/5/9. 3 Haiku broad scans in parallel, ALL PASS. Item 1 (env config): conditional validation mirrors telemetry pattern exactly; mkdir fail-fast with recursive flag; 9 test cases cover all branches. Item 5 (JsonlSessionRepository): all 4 SPEC failure modes (missing/malformed state.json, missing/corrupt JSONL, append Fail-Open) correctly implemented; randomUUID is v4 fallback (SPEC-acceptable). Item 9 (TelegramChannel): Fail-Open on both non-2xx and network errors; ref.channel defensive guard; MSW test covers 4 critical paths. No SUSPECTs → no Stage 2 or fixer. review_cycles = 2. | No fixes required. Noted SPEC-acceptable preference: v4 UUID is fallback, v7 preferred when Node exposes natively — future-state item if Node changes. | Next review cycle (batch 3): bundle the trivial items — 2 (entities), 3 (SessionRepository port), 4 (ChannelGateway port), 7 (StartNewSession), 8 (ReplyTool). All are small; a single Haiku scan per item is fast. After batch 3: remaining items 12 (telemetry conversation.id), 13+15 (test consolidation), 14 (doc drift) — bundle into batch 4. Projected 2 more review cycles then transition to sample phase. |
| 19 | review | Review batch 3: items 2/3/4 (entity + two ports). 3 Haiku broad scans in parallel, ALL PASS. Item 2 (entities): ConversationMessage role-union limited correctly; ConversationRef has opacity comment; no dead-code tests. Item 3 (SessionRepository port): methods match SPEC exactly; JSDoc encodes SPEC failure matrix; role-based naming. Item 4 (ChannelGateway port): Fail-Open documented; single outbound path verified shared by Reply tool + slash handler. No SUSPECTs. review_cycles = 3; 9/15 reviewed. | — | Next cycle (batch 4): items 7 (StartNewSession), 8 (ReplyTool), 12 (telemetry conversation.id). All straightforward. Remaining after that: 13 (test consolidation), 14 (doc drift), 15 (deferred consolidation). 1-2 more review cycles then sample phase. |
| 20 | review | Review batch 4: items 7/8/12. 3 Haiku broad scans in parallel, ALL PASS. Item 7 (StartNewSession): createNew delegation correct; archive implicit; bypasses JobQueue via direct call from webhook route; doesn't swallow errors (adapter handles). Item 8 (ReplyTool): ref-threading integrity end-to-end (Heartbeat no-context; Channel forwards ref); no-op semantics when ref null; NoopChannelGateway fallback registered before ToolProviderFactory. Item 12 (telemetry conversation.id): conditional stamping correct; shrimp.job.id always present; 3 test cases validate both branches. review_cycles = 4; 12/15 reviewed. | — | Next cycle (batch 5, last): items 13 (test consolidation), 14 (doc drift), 15 (deferred consolidation). All three are "meta" items — audits/refactors rather than new components. After that: current_phase = sample. |
| 21 | review | Review batch 5 (final for items 1-15): items 13 and 15. 2 Haiku broad scans, ALL PASS. Item 13: env-config / get-tasks / move-task consolidation preserved every SPEC-driven assertion (required vars, per-section schema + repo calls); -136 LOC / -5 tests legitimate. Item 15: todoist-board-repository -80 LOC via it.each preserved priority/section/comment behaviors; gen-ai-bridge correctly left untouched (each case tests distinct attribute translation). review_cycles = 5. ALL items 1-15 now Review = done. | USER FEEDBACK MID-CYCLE: .env.example was never updated with CHANNELS_ENABLED / TELEGRAM_BOT_TOKEN / TELEGRAM_WEBHOOK_SECRET / SHRIMP_STATE_DIR during items 1 or 11. Added as new item 16 (pending Execute). current_phase reverted to execute to handle it before moving to sample. | Next cycle: item 16 execute — add the 4 new env vars to .env.example with short comments describing conditional validity and SHRIMP_STATE_DIR default (~/.shrimp). No code change; just a doc/example file update. After commit, mark Execute=done, skip Review (doc file — no SPEC behavior at risk) or give it one quick Haiku scan, then current_phase → sample. |
| 22 | execute | Item 16 complete. Added CHANNELS_ENABLED / TELEGRAM_BOT_TOKEN / TELEGRAM_WEBHOOK_SECRET / SHRIMP_STATE_DIR (commented-out; default ~/.shrimp) to .env.example with 4-line comment block explaining conditional validity. Dispatcher did this directly — 13-line edit didn't warrant a SubAgent. Commit 0c63803. Skipped Review (doc file, no SPEC behavior under test). All 16 items now Execute=done AND Review=done. | Skipped spawning Haiku scan for item 16 — a comment-only .env.example change has no SPEC-behavior surface to drift. Marking Review=done by inspection. | current_phase → sample. Next cycle starts sample batch 1: per phase rules, randomly pick 2-3 items, Haiku broad scan → Sonnet verify on any SUSPECT. sample_passes target = 10. Suggest first random pick weighted toward the items with most surface area (6 Job split, 10 webhook, 11 DI wiring, 5 JsonlSessionRepository) since sample is meant to catch drift between test assertions and actual behavior. |
| 23 | execute | User reported two runtime bugs from real Telegram test: (1) first message didn't reach agent → error + reply not saved; (2) subsequent messages got no reply. Root cause: reply delivery depended on the model calling the `reply` tool, which the model typically doesn't do; and AiSdkShrimpAgent used `{ prompt }` shape on empty history, tripping provider validators when `instructions` is also set. Fix landed as item 17 (commit a430705): ChannelJob now delivers each assistant ConversationMessage via ChannelGateway.reply directly; ReplyTool + per-invocation ref-threading removed; AiSdkShrimpAgent always uses `messages` shape. 436/436 pass. Added item 18 per user: run /spec-review → /spec-write → /git:commit to align SPEC.md with the redesigned reply-delivery model (SPEC + architecture.md still reference the removed ReplyTool). | User explicitly called out: "回覆訊息不該依賴 Agent 而是要把最終回傳發給使用者" — Job Worker owns reply delivery, not the agent. Removed ReplyTool entirely (cleaner than keeping it as dead code). | Next cycle: item 18 (SPEC.md alignment). Spawn SubAgent (Sonnet) with /spec-review → /spec-write → /git:commit cadence. Target sections: §Channel Integration (ConversationRef + reply routing), §Session Lifecycle (ChannelJob flow step 5 mentions "the agent may emit replies via the Reply tool during the loop" — no longer true), §Job (ChannelJob execution lifecycle step 3 says same), architecture.md §Component Contracts ReplyTool row (should be deleted). Also check §Glossary for ReplyTool references. After spec-aligned: revert current_phase → review (items 17, 18 need review) then sample. |
| 24 | execute | Item 18 complete. SPEC + architecture.md aligned with the reply-delivery redesign: 6 drifts fixed (§Glossary ConversationRef, §Event-Driven Trigger Flow ChannelJob step 5, §Channel Integration dispatch rules, §Slash Commands dispatch semantics, §Job ChannelJob lifecycle step 3, architecture.md §Component Contracts ReplyTool row deleted + ChannelJob row updated). `grep -in "reply tool\|replytool" SPEC.md docs/architecture.md` → 0. ChannelGateway port mentions stay. 436/436 pass. Commit ed3847b. | No prose sections added — all edits in-place. | USER FEEDBACK CYCLE 24: Langfuse sometimes shows reply generated but not delivered → added item 19. Suspected cause: AiSdkShrimpAgent builds newMessages from result.text only, which is the final step's text; multi-step tool loops can leave assistant text in earlier steps. Next cycle: investigate AI SDK's result shape (result.steps, result.response.messages, result.content), collect all assistant text across steps, return as newMessages so ChannelJob delivers them all. Add integration test that simulates multi-step where trailing step is tool-call or empty. |
| 25 | execute | Item 19 complete. AiSdkShrimpAgent now walks `result.response.messages`, filters to `role === "assistant"`, and flattens each non-empty assistant turn into its own ConversationMessage. Strategy 1 picked (response.messages is the canonical assembled transcript per AI SDK docs; result.text is only the last step's text). Telemetry gen_ai.output.messages still reflects the full assistant output via "\n\n" join. 2 new integration cases: (a) assistant text in step N-1 with empty final step; (b) multiple non-empty assistant turns → 2 ConversationMessages. Edge case: MockLanguageModelV3 array-mode had an off-by-one bug, worked around with function-based doGenerate + manual counter. 438/438 pass, typecheck clean. Commit dc2a6d0. | Strategy 1 chosen over iterating result.steps — response.messages is higher-level and already assembled, matching how chat transcripts work (one assistant turn per bubble). Tool-only turns (no text parts) correctly skipped by the empty-string filter. | All Execute items (1-19) now done. Revert current_phase → review to cover new items 17, 19 (item 18 is docs-only but worth a light re-check); then sample. Next cycle: review batch for items 17 (ChannelJob direct-reply redesign), 18 (SPEC/doc alignment), 19 (multi-step assistant collection). Skip SPEC-behavior scan for item 18 since it doesn't touch code. |
