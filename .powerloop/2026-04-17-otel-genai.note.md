---
goal: Complete OTEL gen_ai semantic conventions coverage for AI SDK integration. Current state (per commits fb82dc1, 5537dbf) emits partial gen_ai attributes, but tool calls / tool results and other LLM interaction details are not fully translated. Finish the gen_ai conversion so OTEL consumers (e.g. Langfuse) can fully trace LLM operation—input/output, tool invocations, tool results, and related spans. Research via WebSearch; do NOT use $(), WebFetch, or other permission-prompting shell/tool syntax.
language: en
current_phase: sample
started_at: 2026-04-17T00:00:00+08:00
interval: 10m
cron_id: c9930473
execute_skills: /coding:refactor → /coding:review → /coding:refactor → /git:commit
review_skills: /coding:review → /coding:refactor → /git:commit
sample_passes: 3/5
review_cycles: 6
---

# OTEL gen_ai Coverage — Progress Note

## Progress Table

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                           | Execute | Review  | Sample  | Notes                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Introduce `GenAiBridgeSpanProcessor` in `src/infrastructure/telemetry/` — an OTel `SpanProcessor` whose `onEnd` reads `ai.*` attrs and writes `gen_ai.*` equivalents (no other behavior). Skeleton, wiring into `NodeSDK` via `spanProcessors`, unit tests with a fake `ReadableSpan`.                                                                                                         | done    | done | pending | Done cycle 4. Skeleton in place, `onEnd` no-op TODO for items #2–#5. NodeSDK wiring: `spanProcessors: [bridge, BatchSpanProcessor(exporter)]`. |
| 2   | Bridge: `ai.toolCall` span → `gen_ai.operation.name=execute_tool`, `gen_ai.tool.name` (from `ai.toolCall.name`), `gen_ai.tool.call.id` (from `ai.toolCall.id`), `gen_ai.tool.type=function`. Also set span kind/name guidance per semconv. Tests for each mapping.                                                                                                                             | done        | done | pending | Done. Mutation via direct `attrs` map write (cast to Attributes) — `setAttribute` is a no-op post-end. Extracted `translateToolCallSpan` + `setIfAbsent` helpers. 5 new test cases pass. 52 total tests green. |
| 3   | Bridge: tool call args/result → `gen_ai.tool.call.arguments` (from `ai.toolCall.args` JSON) and `gen_ai.tool.call.result` (from `ai.toolCall.result` JSON). Respect `recordInputs`/`recordOutputs` — if AI SDK already omitted them, bridge must not invent values. Tests for gating.                                                                                                          | done | done | pending | AI SDK already gates these on its recordInputs/recordOutputs                                                                                   |
| 4   | Bridge: `ai.generateText.doGenerate` (and `ai.streamText.doStream`) span → `gen_ai.operation.name=chat`, `gen_ai.provider.name` (mirror `gen_ai.system`). Tests.                                                                                                                                                                                                                               | done | done | pending | Added `isChatSpan` guard (Set lookup), `translateChatSpan` helper, wired in `onEnd` after tool-call translators. 6 new tests. 64 total pass. Typecheck clean. Dispatcher shape #1 (each translator self-contained). |
| 5   | Bridge: generate structured `gen_ai.input.messages` (from `ai.prompt.messages` array: user/system/assistant with parts including tool_call parts, plus tool role for tool results) and `gen_ai.output.messages` (from `ai.response.text` + `ai.response.toolCalls`). Tests across shapes.                                                                                                      | done | done | pending | Added `translateChatMessages`, `toGenAiInputMessages`, `toGenAiOutputMessages` (exported pure fns), `mapAiSdkPart` helper. 20 new tests (7 pure transform + 13 integration). 84 total pass. Typecheck clean. reasoning/file parts dropped silently via default case in mapAiSdkPart. arguments/response passed as objects (no re-stringify). JSON parse errors caught, silently skipped. |
| 6   | On `shrimp.main-agent` span in `AiSdkMainAgent`: add `gen_ai.operation.name="invoke_agent"`, `gen_ai.agent.name="shrimp.main-agent"`, `gen_ai.provider.name` (from providerName); set `error.type` on caught exceptions. Tests updated.                                                                                                                                                        | done | done | pending | 4 new string constants; 3 attr writes after gen_ai.system; error.type before setStatus. 5 new tests (28 total). Typecheck clean, format unchanged. |
| 7   | Remove deprecated ad-hoc attrs from `AiSdkMainAgent`: `gen_ai.system`, `gen_ai.prompt`, `gen_ai.completion` (all superseded by bridge-emitted `gen_ai.provider.name` + `gen_ai.input.messages` / `gen_ai.output.messages`). Delete corresponding test assertions; stop passing recordInputs/recordOutputs as legacy-alias toggles — keep them only as they flow into `experimental_telemetry`. | done | done | pending | Removed 3 constants, 1 comment block, 2 conditional setAttribute blocks, 1 unconditional setAttribute (gen_ai.system). Deleted 3 legacy tests + 1 stale error-path assertion. 409 tests pass, typecheck clean, format unchanged. |
| 8   | Docs: refresh comment block in `ai-sdk-main-agent.ts` (explain structured gen_ai emission + bridge processor responsibility) and add a short note in `docs/architecture.md` / SPEC.md telemetry section describing the bridge. No feature changes in this item.                                                                                                                                | done        | done | pending | 5-line comment above ATTR_* constants in ai-sdk-main-agent.ts; GenAiBridgeSpanProcessor row added to component table + directory listing in architecture.md; gen_ai semconv paragraph + component table row added to SPEC.md Telemetry Emission + Telemetry Design sections. Typecheck clean, 409 tests pass, format unchanged. All 8 execute items done. |
| 9   | Emit agent-level `gen_ai.input.messages` and `gen_ai.output.messages` on the `shrimp.main-agent` span so trace-root consumers (Langfuse etc.) can show the overall agent invocation's Input/Output. Input = `[{role:"system"...},{role:"user"...}]` built from `MainAgentInput.systemPrompt` + `userPrompt`. Output = `[{role:"assistant", parts:[{type:"text", content: result.text}]}]` built from the AI SDK result. Gate on `recordInputs`/`recordOutputs`. Reuse item #5's exported `toGenAiOutputMessages` where applicable. Tests. | done | done | pending | Imported `toGenAiOutputMessages` from gen-ai-bridge-span-processor (infra-to-infra). Added 2 constants (`ATTR_GEN_AI_INPUT_MESSAGES`, `ATTR_GEN_AI_OUTPUT_MESSAGES`). Input built inline (system+user array). Output via `toGenAiOutputMessages(result.text, [])`. 5 new tests covering success (recordInputs/recordOutputs true), gating false combos, and error path (input present, output absent). 414 total tests pass. Typecheck clean, format unchanged. |
| 10  | Bridge: `ai.prompt.tools` (AI SDK JSON-stringified array of tool definitions) → `gen_ai.tool.definitions` on chat spans. Value pass-through (AI SDK already stringifies). Gate on source attr presence; setIfAbsent guard. Tests for presence, absence, and non-chat-span skip. | done | done | pending | Added `translateChatTools(span)` helper (~8 lines). Option A chosen: pass `ai.prompt.tools` value through verbatim (it's already a `string[]` of JSON-stringified tool defs per AI SDK index.mjs L4307–4310; no re-serialization needed). Dispatched from `onEnd` after `translateChatMessages`. 5 new tests in `describe("tool definitions bridge")`: string[] present→copied, absent→not set, non-chat span→not mapped, pre-existing preserved, single-string future-shape robustness. 89 total tests pass. Typecheck clean. Format unchanged. |
| 11  | Audit gen_ai semconv coverage: systematically diff our current emission vs OTel gen_ai spec (spans, events, registry attrs) and Langfuse's documented attribute expectations. Produce a gap list; convert each gap into a new execute item. No code changes — this is a planning/research item. | done | done | pending | Audit produced 3 OTel-aligned follow-up items (#12–#14). Langfuse-only aliases skipped per user directive. `gen_ai.conversation.id` deferred pending user decision (touches MainAgentInput boundary). Event-based emission deferred (currently spec-compliant via span-attr fallback). |
| 12  | Design and spec the **Heartbeat ID** mechanism in `SPEC.md` via `/spec-write` methodology. Definition: a stable identifier generated by Shrimp at the start of each Processing Cycle, carried through to the Main Agent invocation, and suitable for correlating spans / logs / external observability as a conversation-scoped key. Spec must cover: where generated, format (UUID v7 or similar), lifetime (per-cycle), contracts it affects (`ProcessingCycle`, `MainAgentInput`), and relation to OTel `gen_ai.conversation.id`. No code changes in this item. | done | done | pending | Priority: must-have prerequisite for #13. Size: S. Use /spec-write skill workflow (Intent / Scope / Behavior / Refinement); keep scope tight — this is an ID-generation contract, not a session-state feature. |
| 13  | Implement the Heartbeat ID per spec (#12): generate in `ProcessingCycle` (UUID v7 from `node:crypto` or equivalent), add `heartbeatId: string` to `MainAgentInput`, thread through all callers, emit `gen_ai.conversation.id = heartbeatId` on `shrimp.main-agent` span (new constant in `AiSdkMainAgent`). Tests for generation uniqueness, propagation, and span attr emission. | done | done | pending | UUID v4 via `crypto.randomUUID()` (TODO comment for v7 when Node exposes it). Generated before `runInSpan` in `ProcessingCycle.run()`. `ATTR_GEN_AI_CONVERSATION_ID` constant added; written unconditionally after `ATTR_GEN_AI_PROVIDER_NAME`. 4 new tests (UUID shape, uniqueness across cycles, conversation.id on success, conversation.id when recordInputs/recordOutputs=false). 423 total tests pass. Typecheck clean. |
| 14  | Rename span names to follow `{operation} {model}` / `{operation} {agent.name}` format per semconv SHOULD. Bridge rewrites chat span names (`ai.generateText.doGenerate` → `chat {gen_ai.request.model}`). `AiSdkMainAgent` renames outer span (`shrimp.main-agent` → `invoke_agent shrimp.main-agent`). Update tests. | done | done | pending | Bridge rename feasible via direct `span.name` field assignment (same pattern as attributes). `updateName` is a no-op post-end (Span.js L241), but `SpanImpl.name` is a plain public field (Span.d.ts L42) — cast via `span as unknown as { name: string }`. Foreign-span guard: only rewrites spans starting with `ai.`. Outer rename via `span.updateName("invoke_agent shrimp.main-agent")` before first setAttribute. `makeRecordingTracer.updateName` fixed to record new name. 6 new tests (1 outer-span, 5 bridge canonicalization). 429 total tests pass. Typecheck clean. |
| 15  | Emit `gen_ai.agent.id` and `gen_ai.agent.version` on `shrimp.main-agent` span. `agent.id` from a stable constant, `agent.version` from package.json or a `SHRIMP_VERSION` env var. Tests. | done | done | pending | WebSearch confirmed: agent.id is a stable unique identifier (not human-readable name); agent.name remains "shrimp.main-agent". Chose option (a): hardcoded UUID v4 constant `SHRIMP_MAIN_AGENT_ID` identifying the Shrimp Main Agent type across all deployments. Version resolved via `import pkg from "../../../package.json"` (resolveJsonModule=true). 2 new attr constants + 2 file-level constants + 2 setAttribute calls (unconditional, placed after agent.name). 3 new tests: UUID format regex, semver format regex, present regardless of recordInputs/recordOutputs. 432 total tests pass (was 429). Typecheck clean. Format unchanged. |
| 16  | Bridge `gen_ai.usage.cache_read.input_tokens` on chat spans from AI SDK's cache-read token attr (grep `node_modules/ai/dist/index.mjs` for `cache` to find the exact attr name). Skip batch if AI SDK doesn't expose cache tokens. Tests. | done | done | pending | WebSearch confirmed: cache_read=`gen_ai.usage.cache_read.input_tokens`, cache_write=`gen_ai.usage.cache_creation.input_tokens` (NOT cache_write). Both standard. Added `translateUsageTokens(span)` helper (14 lines). Source: `ai.usage.inputTokenDetails.cacheReadTokens` + `ai.usage.inputTokenDetails.cacheWriteTokens`. Zero-value correct via `Number.isFinite`. 7 new tests (cases 1–7). 439 total pass. Typecheck clean. ALL 16 execute items done — phase should flip to review next cycle. |

## Log Table

| Cycle | Phase   | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Decision                                                                                                                                                                                                                                                                                                                                                                                         | Handoff                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | plan    | Initial plan draft (14 items, mostly duplicated AI SDK's built-in gen_ai emission).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                                                                                                                                                                                                                                                | Superseded by cycle 3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2     | plan    | Verified actual AI SDK emission by grepping node_modules/ai/dist/index.mjs: AI SDK already sets gen_ai.system, gen_ai.request.{model,max_tokens,temperature,top_k,top_p,...}, gen_ai.response.{model,id,finish_reasons}, gen_ai.usage.{input_tokens,output_tokens} on ai.generateText.doGenerate / ai.streamText.doStream spans. ai.toolCall spans only carry ai.toolCall.{name,id,args,result}. Rewrote plan around a GenAiBridgeSpanProcessor.                                                                                                         | Single translator, no duplication with AI SDK, community-standard pattern. Initially kept legacy gen_ai.prompt/completion aliases for Langfuse.                                                                                                                                                                                                                                                  | Superseded by cycle 3 (user directive).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4     | execute | Item #1: created `src/infrastructure/telemetry/gen-ai-bridge-span-processor.ts` (pure `SpanProcessor` skeleton, no-op `onEnd` with TODO for items #2–#5). Wired into `OtelTelemetry` via `NodeSDK({ spanProcessors: [bridge, BatchSpanProcessor(exporter)] })` so bridge runs before export. Added `@opentelemetry/sdk-trace-base` as a direct dep. New tests `tests/infrastructure/telemetry/gen-ai-bridge-span-processor.test.ts` (instantiation, onStart/onEnd safety, shutdown/forceFlush resolve). `pnpm typecheck` clean, 48 telemetry tests pass. | Explicit `spanProcessors` option replaces deprecated `traceExporter` shortcut — required because we now prepend a custom processor. Bridge ordered before `BatchSpanProcessor` so attribute writes land before export. Dependency was already transitive; promoted to direct for explicit imports.                                                                                               | Next cycle = execute item #2 (map `ai.toolCall` → `gen_ai.operation.name=execute_tool`, `gen_ai.tool.name`, `gen_ai.tool.call.id`, `gen_ai.tool.type=function`). Implementation site: `onEnd` in gen-ai-bridge-span-processor.ts. Read span attrs via `span.attributes`, write with `(span as unknown as Span).setAttribute(...)` or, cleaner, via the mutable `span.attributes` map — confirm which SpanProcessor contract permits (ReadableSpan is read-only; may need to cast to Span or use a different hook). Worth a quick check in @opentelemetry/sdk-trace-base at start of next batch. Add targeted tests per mapping. |
| 3     | plan    | User directive: go OTel-standard only; no backend-specific (Langfuse) aliases. Dropped legacy gen_ai.prompt / gen_ai.completion retention. Added new items: #6 now includes gen_ai.provider.name on main-agent span; new item #7 deletes deprecated ad-hoc attrs from AiSdkMainAgent + their test assertions. Final plan = 8 items: bridge processor (1) + bridge mappings (2–5) + main-agent additions (6) + legacy cleanup (7) + docs (8).                                                                                                             | Structured gen_ai.input.messages / gen_ai.output.messages (per semconv) become the canonical input/output surface. Deprecated flat gen_ai.prompt / gen_ai.completion are removed, not aliased. Bridge is the single translator; AiSdkMainAgent only adds what the bridge cannot know from AI SDK attrs alone (invoke_agent operation, agent.name, provider.name, error.type). Sample target = 5. | Next cycle = execute item #1 (skeleton GenAiBridgeSpanProcessor in src/infrastructure/telemetry/gen-ai-bridge-span-processor.ts, wire into OtelTelemetry NodeSDK via spanProcessors, unit tests with a fake ReadableSpan in tests/infrastructure/telemetry/). Use /coding:refactor first for SRP shape. Self-review = pnpm typecheck + pnpm test tests/infrastructure/telemetry/. One /git:commit.                                                                                                                                                                                                                              |
| 6     | execute | Item #3: added `translateToolCallArgsResult(span, toolCall)` helper. Grepped AI SDK: both `ai.toolCall.args` and `ai.toolCall.result` are emitted as already-JSON-stringified values via `selectTelemetryAttributes({ output: () => JSON.stringify(...) })` (index.mjs L2801-2803, L2871-2873), and BOTH are gated on the single `recordOutputs` flag — not `recordInputs` separately. Bridge passes through verbatim (no re-stringify). Extracted shared `isToolCallSpan(span)` guard; `onEnd` computes once, passes bool to both translators. 6 new tests (both present / args-only / result-only / neither / non-tool-call with stray ai.toolCall.args / pre-existing preserved). 58 tests pass, typecheck clean. | Value shape = string (pass-through). One `recordOutputs` gate (AI SDK quirk — not recordInputs), so our 6 items description was slightly wrong. No behavior bug in the bridge itself. | Next cycle = execute item #4 (map `ai.generateText.doGenerate` / `ai.streamText.doStream` → `gen_ai.operation.name=chat`, `gen_ai.provider.name` mirroring `gen_ai.system`). Same file. Add `translateChatSpan(span)` helper and dispatch based on span name. Do NOT touch request/response attrs AI SDK already emits. |
| 5     | execute | Item #2: implemented `translateToolCallSpan` in `onEnd`. Verified `SpanImpl.setAttribute` guards on `_isSpanEnded()` — so cast-to-Span approach is a silent no-op. Chose direct map mutation (`attrs as Attributes`, then `(attrs as Record<string, unknown>)[key] = value`) instead. Extracted `translateToolCallSpan(span)` and `setIfAbsent(attrs, key, value)` helpers (SRP: each translator will be its own function, `onEnd` just dispatches). Removed stale "no mutations yet" test, added 5 new test cases covering all required scenarios. 52 total tests pass, typecheck clean, format clean. | Direct map mutation is the only viable path because `setAttribute` is a no-op post-`end()`. `setIfAbsent` guards against overwriting already-present gen_ai.* attrs. | Next cycle = execute item #3 (bridge `ai.toolCall.args` → `gen_ai.tool.call.arguments` and `ai.toolCall.result` → `gen_ai.tool.call.result`, both gated on attr presence to respect AI SDK's recordInputs/recordOutputs). Same file, same pattern — add `translateToolCallArgsResult(span)` helper, call from `onEnd`, add tests. |
| 7     | execute | Item #4: added `CHAT_SPAN_NAMES` Set constant (`ai.generateText.doGenerate`, `ai.streamText.doStream`), `isChatSpan(span)` guard, and `translateChatSpan(span)` helper. Confirmed via AI SDK index.mjs grep: these two are the inner LLM-call spans (lines 4291, 7164); orchestration wrappers `ai.generateText`/`ai.streamText` (lines 4127, 6911) do NOT carry `gen_ai.system` and are excluded. Also confirmed `ai.generateObject.doGenerate` and `ai.streamObject.doStream` exist but are out of scope for item #4. Dispatcher shape #1 kept (each translator self-contained; `onEnd` calls each in sequence). 6 new test cases: generateText.doGenerate with system / streamText.doStream with system / absent gen_ai.system / orchestration wrapper skipped / tool-call span unaffected / pre-existing operation.name preserved. 64 total tests pass, typecheck clean, format clean. | Shape #1 preserved — translators are self-contained, `onEnd` reads naturally top-to-bottom. No detection duplication since each function owns its own guard. | Next cycle = execute item #5 (gen_ai.input.messages + gen_ai.output.messages from ai.prompt.messages / ai.response.text / ai.response.toolCalls). |
| 8     | execute | Item #5: added `translateChatMessages(span)` dispatcher + two exported pure fns `toGenAiInputMessages` / `toGenAiOutputMessages` + private `mapAiSdkPart` helper. Typed AI SDK message shapes with `AiSdkMessage`, `AiSdkPart`, `GenAiPart` interfaces (no `any`). reasoning/file/unknown part types return `null` from `mapAiSdkPart` (default case), filtered out; messages that become empty are dropped. `arguments`/`response` passed as-is objects (no re-stringify). Both fns exported for direct unit tests. JSON.parse wrapped in try/catch, silently skip on error. `onEnd` adds one call: `translateChatMessages(span)`. 20 new test cases (7 pure toGenAiInputMessages + 5 pure toGenAiOutputMessages + 8 integration). 84 total tests pass, typecheck clean, format clean. | `toGenAiInputMessages` and `toGenAiOutputMessages` exported — required so test file can import and unit-test them directly without span setup. All other helpers remain private. | Next cycle = execute item #6 (gen_ai.operation.name=invoke_agent, gen_ai.agent.name, gen_ai.provider.name, error.type on shrimp.main-agent span in AiSdkMainAgent). |
| 9     | execute | Item #6: added 4 string constants (`ATTR_GEN_AI_OPERATION_NAME`, `ATTR_GEN_AI_AGENT_NAME`, `ATTR_GEN_AI_PROVIDER_NAME`, `ATTR_ERROR_TYPE`) to the existing ATTR_* block in `ai-sdk-main-agent.ts`. Added 3 `setAttribute` calls immediately after `gen_ai.system` write (operation.name, agent.name, provider.name). Added `error.type` setAttribute in catch block before `setStatus`. Existing deprecated attrs (gen_ai.system/prompt/completion) untouched — item #7 owns removal. 5 new tests in `describe("gen_ai semantic conventions")` block: operation.name on success, agent.name on success, provider.name on success, error.type on throw, provider.name present on throw. 28 total tests pass, typecheck clean, format unchanged. | No new abstractions — pure minimal additions as specified. error.type placed before setStatus so attr survives regardless of span lifecycle. | Next cycle = execute item #7 (remove deprecated gen_ai.system/prompt/completion from AiSdkMainAgent and their test assertions). |
| 10    | execute | Item #7: deleted 3 deprecated constants (`ATTR_GEN_AI_SYSTEM`, `ATTR_GEN_AI_PROMPT`, `ATTR_GEN_AI_COMPLETION`) and the misleading comment block explaining legacy retention. Removed `span.setAttribute(ATTR_GEN_AI_SYSTEM, ...)`, the `if (this.recordInputs) { span.setAttribute(ATTR_GEN_AI_PROMPT, ...) }` block, and `if (this.recordOutputs) { span.setAttribute(ATTR_GEN_AI_COMPLETION, ...) }`. Moved `import type` statements above `const` declarations (import-ordering fix). In tests: deleted 3 legacy tests (`gen_ai.system/prompt/completion on success`, `omit gen_ai.prompt when recordInputs false`, `omit gen_ai.completion when recordOutputs false`) and removed tautological `not.toHaveProperty("gen_ai.completion")` from the error-path test. `recordInputs`/`recordOutputs` fields and their flow into `experimental_telemetry` preserved. 409 tests pass. `pnpm typecheck` clean. `pnpm format` no changes. | Pure deletion batch — no new logic. Class is structurally smaller: `run()` no longer has conditional setAttribute blocks inside `startActiveSpan`, only the 3 new semconv attrs + agent + try/catch remain. | Next cycle = execute item #8. |
| 11    | execute | Item #8: docs-only batch. Added 5-line comment above `ATTR_*` block in `ai-sdk-main-agent.ts` stating the split (agent-level attrs here, LLM-call/tool-call/messages in GenAiBridgeSpanProcessor). Added GenAiBridgeSpanProcessor to `docs/architecture.md` telemetry directory listing + component table (single responsibility + spanProcessors ordering). Added gen_ai semconv paragraph + component-table row to SPEC.md Telemetry section; updated AiSdkMainAgent row to reflect agent-level gen_ai annotations (spec-to-reality reconciliation). Typecheck clean, 409 tests pass. All 8 execute items done; phase flipped to review. | Docs link to code paths rather than duplicate content. SPEC.md AiSdkMainAgent row updated to match reality per items #6/#7 — deliberate spec amendment, not scope creep. | Next cycle = review phase batch: pick lowest 2–3 pending Review items (#1, #2, #3). Haiku broad scan → Sonnet verify SUSPECT. |
| 12    | review  | Review batch 1: items #1, #2, #3 — three Haiku broad-scan SubAgents dispatched in parallel. All returned PASS; no SUSPECT items surfaced, so no Stage-2 Sonnet verification needed. Marked Review=done for #1–#3. review_cycles=1. | Haiku stage 1 is sufficient when code is straightforward and tests are tight — skip Stage 2 to save cycles. | Superseded by cycle 13 — user feedback triggered scope extension. |
| 13    | plan    | User feedback: after removing deprecated gen_ai.prompt/completion from the outer span (item #7), Langfuse-style trace lists lost outer Input/Output preview. Inner chat spans still carry structured messages (via bridge), but trace-root consumers read from the outer span. Per gen_ai-agent-spans semconv, `invoke_agent` spans MAY carry gen_ai.input.messages / gen_ai.output.messages representing the overall agent call — complementing, not duplicating, the inner per-turn messages. Added new item #9 to emit these on `shrimp.main-agent`. Phase reverted to execute. | Item #9 is standard-compliant (not Langfuse-specific). Inner spans remain authoritative for per-LLM-turn detail; outer span summarises the whole agent invocation. Reuse `toGenAiOutputMessages` export from item #5 to avoid reimplementing output shape. | Next cycle = execute item #9. Touch only `src/infrastructure/ai/ai-sdk-main-agent.ts` + its test. Gate on recordInputs/recordOutputs already on the class. Build input array inline from systemPrompt + userPrompt (no bridge involvement — this data lives on the agent, not the AI SDK span). |
| 14    | plan    | User feedback #2: Langfuse's Tools panel not marking tool invocations. Diagnosis: AI SDK emits `ai.prompt.tools` (JSON-stringified tool schemas) on chat spans, but we never bridge it to `gen_ai.tool.definitions` — Langfuse's available-tools list reads from the gen_ai attr and stays empty. Separate from `gen_ai.output.messages` tool_call parts (per-invocation), which ARE emitted by item #5. Added item #10 (single mapping) + item #11 (broader gen_ai coverage audit — user concern we might be missing more gaps). | Treat Langfuse mismatches as a symptom of incomplete gen_ai coverage, not as Langfuse-specific work. Item #11 is a deliberate audit-before-patch gate so we stop discovering gaps piecemeal. Item #10 is small enough to batch before the audit. | Next cycles = execute item #9, then #10, then #11 (audit only — produces new items, no code). After #11, any discovered items get appended as #12+, then phase → review. |
| 15    | execute | Item #9: imported `toGenAiOutputMessages` from gen-ai-bridge-span-processor (infra-to-infra import, permitted). Added 2 constants (`ATTR_GEN_AI_INPUT_MESSAGES`, `ATTR_GEN_AI_OUTPUT_MESSAGES`). Input messages built inline as system+user array, written before `new ToolLoopAgent(...)`, gated on `this.recordInputs`. Output messages via `toGenAiOutputMessages(result.text, [])`, written after `agent.generate()` returns, gated on `this.recordOutputs`. On throw path: input is already written (pre-generate), output block is never reached. Updated comment block to mention overall input/output.messages. 5 new tests in `gen_ai semantic conventions` block: success+recordInputs=true (parsed structure check), success+recordOutputs=true (parsed structure check), recordInputs=false (no input attr, output present), recordOutputs=false (input present, no output attr), error path (input present, output absent). 414 total tests pass (was 409). Typecheck clean. Format unchanged. | Reuse decision: YES — `toGenAiOutputMessages(text, [])` fits exactly; empty toolCalls is valid since agent-level output is text-only. Infra-to-infra import does not break clean architecture. Inline build for input is 7 lines — too small to extract helper. | Next cycle = execute item #10 (bridge `ai.prompt.tools` → `gen_ai.tool.definitions` on chat spans). |
| 16    | execute | Item #10: added `translateChatTools(span)` helper (~8 lines) at end of `gen-ai-bridge-span-processor.ts`, dispatched from `onEnd` after `translateChatMessages`. Option A chosen: value passed verbatim (AI SDK already emits `ai.prompt.tools` as `string[]` of JSON-stringified tool defs, L4307–4310 in index.mjs; no re-serialization needed). Guard: `isChatSpan(span)` reused, `if (tools == null) return` for absence, `setIfAbsent` for pre-existing protection. 5 new tests in `describe("tool definitions bridge")`: string[] present→copied, absent→not set, non-chat span (ai.toolCall)→not mapped, pre-existing preserved, single-string future-shape robustness. 89 total tests pass (was 84). Typecheck clean. Format unchanged. | Option A: lossless pass-through avoids unnecessary parse+re-stringify; downstream consumers decode per element. Documented inline. | Next cycle = execute item #11 (audit gen_ai semconv coverage — planning/research, no code). |
| 17    | execute | Item #11 (audit): SubAgent WebSearched OTel gen_ai spec + Langfuse OTel integration docs; grep-verified current emission against `src/`. Produced 6-gap report. After user-directive filter (OTel-standard-first): 3 items appended (#12 span naming, #13 agent.id/version, #14 cache tokens). Langfuse-only aliases dropped. `gen_ai.conversation.id` deferred — presented to user. Event-based emission deferred (currently spec-compliant via span-attr fallback). | User response: add Heartbeat ID mechanism to serve as conversation.id source, spec'd via /spec-write first. Renumbered: new #12 (spec) + #13 (impl) inserted; existing audit items shifted to #14-#16. | Next cycle = execute item #12 (Heartbeat ID spec in SPEC.md). |
| 18    | execute | Item #12: SubAgent followed /spec-write skill methodology (Intent/Scope/Behavior/Refinement + IS-NOT). Edits landed in `SPEC.md`: glossary row for Heartbeat ID; Processing Cycle role-contract row; new dedicated Heartbeat ID subsection with a 6-row contract table (Purpose / Generation / Lifetime / Propagation / OTel mapping / Failure); 5-bullet IS-NOT list distinguishing it from OTel trace ID, user session, and external-caller input; Main Agent Input row updated to reference `heartbeatId`. No code/tests touched. 419 tests still pass, typecheck clean. | Spec placed Heartbeat ID under Processing Cycle (not Telemetry) because generation lives there; telemetry emission is a consumer, not the owner. IS-NOT list is explicit about NOT-an-OTel-trace-ID and NOT-a-user-session to prevent scope creep in #13's implementation. | Next cycle = execute item #13 (implement: generate UUID in ProcessingCycle → MainAgentInput.heartbeatId → gen_ai.conversation.id on shrimp.main-agent span). |
| 19    | execute | Item #13: Implemented Heartbeat ID. `heartbeatId: string` added to `MainAgentInput` with JSDoc. `randomUUID()` imported from `node:crypto` in `ProcessingCycle`; called at top of `run()` before `runInSpan` (spec: before task selection). ID threaded into `MainAgentInput.heartbeatId`. `ATTR_GEN_AI_CONVERSATION_ID = "gen_ai.conversation.id"` constant added to `AiSdkMainAgent`; `span.setAttribute` placed unconditionally after `ATTR_GEN_AI_PROVIDER_NAME` write and before `recordInputs` gate. `baseInput` in `ai-sdk-main-agent.test.ts` updated with fixed UUID. 4 new tests: UUID shape regex in processing-cycle; uniqueness across two successive cycles; `gen_ai.conversation.id` equals `heartbeatId` on success; `gen_ai.conversation.id` present even when recordInputs=false + recordOutputs=false. 423 total tests pass (was 419). Typecheck clean. Format unchanged. | UUID v4 via Node builtin (TODO for v7). Generation placed before `runInSpan` in `ProcessingCycle.run()` per spec requirement (before task selection). Correlation ID always recorded — not gated on recordInputs/recordOutputs. | Next cycle = review item #13 (or execute item #14 — span name rename). |
| 20    | execute | Item #14: Span name canonicalization. Verified: `updateName` is a no-op post-end (Span.js L241 guards `_isSpanEnded()`), but `SpanImpl.name` is a plain public `string` field (Span.d.ts L42) — same direct-assignment trick as for `attributes`. Bridge rename feasible. Outer span: `span.updateName("invoke_agent shrimp.main-agent")` called first in startActiveSpan callback before any setAttribute. Bridge: `renameToCanonicalForm(span)` helper added — dispatches on `isChatSpan`/`isToolCallSpan`; renames to `chat {gen_ai.request.model}` or `execute_tool {gen_ai.tool.name}` respectively; skips if model/name absent; foreign-span guard (`!span.name.startsWith("ai.")`). `makeRecordingTracer.updateName` fixed to persist new name into `record.name`. `findMainAgentSpan` updated to match renamed span. 6 new tests: outer-span rename to `invoke_agent shrimp.main-agent`; 5 bridge canonicalization (chat with model, chat without model, tool-call with name, tool-call without name, foreign span). 429 total tests pass (was 423). Typecheck clean. Format unchanged. | Direct `name` field mutation justified: identical runtime situation as attribute mutation — TypeScript interface `readonly` is compile-time only, underlying JS object is mutable. `renameToCanonicalForm` placed last in `onEnd` so gen_ai attrs (written by prior translators) are available for the name computation. | Next cycle = review batch (items #4–#14 pending review). |
| 21    | execute | Item #15: `gen_ai.agent.id` + `gen_ai.agent.version` on outer span. WebSearch confirmed: agent.id is a stable unique identifier (not human-readable; example in spec is an OpenAI assistant ID). Chose option (a): hardcoded UUID v4 constant `SHRIMP_MAIN_AGENT_ID = "e3a7c2f1-..."` placed in module scope with explanatory comment. Version from `import pkg from "../../../package.json"` (resolveJsonModule=true, no tsconfig change needed). 2 new ATTR_* string constants + 2 module-level constants + 2 unconditional `setAttribute` calls (after agent.name, before provider.name). 3 new tests: UUID regex match, semver regex match, both present regardless of recordInputs/recordOutputs. 432 total tests pass (was 429). Typecheck clean. Format unchanged. | agent.id UUID is type-level (identifies the Shrimp Main Agent implementation), not instance-level — never changes unless a fundamentally new agent implementation replaces it. | Next cycle = review batch (items #4–#15 pending review). |
| 22    | execute | Item #16: Cache token bridge. WebSearch confirmed both attrs are in OTel gen_ai standard: `gen_ai.usage.cache_read.input_tokens` (cache-read) and `gen_ai.usage.cache_creation.input_tokens` (cache-write; NOT cache_write). Added `translateUsageTokens(span)` helper (14 lines). Source attrs: `ai.usage.inputTokenDetails.cacheReadTokens` + `ai.usage.inputTokenDetails.cacheWriteTokens` (canonical path preferred over `cachedInputTokens` alias to avoid double-counting). Zero-value guard: `Number.isFinite` (not falsy check). Dispatched from `onEnd` after `translateChatTools`, before `renameToCanonicalForm`. 7 new tests covering: read=42 mapped, absent→not set, read=0 mapped (zero is legitimate), non-chat span not mapped, setIfAbsent preserves pre-existing, write=128 mapped, both present mapped independently. 439 total tests pass (was 432). Typecheck clean. Format unchanged. | Both cache attrs are standard — both bridged. `gen_ai.usage.cache_creation.input_tokens` is the correct OTel name for what AI SDK calls `cacheWriteTokens`. | ALL 16 execute items done. Phase should flip to review next cycle. |
