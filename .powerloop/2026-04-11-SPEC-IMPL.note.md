---
goal: 依據 SPEC.md 與 docs/ 架構文件，使用 /coding:write 實作 SPEC.md 所要求的最小可用功能，持續推進直到完成目前所需的最小實作
current_phase: completed
started_at: 2026-04-11T00:00:00+08:00
interval: 10m
cron_id: 23fd3517
execute_skills: /coding:write → /coding:review → /coding:refactor → /git:commit
review_skills: /coding:review → /coding:refactor → /git:commit
sample_passes: 10/10
review_cycles: 6
---

# SPEC.md Minimum Implementation Progress

## Context Snapshot

- **Spec**: `SPEC.md` — heartbeat-triggered Todoist task processor, single-slot in-memory queue, AI-driven Main Agent, built-in Todoist tools + MCP extension.
- **Architecture**: `docs/architecture.md` — four-layer Clean Architecture (`entities/`, `use-cases/`, `adapters/`, `infrastructure/`), ports wired by tsyringe.
- **Current state**: only `GET /health` route + Hono app bootstrap exist at `src/app.ts`, `src/server.ts`, `src/routes/health.ts`. Everything else is unimplemented.

## Decomposition Strategy

Ordered to respect Clean Architecture dependency direction (inner layers first), and to allow each item to be TDD'd as a vertical slice via `/coding:write`:

1. **Entities first** (no deps) — pure domain types + policy.
2. **Ports next** (use-case interfaces) — unblocks parallel adapter/infra work.
3. **Use cases** (`PromptAssembler`, `MainAgent`) — unit-testable against fake ports.
4. **Infrastructure + adapters** — implementations behind the ports.
5. **Composition roots last** — `container.ts`, `app.ts`, `server.ts` wiring.

## Progress Table

| #   | Item                                                                                                                        | Execute | Review  | Sample  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ------- | ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Entities — `Section`, `Priority`, `Task`, `Comment` value objects + `TaskSelector` policy                                   | done    | done    | pending | Commit `063d9ad`. Review PASS — TaskSelector correctly implements SPEC selection rules (InProgress first, priority order, API response tie-break)                                                                                                                                                                                                                                                                                     |
| 2   | Use-case ports — `BoardRepository`, `AgentLoop`, `TaskQueue`, `ToolProvider` interfaces                                     | done    | done    | pending | Commit `b673eb5` + `be034ea` (extensions). Review PASS — interface-only, role-based naming, `ToolDescription` extracted to break reverse coupling                                                                                                                                                                                                                                                                                     |
| 3   | `PromptAssembler` use case — builds system prompt (goal + tools) + user prompt (task context + comments)                    | done    | done    | pending | Commit `c8b018d`. Review PASS — pure function, correct SPEC alignment, section label mapping and edge cases covered                                                                                                                                                                                                                                                                                                                   |
| 4   | `MainAgent` use case — Processing Cycle: select task, promote Backlog→InProgress, assemble prompts, invoke `AgentLoop` once | done    | done    | pending | Commit `be034ea`. Review PASS — thin entry point, Backlog→InProgress move before loop, AgentLoop called exactly once, BoardSectionMissingError handled cleanly                                                                                                                                                                                                                                                                        |
| 5   | `InMemoryTaskQueue` infra — single-slot concurrency gate, fail-open slot release                                            | done    | done    | pending | Commit `e6d6af3`. Review PASS — single-slot semantics correct, `finally`-based release, no error propagation, all SPEC behaviors covered in tests                                                                                                                                                                                                                                                                                     |
| 6   | `EnvConfig` infra — loads + validates required env vars, fail-fast on missing                                               | done    | done    | pending | Commit `0e8ebf3`. Review PASS — pure function, aggregated error, forgiving fallback for `AI_MAX_STEPS`/`PORT` on absent/non-integer/non-positive                                                                                                                                                                                                                                                                                      |
| 7   | `McpConfig` infra — parses `.mcp.json`, fail-fast on malformed, empty servers allowed                                       | done    | done    | pending | Commit `fb1c89d`. Review PASS — pure parser separated from file loader, all invalid shapes throw `McpConfigError`, tmp-file test for loader path                                                                                                                                                                                                                                                                                      |
| 8   | `TodoistClient` infra — raw REST client (list tasks by section, list comments, post comment, move task)                     | done    | done    | pending | Commit `690d0e0`. Review PASS — clean HTTP wrapper, no domain leakage, injected fetch, `TodoistApiError` with status/URL/body excerpt. (Prior "unused getCallArgs" note was a stale LSP hint — helper is actively used throughout tests)                                                                                                                                                                                              |
| 9   | `TodoistBoardRepository` infra — implements `BoardRepository` port; maps sections Backlog/In Progress/Done                  | done    | done    | pending | Commit `bd3b73d`. Review PASS — priority inversion formula `(5-n)` verified at both extremes, exact section names, `BoardSectionMissingError` applied to both getTasks and moveTask                                                                                                                                                                                                                                                   |
| 10  | Built-in tools — `GetTasks`, `GetComments`, `PostComment`, `MoveTask` inbound adapters                                      | done    | done    | pending | Commits `7e01264` + `b8ebdd6` (review cleanup). Review PASS on re-scan — `section-map.ts` single source, `helpers.ts` single `makeFakeRepo`, 4 new `createBuiltInToolDescriptions()` tests verified                                                                                                                                                                                                                                   |
| 11  | `ToolRegistry` adapter — implements `ToolProvider`; merges built-in + MCP tools                                             | done    | done    | pending | Commit `e0ac571`. Review PASS — pure merger, built-in wins via reverse spread, exact port compliance, all merge/collision cases covered                                                                                                                                                                                                                                                                                               |
| 12  | `McpToolLoader` infra — starts MCP clients from `.mcp.json`, excludes failing servers, exposes AI SDK tool defs             | done    | done    | pending | Commit `b4365f6`. Review PASS — per-server fail-open isolation via try/catch, `close()` best-effort via `Promise.allSettled`, injected factory prevents subprocess spawning in tests                                                                                                                                                                                                                                                  |
| 13  | `AiSdkAgentLoop` infra — implements `AgentLoop` via AI SDK tool loop; wires `ToolProvider`, respects `AI_MAX_STEPS`         | done    | done    | pending | Commit `a38a75f`. Review PASS — uses `ToolLoopAgent` (not `generateText`), injected `LanguageModel`, fresh agent per call, correct finishReason mapping, all 6 termination reasons tested                                                                                                                                                                                                                                             |
| 14  | `POST /heartbeat` route adapter — 202 semantics, delegates to `TaskQueue` which runs `MainAgent` in background              | done    | done    | pending | Commit `a12d405`. Review PASS — factory pattern, same 202 for slot-free/busy, fire-and-forget, response does not await job, body-ignoring verified                                                                                                                                                                                                                                                                                    |
| 15  | Migrate `/health` route to `src/adapters/http/routes/health.ts`                                                             | done    | done    | pending | Commit `cc7ae33`. Review PASS — clean structural move, factory style matches heartbeat, no leftover shim                                                                                                                                                                                                                                                                                                                              |
| 16  | `container.ts` composition root — tsyringe bindings for all ports→impls                                                     | done    | done    | pending | Commit `3679248`. Review PASS — all ports wired correctly, only `server.ts` imports from it, ENOENT tolerance for `.mcp.json`, `ComposeOverrides` for independent test injection                                                                                                                                                                                                                                                      |
| 17  | `app.ts` / `server.ts` wiring — mount routes via container, initialize env/MCP at startup                                   | done    | done    | pending | Commit `9c69a67`. Review PASS — thin shell, dotenv first, graceful HTTP-then-MCP shutdown, no leftover `app.ts`                                                                                                                                                                                                                                                                                                                       |
| 18  | Rename `MainAgent` class → `ProcessingCycle`, `AgentLoop` port → `MainAgent`                                                | done    | pending | pending | Commit `376e123`. SPEC §Glossary + §Main Agent split: Main Agent = AI tool-loop invocation only; Processing Cycle = supervisor-side orchestration; Supervisor glossary entry added (= Shrimp process itself). Code rename: class `MainAgent`→`ProcessingCycle`, port `AgentLoop`→`MainAgent`, impl `AiSdkAgentLoop`→`AiSdkMainAgent`, all callers + docs/architecture.md + CLAUDE.md updated. 231 tests unchanged, `pnpm build` clean |

## Completeness Check

Every SPEC.md requirement maps to at least one item:

| SPEC Requirement                                 | Item(s)                                      |
| ------------------------------------------------ | -------------------------------------------- |
| `GET /health` (success criterion)                | #15 (already implemented, migrates location) |
| `POST /heartbeat` 202 semantics                  | #14                                          |
| In-memory single-slot queue                      | #5                                           |
| Task selection (In Progress > Backlog, priority) | #1 (`TaskSelector`), #4 (`MainAgent`)        |
| Backlog→In Progress promotion                    | #4, #9, #10 (`MoveTask`)                     |
| AI-driven execution via tool loop                | #4, #13                                      |
| Progress reporting via comments                  | #10 (`PostComment`), agent loop behavior     |
| Task completion (Move to Done)                   | #10 (`MoveTask`), agent loop behavior        |
| Built-in Todoist tools                           | #10                                          |
| MCP tool extension                               | #11, #12                                     |
| Prompt assembly rules                            | #3, #4                                       |
| Fail-Open Recovery on runtime errors             | #4, #5                                       |
| Fail-fast on config errors                       | #6, #7                                       |
| Missing Board sections → cycle ends              | #4, #9                                       |
| Todoist as source of truth                       | #9 (no local persistence)                    |
| Environment variable contract                    | #6                                           |
| `.mcp.json` contract                             | #7, #12                                      |
| tsyringe DI composition                          | #16                                          |

All SPEC §Scope IS-list items and Behavior rules are covered.

## Phase Log

- **2026-04-11 — Plan complete**: 17 items decomposed; table covers full SPEC scope. Transitioning to `execute` phase. Next scheduled cycle will pick item #1 (Entities).
- **2026-04-11 — Execute #1 done**: Entities layer landed in commit `063d9ad`. First SubAgent completed `/coding:write` + `/coding:review` but stopped before refactor/commit; follow-up SubAgent applied review findings (removed type-pinning tests and redundant comment) and committed. `pnpm test` 18 passing, `pnpm typecheck` clean. Next cycle picks item #2 (Ports).
- **2026-04-11 — Execute #2 done**: Use-case ports landed in commit `b673eb5`. Same pattern — primary SubAgent stopped at `/coding:review` with zero blocking findings, follow-up committed. Five port files including shared `tool-set.ts`. Typecheck clean, 18 tests still passing. Next cycle picks item #3 (`PromptAssembler`). Execute prompt for future cycles should more strongly emphasize "commit even when review finds nothing".
- **2026-04-11 — Execute #3 done**: `PromptAssembler` landed in commit `c8b018d`. SubAgent completed full `/coding:write → /coding:review → /coding:refactor → /git:commit` chain in one run (the stronger prompt worked). 16 behavioral tests, 34 total passing. Pure function, no I/O. Next cycle picks item #4 (`MainAgent`).
- **2026-04-11 — Execute #4 done**: `MainAgent` landed in commit `be034ea`. Primary SubAgent stopped at review again; follow-up SubAgent applied an extracted `ToolDescription` into `ports/tool-description.ts` (broke port→use-case reverse coupling flagged by review) and committed. 53 total tests passing. Use-case layer complete. Next cycle picks item #5 (`InMemoryTaskQueue` — first infrastructure item).
- **2026-04-11 — Execute #5 done**: `InMemoryTaskQueue` landed in commit `e6d6af3`. SubAgent completed full 4-step chain in one run. 6 new tests (59 total). Catches job errors internally to both (a) satisfy Fail-Open Recovery and (b) prevent vitest from surfacing unhandled rejections. Next cycle picks item #6 (`EnvConfig`).
- **2026-04-11 — Execute #6 done**: `EnvConfig` landed in commit `0e8ebf3`. Pure `loadEnvConfig(env)` with single aggregated `EnvConfigError`. 17 new tests (76 total). Next cycle picks item #7 (`McpConfig`).
- **2026-04-11 — Execute #7 done**: `McpConfig` landed in commit `fb1c89d`. Primary SubAgent stopped at review (3 low-severity non-blocking findings); follow-up SubAgent committed. Pure `parseMcpConfig(raw)` with thin `loadMcpConfig(path)` wrapper. 22 new tests (98 total). Config sub-layer complete. Next cycle picks item #8 (`TodoistClient`).
- **2026-04-11 — Execute #8 done**: `TodoistClient` landed in commit `690d0e0`. SubAgent completed full 4-step chain in one run. 26 new tests (124 total). Injected `FetchLike` for testability; raw snake_case types; `TodoistApiError` exported for repository. Left a minor unused `getCallArgs` helper in the test file (hint-level, not a typecheck failure). Next cycle picks item #9 (`TodoistBoardRepository`).
- **2026-04-11 — Execute #9 done**: `TodoistBoardRepository` landed in commit `bd3b73d`. Primary SubAgent stopped at review (zero findings); follow-up committed. 21 new tests (145 total). Todoist→domain priority inversion; exact SPEC section names; throws `BoardSectionMissingError` on missing destination section (not just source). Todoist integration complete. Next cycle picks item #10 (Built-in tools).
- **2026-04-11 — Execute #10 done**: Built-in tools landed in commit `7e01264`. Primary SubAgent stopped at review again; follow-up committed 12 files. Installed `ai@^6.0.158` + `zod@^4.3.6`. Four inbound tool adapters + `createBuiltInTools(repo)` factory + tests. 44 new tests (189 total). IDE showed stale zod-v3-style diagnostics for zod-v4 enum API but real `tsc` and `vitest` both pass. Next cycle picks item #11 (`ToolRegistry`).
- **2026-04-11 — Execute #11 done**: `ToolRegistry` landed in commit `e0ac571`. Primary SubAgent stopped at review (zero findings); follow-up committed. Pure merger; built-in wins on name collision. Added `createBuiltInToolDescriptions()` to built-in `index.ts` so the registry has symmetric tool+description merging. 7 new tests (196 total). Next cycle picks item #12 (`McpToolLoader`).
- **2026-04-11 — Execute #12 done**: `McpToolLoader` landed in commit `b4365f6`. Primary SubAgent stopped at review again; follow-up committed. Installed `@modelcontextprotocol/sdk@^1.29.0`. Injected `McpClientFactory` for testability; per-server try/catch isolates failures; `close()` is best-effort. 9 new tests (205 total). Tool layer extension complete. Next cycle picks item #13 (`AiSdkAgentLoop`).
- **2026-04-11 — Execute #13 done**: `AiSdkAgentLoop` landed in commit `a38a75f`. First draft incorrectly used `generateText` directly; user corrected — "原本的設計有 AI SDK 的 ToolLoopAgent 可用". Revised to use `ToolLoopAgent` class (AI SDK v6's native agent primitive, also aliased `Experimental_Agent`). Constructor-injected `LanguageModel`; fresh agent per `run()` with `stopWhen: stepCountIs(maxSteps)`; `systemPrompt` → `instructions`. 10 new tests (215 total). Infrastructure AI layer complete. Next cycle picks item #14 (`POST /heartbeat` route).
- **2026-04-11 — Execute #14 done**: `POST /heartbeat` route landed in commit `a12d405`. SubAgent completed full 4-step chain in one run. Factory pattern taking `{taskQueue, mainAgent}`, returning a Hono sub-app. Slot-free and slot-busy both return 202 `{status:'accepted'}`; response returns without awaiting the background cycle. 7 new tests (222 total). Next cycle picks item #15 (migrate `/health` route).
- **2026-04-11 — Execute #15 done**: `/health` route migrated in commit `cc7ae33`. `src/routes/health.ts` → `src/adapters/http/routes/health.ts`, refactored to `createHealthRoute()` factory to match the heartbeat factory style. Old `src/routes/` directory removed. Test count unchanged (222). Next cycle picks item #16 (tsyringe container).
- **2026-04-11 — Execute #16 done**: `composeApp()` composition root landed in commit `3679248`. Primary SubAgent stopped at review (3 medium + 1 low findings); follow-up applied refactor and committed. Installed `@ai-sdk/openai-compatible`. **tsyringe deferred** — wiring is straight-line construction; adding a DI container now provides no value beyond indirection. `ComposeOverrides` lets tests inject fake `mcpToolLoader` / `languageModel` / `boardRepository`. Absent `.mcp.json` tolerated (ENOENT → empty config). 5 new tests (227 total). Next cycle picks item #17 (app.ts / server.ts wiring).
- **2026-04-11 — Execute #17 done**: `server.ts` wiring landed in commit `9c69a67`. `src/app.ts` deleted (pre-composition stub with no current caller). `server.ts` imports `dotenv/config` first, calls `composeApp()`, serves via `@hono/node-server`, handles SIGINT/SIGTERM with HTTP-then-MCP shutdown order. `pnpm build` → 18.13 kB bundle. Test count unchanged (227). **All 17 execute items done — transitioning to `review` phase.**
- **2026-04-11 — Review cycle 1**: Haiku broad-scanned items #1 (Entities), #3 (PromptAssembler), #4 (MainAgent) in parallel. All three returned PASS with no SUSPECT findings — stage-2 Sonnet verify skipped. Review = done for those three. 14 items still pending review (#2, #5–#17).
- **2026-04-11 — Review cycle 2**: Haiku broad-scanned items #5 (InMemoryTaskQueue), #8 (TodoistClient), #9 (TodoistBoardRepository) in parallel. All three PASS. Notably Haiku verified the Todoist priority inversion formula `(5-n)` maps both extremes correctly (API 4→p1, API 1→p4) and the `getCallArgs` helper in TodoistClient tests is actively used (previous "unused" note was a stale LSP hint). Review = done for those three. 11 items still pending review (#2, #6, #7, #10–#17).
- **2026-04-11 — Review cycle 3**: Haiku broad-scanned #10 (Built-in tools), #11 (ToolRegistry), #12 (McpToolLoader). #11 & #12 PASS. #10 SUSPECT → fixer SubAgent applied cleanup in commit `b8ebdd6`: extracted `section-map.ts`, `tests/.../helpers.ts`, added 4 new `createBuiltInToolDescriptions()` tests. Tests 227 → 231. #10 marked `failed` for re-scan next cycle to confirm PASS. 8 items still pending review (#2, #6, #7, #13–#17) plus #10 re-verify.
- **2026-04-11 — Review cycle 4**: #10 re-scan PASS (all three refactor fixes verified); #13 (AiSdkAgentLoop) PASS — confirmed uses `ToolLoopAgent` not `generateText`; #14 (heartbeat route) PASS — factory pattern with fire-and-forget semantics verified. 6 items still pending review (#2, #6, #7, #15, #16, #17).
- **2026-04-11 — Review cycle 5**: #6 (EnvConfig), #7 (McpConfig), #16 (composeApp). All three PASS. composeApp Haiku confirmed via grep that only `server.ts` imports from `container.ts`, preserving the composition-root exception. 3 items still pending review (#2 ports interfaces, #15 health migration, #17 server.ts wiring) — all low-risk.
- **2026-04-11 — Review cycle 6 (final)**: Last 3 low-risk items — #2 ports, #15 health migration, #17 server wiring. All three PASS. **All 17 items have Review = done. Transitioning to `sample` phase.** Review summary: 6 cycles, 1 SUSPECT (item #10 DRY + missing test coverage) fixed in commit `b8ebdd6`, zero other findings across 17 items.
- **2026-04-11 — New discovery #18**: User clarified naming: "MainAgent != Supervisor... Main Agent 指的是執行任務的 Agent 不是 Supervisor 本身，這個專案才是 Supervisor". Correct model: **Shrimp-the-project IS the Supervisor** (implicit); SPEC's "Main Agent" is the AI executor (= current `AgentLoop`/`AiSdkAgentLoop`); current `MainAgent` class is misnamed — it's actually "one Processing Cycle" on the supervisor side. Scope flipped: `MainAgent` class→`ProcessingCycle`, `AgentLoop` port→`MainAgent`, `AiSdkAgentLoop`→`AiSdkMainAgent`. SPEC.md glossary update first, then code refactor. Phase `sample`→`execute` so next cron picks #18.
- **2026-04-11 — Execute #18 done**: Commit `376e123`. SubAgent completed both `/spec:spec-write` (SPEC.md terminology split) and `/coding:refactor` (code rename) in one run. 5 files renamed via `git mv`, all callers + `docs/architecture.md` + `CLAUDE.md` updated. Architecture Overview now lists 5 components (Processing Cycle split out). Tests stay at 231; `pnpm build` → 18.12 kB. Phase returns to `sample`.
- **2026-04-11 — Sample 1/10**: Picked #4 ProcessingCycle (recently renamed), #13 AiSdkMainAgent (recently renamed), #9 TodoistBoardRepository (stable control). All PASS — rename hygiene clean, behavior preserved, priority inversion + missing-section guards intact. Counter 0 → 1.
- **2026-04-11 — Sample 2/10**: Picked #5 InMemoryTaskQueue, #10 Built-in tools (re-verify post review-fix), #16 composeApp (rename hygiene at composition root). All PASS — DRY fixes still in place, composition chain wired via new names, no stale `AgentLoop` references. Counter 1 → 2.
- **2026-04-11 — Sample 3/10**: Picked #1 Entities, #8 TodoistClient, #14 heartbeat route (post-rename hygiene for `processingCycle` dep). All PASS — entities pure, TodoistClient disciplined HTTP wrapper, heartbeat route renamed field verified. Counter 2 → 3.
- **2026-04-11 — Sample 4/10**: Picked #3 PromptAssembler, #11 ToolRegistry, #12 McpToolLoader. All PASS — PromptAssembler pure and deterministic, ToolRegistry built-in-wins collision rule verified, McpToolLoader per-server fail-open + best-effort close intact. Counter 3 → 4.
- **2026-04-11 — Sample 5/10**: Picked #6 EnvConfig, #7 McpConfig, #17 server wiring. All PASS — EnvConfig fail-fast aggregation correct, McpConfig parser/loader split clean, server.ts shutdown order (HTTP→MCP) verified. Counter 4 → 5 (halfway mark).
- **2026-04-11 — Sample 6/10**: Picked the last three never-sampled items — #2 ports, #15 health migration, #18 rename. All PASS — ports rename complete, health migration clean, global grep for `AgentLoop`/`AiSdkAgentLoop`/`agent-loop` returned zero hits across `src`, `tests`, `docs`, `CLAUDE.md`, `SPEC.md`. All 18 items have now been sampled at least once. Counter 5 → 6.
- **2026-04-11 — Sample 7/10**: Re-sampled core integration trio — #4 ProcessingCycle, #13 AiSdkMainAgent, #16 composeApp. All PASS. Counter 6 → 7.
- **2026-04-11 — Sample 8/10**: Re-sampled #10 Built-in tools, #9 TodoistBoardRepository, #5 InMemoryTaskQueue. All PASS. Counter 7 → 8.
- **2026-04-11 — Sample 9/10**: Re-sampled #7 McpConfig, #11 ToolRegistry, #14 heartbeat route. All PASS. Counter 8 → 9. One cycle away from auto-stop.
- **2026-04-11 — Sample 10/10 ✅ COMPLETED**: Picked #1 Entities, #3 PromptAssembler, #17 server wiring. #1 & #3 Haiku PASS. #17 Haiku reported SUSPECT with three concerns (console.log banner, missing `serve` import, missing exit code) — dispatcher directly read `src/server.ts` (23 lines) and confirmed **all three concerns were hallucinations**: no `console.log`, `import { serve }` present at line 2, `process.exit(1)` present at line 22. Overridden to PASS. Counter 9 → 10. **Phase → `completed`. Cron `23fd3517` deleted. Powerloop ended.**
