---
goal: Fix test-quality issues in the Shrimp test suite one item at a time. If Review or Sample uncovers new issues, append them and keep going until all test-quality issues are properly handled. Review/Sample must be blind — scanners receive only the target file(s) + /coding:testing rubric, never prior findings or this note's Notes column.
language: en
current_phase: review
started_at: 2026-04-24T00:00:00Z
interval: 10m
cron_id: f952c985
execute_skills: /coding:testing, /coding:refactoring
review_skills: /coding:testing, /coding:principles
sample_passes: 0/5
review_cycles: 1
---

# Test Quality Remediation

## Progress Table

| # | Item | Execute | Review | Sample | Notes |
|---|------|---------|--------|--------|-------|
| 1 | Replace module-level `vi.mock()` of OpenTelemetry SDK in `tests/infrastructure/telemetry/otel-telemetry.test.ts` with `InMemorySpanExporter` / real `BasicTracerProvider` so assertions target observable span outcomes, not constructor-arg shapes. | done | failed | pending | Cycle 11: review FAIL on NodeSDK.prototype spy; fixed by adding injectable `sdk` option (mirrors `tracer` escape hatch); re-scan next review cycle |
| 2 | Remove `as unknown as HeartbeatJob` / `ChannelJob` / `StartNewSession` casts in `tests/adapters/http/routes/heartbeat.test.ts`, `tests/adapters/http/routes/channels/telegram.test.ts`, and `tests/adapters/http/app.test.ts`. Narrow the route dependency to a minimal interface (or function type) so fakes are structurally type-checked. | done | done | pending | Review cycle 11 PASS — both Haiku and Sonnet verify clean |
| 3 | Remove the `InspectableAgent` subclass seam in `tests/infrastructure/ai/ai-sdk-shrimp-agent.test.ts` `experimental_telemetry forwarding` block. Assert observable behavior through `MockLanguageModelV3` / the supplied tracer instead of overriding `buildToolLoopAgentOptions`. | done | failed | pending | Cycle 11: review FAIL on multi-step model duplication; fixed by extracting `makeMultiStepModel` helper; re-scan next review cycle |
| 4 | Replace the 10-tick `flushAsync()` microtask poll in `tests/adapters/http/routes/heartbeat.test.ts` with a deterministic await path (e.g., expose a `preCheckPromise` seam or resolve the fire-and-forget chain before `res` returns). | done | pending | pending | Added optional `onPreCheckSettled(chain)` callback in route deps (Introduce Seam); prod stays fire-and-forget; tests capture + await chain |
| 5 | Add a `ChannelJob` integration test that persists a user message, makes the agent throw, then re-reads the session through the real `JsonlSessionRepository` to assert the user message survives — locks the semantic contract beyond call-ordering. | done | pending | pending | +67 lines; sibling to existing Auto Compact integration; passed first run → contract holds |
| 6 | Introduce a single source of truth for HTTP/Job log event names (export consts from source, import into tests) and replace exact-string `toHaveBeenCalledWith("heartbeat received")` / `"cycle finished"` / `"heartbeat enqueued"` etc. with the constants. | done | pending | pending | 7 events promoted across 4 src + 3 test files; strings byte-identical; substring matchers & non-asserted events left alone |
| 7 | Move the compile-time-only `Expect<T extends false>` invariant block in `tests/use-cases/heartbeat-job.test.ts:249-267` into a dedicated `.test-d.ts` (or equivalent ts-expect-error file). Drop the `expect(true).toBe(true)` runtime dummy. | done | pending | pending | Moved to `tests/use-cases/heartbeat-job.types.test-d.ts`; vitest skips via its include glob; TS6196 warnings resolved via exported `_Assert` tuple |
| 8 | Replace the hand-rolled `makeRecordingTracer` (`tests/infrastructure/ai/ai-sdk-shrimp-agent.test.ts:25-88`) with `InMemorySpanExporter` + `BasicTracerProvider` from `@opentelemetry/sdk-trace-base`. | done | pending | pending | Fake → real: ~-65 lines; exception asserts translated to `span.events["exception"]`; `span.ended` implicit via `getFinishedSpans()` |
| 9 | Remove `as unknown as Task` cast from `makeTask` in `tests/adapters/http/routes/heartbeat.test.ts:31` (and audit other test files for the same pattern). Either narrow to a `Pick<Task, ...>` shape or construct a real `Task` without the cast. | done | pending | pending | Constructed real `Task` with `Priority.p2` / `Section.Backlog`; only occurrence across tests/ |

## Log Table

| Cycle | Phase | Summary | Decision | Handoff |
|-------|-------|---------|----------|---------|
| 1 | plan | Seeded 8 items from prior scan: otel mock replacement, `as unknown as` casts, InspectableAgent seam, flushAsync poll, agent-error persistence integration, log event consts, type-level assertion cleanup, recording-tracer replacement. | Seeded Plan with prior findings intentionally; Review/Sample phases will run blind per goal. Ordered highest-leverage first (items 1–5 Medium, 6–8 Low). | Next cycle: Execute phase item #1 (otel-telemetry mock replacement). Fixer runs `pnpm test` after edits and must /git:commit with a single conventional-commit per item. Keep scope to the one item — no drive-by refactors. |
| 2 | execute | Item #1 done. Replaced three module-level `vi.mock()` calls on OTel SDK modules with real `BasicTracerProvider` + `InMemorySpanExporter` + `SimpleSpanProcessor`. Deleted 3 library-testing cases (NodeSDK/OTLPExporter/resource constructor-arg assertions) that had no observable user outcome. Kept targeted `vi.spyOn(NodeSDK.prototype, "shutdown")` for the error-swallow path to avoid needing a production seam. Full suite: 41 files / 711 tests pass. | Deletions justified: each removed test asserted against mock-constructor args of third-party classes; no behavioral coverage lost. | Next cycle: Execute item #2 (remove `as unknown as HeartbeatJob/ChannelJob/StartNewSession` casts in HTTP route tests by narrowing the route dependency types). Scope strictly to the three listed test files plus the minimal source-side interface narrowing needed; no drive-by edits. |
| 3 | execute | Item #2 done. Added narrow `HeartbeatJobRunner = Pick<HeartbeatJob, "run">`, `ChannelJobRunner = Pick<ChannelJob, "run">`, `SessionStarter = Pick<StartNewSession, "execute">` types co-located with each route source. Route deps signatures now accept the narrow types; concrete classes continue to satisfy them structurally, so container wiring compiles unchanged. All three target test files drop `as unknown as` casts and build plain object fakes typed against the narrow interfaces. Full suite: 711 pass. New discovery: `as unknown as Task` on makeTask helper (item #9 appended). | Chose co-located `Pick` types over a new shared file per /coding:refactoring "Extract Interface" — keeps the narrowing visible at the consumer site and avoids premature abstraction. | Next cycle: Execute item #3 (remove the `InspectableAgent` subclass seam from `tests/infrastructure/ai/ai-sdk-shrimp-agent.test.ts`; assert `experimental_telemetry` forwarding through observable behavior on `MockLanguageModelV3` / the supplied tracer). Scope: one test file, maybe a tiny source seam only if unavoidable. |
| 4 | execute | Item #3 done. Dropped `makeInspectableAgent` + `InspectableAgent extends AiSdkShrimpAgent` subclass seam that overrode protected `buildToolLoopAgentOptions` solely to capture `experimental_telemetry`. Three cases under `describe("experimental_telemetry forwarding")` rewritten to assert on recorded span attributes (`gen_ai.agent.name == "shrimp.job"`, `gen_ai.input.messages` / `gen_ai.output.messages` presence when recordInputs/Outputs = true, absent when false) and tracer invocation (sentinel tracer fires). Also removed two now-unused AI SDK type imports. ~44-/38+ lines. Full suite 711 pass. | The observable contract equivalent to "functionId forwarded" is `gen_ai.agent.name` on the span; equivalent to "tracer identity" is "this tracer fired at all". Both hold without touching protected methods. | Next cycle: Execute item #4 (replace 10-tick `flushAsync()` microtask poll in `tests/adapters/http/routes/heartbeat.test.ts` with a deterministic awaitable — likely expose a pre-check promise via a route seam). Mind that item #4 may require a small source-side change to `src/adapters/http/routes/heartbeat.ts`. |
| 5 | execute | Item #4 done. Added optional `onPreCheckSettled?: (p: Promise<void>) => void` to `createHeartbeatRoute` deps (Introduce Seam technique, /coding:refactoring). Handler captures the detached pre-check chain into `chain`, `void chain` preserves fire-and-forget semantics, then invokes the callback when provided. Production callers (container/server) leave it undefined — 202 response continues to precede pre-check completion per SPEC §POST /heartbeat. Tests drop the 10-tick microtask poll and replace every `await flushAsync()` with a `let preCheckChain` capture + `await preCheckChain`. Auth-rejection tests drop the wait entirely since the handler short-circuits. Full suite 711 pass. | Chose deps-level seam over a globalThis / module-scoped ref per the brief — keeps the test concern explicit at the route boundary instead of leaking it into shared state. Seam adds 5 lines of prod code; net -1 line on tests. | Next cycle: Execute item #5 (add an integration test that persists a user message, makes the agent throw, and re-reads the session via real `JsonlSessionRepository` — semantic-contract coverage for `ChannelJob` agent-error path). Expect ~30-50 lines in `tests/use-cases/channel-job.test.ts` under a new describe block or a sibling to the existing integration test around L820. |
| 6 | execute | Item #5 done. Added `describe("Agent-error persistence (integration)")` with one case in `tests/use-cases/channel-job.test.ts`, mirroring the existing Auto Compact integration block's `mkdtemp` setup. Uses real `JsonlSessionRepository` on a temp dir, injects a throwing `ShrimpAgent`, asserts `ChannelJob.run` rejects, then reads the session back via `realRepo.getCurrent()` to confirm the new user message is on disk. Passed on the first run — the semantic contract holds. Suite now 712 tests (from 711). +67 lines. | Chose a real-repo integration case over additional mock assertions because a mock-only test cannot catch a future regression where `append` becomes best-effort: the return type wouldn't change, but the meaning would. | Next cycle: Execute item #6 (introduce log event name constants in source, replace exact-string `toHaveBeenCalledWith("heartbeat received")` / `"cycle finished"` / `"heartbeat enqueued"` / `"cycle idle"` / `"cycle skipped — board section missing"` etc. in tests). Scope: define consts near their emitter (e.g. `src/adapters/http/routes/heartbeat.ts`, `src/use-cases/heartbeat-job.ts`, `src/use-cases/channel-job.ts`) and update call sites + tests. Keep production log output byte-identical so observability dashboards / grep patterns are unaffected. |
| 7 | execute | Item #6 done. Promoted 7 asserted log-event literals to exported consts next to their emitters: `HEARTBEAT_RECEIVED`/`_ENQUEUED`/`_REJECTED`/`_PRE_CHECK_SKIPPED` in `src/adapters/http/routes/heartbeat.ts`, `CYCLE_FINISHED`/`_IDLE`/`_SKIPPED_SECTION_MISSING` in `src/use-cases/heartbeat-job.ts`, and a local `CYCLE_FINISHED` in `src/use-cases/channel-job.ts` (kept independent to avoid cross-use-case import coupling). Tests import the same symbols. Skipped: `"session started"` and two non-asserted pre-check warn strings (no test binding), plus `expect.stringContaining(...)` sites (already drift-tolerant). All promoted strings byte-identical incl. em-dash in CYCLE_SKIPPED_SECTION_MISSING. Suite 712 pass. | Per-file const style: 1-3 events → individual consts; no file hit the "grouped object" threshold. Independent `CYCLE_FINISHED` in channel-job.ts chosen over re-exporting from heartbeat-job.ts because the two use-cases shouldn't depend on each other's observability copy. | Pre-existing TS6196 diagnostics on `_NoSummarize` / `_NoCompactionThreshold` in heartbeat-job.test.ts:260/263 resurfaced as editor warnings — they are inside the type-assertion block item #7 is scheduled to remove entirely, so leave them. Next cycle: Execute item #7 (move the `Expect<T extends false>` block out of heartbeat-job.test.ts into a `.test-d.ts` file and drop the `expect(true).toBe(true)` runtime dummy). |
| 8 | execute | Item #7 done. Created `tests/use-cases/heartbeat-job.types.test-d.ts` (+22 lines) holding `Expect<T extends false>` + an exported `_Assert` tuple asserting that `summarize` and `compactionThreshold` are NOT keys of `HeartbeatJobConfig` (SPEC §Auto Compact invariant: HeartbeatJob has no Session). Runtime test file drops the entire mixed-mode `describe("HeartbeatJob — Auto Compact exclusion")` block and the unused `HeartbeatJobConfig` import (-26 lines). Vitest include pattern `tests/**/*.test.ts` doesn't match `.test-d.ts`, so the file is type-checked only. Suite now 711 tests (was 712, -1 runtime case that was the `expect(true).toBe(true)` stub). Typecheck clean. | Used exported tuple alias to satisfy TS "unused local" checks without pragma comments — exports are implicitly used. Preferred this over a noUnusedLocals-suppressing `.d.ts` approach because the file still needs access to the production type. | Pre-existing TS6196 diagnostics on `_NoSummarize` / `_NoCompactionThreshold` are resolved. Next cycle: Execute item #8 (replace hand-rolled `makeRecordingTracer` in `tests/infrastructure/ai/ai-sdk-shrimp-agent.test.ts:25-88` with `InMemorySpanExporter` + `BasicTracerProvider` from `@opentelemetry/sdk-trace-base`; already proven in item #1 for otel-telemetry tests). Mind that the recording-tracer helper is used across multiple describe blocks. |
| 9 | execute | Item #8 done. Replaced the 64-line hand-rolled `makeRecordingTracer` + `RecordedSpan` fake with a 7-line `makeInMemoryTracer()` helper that wires a real `BasicTracerProvider` + `InMemorySpanExporter` + `SimpleSpanProcessor` from `@opentelemetry/sdk-trace-base`. Tests now read finished spans from `exporter.getFinishedSpans()`. Assertion translations: `span.ended === true` → implicit (only finished spans are exported, comment added); `span.exceptions.toContain(boom)` → `span.events.some(e => e.name === "exception" && e.attributes?.["exception.message"] === boom.message)` (comment added); `spans[0]` → `findShrimpAgentSpan(spans)!` because real provider exports inner AI SDK spans first. Net -65 lines. Suite 711 pass. | "Test Double Decision Table" in /coding:testing: prefer real over fake. Real SDK primitives eliminate fake-drift risk when `@opentelemetry/api` evolves. | Execute phase has one pending item left (#9 — `as unknown as Task` cast in `makeTask` in heartbeat.test.ts plus any sibling occurrences, discovered during item #2). After that, phase transitions to review. Next cycle: Execute item #9. Scope: narrow `Task` cast to a `Pick`-based shape or construct a real `Task`; audit other test files for the same pattern before considering it complete. |
| 10 | execute | Item #9 done. `makeTask` in `tests/adapters/http/routes/heartbeat.test.ts` now constructs a real `Task` (`id`, `title`, `priority: Priority.p2`, `section: Section.Backlog`) — cast removed, `Priority` import added. Audit: `grep "as unknown as Task" tests/` returns nothing, so the pattern was isolated to this single file. The old fake had fictional fields (`content`, `sectionId`, numeric `priority: 1`) that never mapped to the real entity; route handler only reads `.length` of the arrays, so nothing regressed. Suite 711 pass. | No `TestTask` parallel type introduced per brief. | ALL Execute items done (1-9). Phase transitions to REVIEW. Next cycle: blind Review Stage 1 — dispatch Haiku scanners in parallel for 2-3 items, seeded ONLY with the target test file path + /coding:testing rubric (NOT this note's Notes column / Handoff / prior findings). First review batch recommendation: items #1, #2, #3 (highest-leverage refactors). |
| 11 | review | Review batch 1: items #1, #2, #3. Stage 1 Haiku: #1 SUSPECT, #2 PASS, #3 SUSPECT. Stage 2 Sonnet verify: #1 FAIL on NodeSDK.prototype.shutdown spy (other flags over-sensitive); #2 PASS; #3 FAIL on multi-step model duplication (other flags over-sensitive). Two fixers dispatched in parallel: (a) added injectable `sdk?: Pick<NodeSDK,"start"\|"shutdown">` option to `OtelTelemetryOptions`, replacing prototype-spy test with stub-SDK injection; (b) extracted `makeMultiStepModel(steps)` with named `StepUsage` type + `DEFAULT_USAGE` const. Initial (b) commit introduced a TS2322 on `StepConfig["usage"]` being `T \| undefined`; dispatcher fixed inline by factoring `StepUsage` out as a non-optional alias. Typecheck clean, suite 711 pass. Commits `5ed2db2` + `b1deaf5`. | Blind review worked: Sonnet correctly identified 1 real concern per SUSPECT file and rejected 11 over-sensitive flags (helper-at-n=1 repetition, `it.each`-as-duplication, semconv-span-names-as-brittle, providerOptions-as-library-testing). #2 passed both stages → Review = done. #1 and #3 Review = failed — re-scan next review cycle to verify fixes. | Open Review items (pending): #4, #5, #6, #7, #8, #9. Next cycle: pick 2-3 items for blind Stage 1. Suggested batch: #4 (route seam), #5 (integration test), #7 (type-only invariant) — mix of source-seam, integration-level, and type-level concerns. Items #1 + #3 will re-enter scanning the cycle after that (after new cycle marks them failed→re-scan). |
