---
goal: Update SPEC.md to add Auto Compact spec for Channel Sessions — token-threshold trigger, dedicated SummarizePort, generate new Session ID on compaction (switch current pointer), HeartbeatJob unaffected. Spec must be rigorous so downstream implementation does not drift.
language: en
current_phase: review
started_at: 2026-04-19T00:00:00Z
interval: 10m
cron_id: 3edbb0f5
execute_skills: /spec:spec-write -> /spec:spec-review -> /spec:spec-write -> /git:commit
review_skills: /spec:spec-review -> /spec:spec-write -> /git:commit
sample_passes: 0/5
review_cycles: 1
---

# Auto Compact Spec — powerloop notes

## Progress Table

| # | Item | Execute | Review | Sample | Notes |
|---|------|---------|--------|--------|-------|
| 1 | Add Glossary entries for Auto Compact, Conversation Summary, Compaction Threshold, and SummarizePort | done | pending | pending | Foundational vocabulary used by later items; commit 3a45b73 |
| 2 | Extend Scope IS list with Auto Compact (token-threshold triggered, per-Session) | done | pending | pending | Commit 241fda3 |
| 3 | Extend Scope IS-NOT to exclude cross-Session summarization, manual compact command, and HeartbeatJob participation | done | pending | pending | Commit 66b7d08 |
| 4 | Add Auto Compact behavior subsection under Session Lifecycle: token-threshold trigger using last-turn prompt tokens, new Session UUID rotation, state.json pointer switch, previous Session file retained as archive (mirroring /new) | done | done | pending | Review PASS (Haiku); UUID wording already fixed in cycle 6 |
| 5 | Update Session Lifecycle participation/coverage table to state HeartbeatJob has no Session and is not subject to Auto Compact; confirm ChannelJob is the only participant | done | pending | pending | Commit ea6db3a; also fixed UUID v4 → UUID wording to match existing Session language |
| 6 | Update Prompt Structure table/notes to describe post-compaction history shape seen by ShrimpAgent (summary prepended, older turns dropped) | done | pending | pending | Commit f10cd5d |
| 7 | Add SummarizePort contract to Design layer: role, input (prior history), output (Conversation Summary), provider/model usage, independence from ShrimpAgent | done | done | pending | Review PASS (Haiku); leak scrubbed in item #8 |
| 8 | Add Deployment & Configuration env vars for Auto Compact (compaction threshold token count; optional summarization model override) following required/optional conventions | done | pending | pending | Commit 677cb69; also scrubbed "item #8" leak in SummarizePort subsection |
| 9 | Add Failure Handling rules: summarization call failure (skip compaction, continue with current Session), new Session file write failure, missing token-usage telemetry from provider | done | done | pending | Review PASS (Haiku); skip/recovery distinction verified |
| 10 | Add cross-reference pass: reiterate HeartbeatJob-unaffected in Telemetry and any other affected sections; ensure /new Slash Command section links to shared rotation semantics | done | pending | pending | Commit dac7e72; added post-append compaction step to ChannelJob lifecycle tables; fixed 2 #failure-handling links → #session-lifecycle |

## Log Table

| Cycle | Phase | Summary | Decision | Handoff |
|-------|-------|---------|----------|---------|
| 1 | plan | Decomposed Auto Compact spec goal into 10 ordered items covering Glossary, Scope, Session Lifecycle behavior, Prompt Structure, SummarizePort Design, env vars, Failure Handling, and cross-reference sweep. | Items ordered so vocabulary (Glossary) precedes Scope, Behavior, Design, Config. Each item is one section or one coherent cross-section change to fit a single 10m cycle. | Execute phase starts next cycle with item #1 (Glossary). Use /spec:spec-write workflow per cycle; self-review before commit. Do NOT expand scope beyond the 4 stated requirements — no manual /compact command, no cross-Session summarization, no user-facing compaction notifications. |
| 2 | execute | Item #1 done: added 4 Glossary entries (Auto Compact, Compaction Threshold, Conversation Summary, SummarizePort) as a cluster after Session row. Commit 3a45b73. SubAgent self-review PASS. | Cluster-after-Session placement chosen over strict alphabetical because SPEC.md Glossary groups conceptually (Channel terms cluster together), not alphabetically — verified against existing ordering. | Next cycle: item #2 (Scope IS entry for Auto Compact). New Glossary terms are now available as vocabulary for Scope/Behavior items. Reminder: SummarizePort is "Shrimp Agent" with space in Glossary — future items must use the same spelling. |
| 3 | execute | Item #2 done: added one Auto Compact row to Scope IS after "Session-scoped conversation history". Commit 241fda3. SubAgent self-review PASS. | Placed near existing Session row (not alphabetical) — matches SPEC.md convention of grouping by concept. | Next cycle: item #3 (Scope IS-NOT exclusions). Must explicitly list: cross-Session summarization, manual compact command, HeartbeatJob participation. Use same style as existing IS-NOT rows (short Excluded + Reason). |
| 4 | execute | Item #3 done: added 3 IS-NOT rows (cross-Session summarization, manual compact command, Auto Compact for HeartbeatJob) after Slash Command extensibility. Commit 66b7d08. SubAgent self-review PASS. | Clustered with Session/Channel boundary rows (not alphabetical) — matches SPEC.md convention. | Next cycle: item #4 (core Auto Compact behavior subsection under Session Lifecycle). This is the most substantial item — must specify: trigger rule (token threshold against last-turn prompt tokens), new Session UUID generation, state.json pointer switch, previous Session archived on disk (mirror /new rotation), timing of check (before next ChannelJob invocation). Must call out HeartbeatJob exclusion explicitly. |
| 5 | execute | Item #4 done: added Auto Compact subsection under Session Lifecycle between "/new rotation" and "Participation in Jobs". Covers trigger rule (>=, eval after agent+append), 6-step procedure, next-job effect, HeartbeatJob exclusion, concurrency, /new relationship, failure ref. Commit 0778ded. | Decisions: (a) append-then-evaluate-then-compact — archived file is faithful record; (b) `>=` not `>` — trigger as soon as met; (c) Conversation Summary = ConversationMessage role: "system"; (d) new Session ID = UUID v4. | Next cycle: item #5 (Participation in Jobs table update — explicit HeartbeatJob/ChannelJob Auto Compact participation). REVIEW CHECK FOR LATER: SubAgent chose UUID v4 for new Session ID, but existing Session Lifecycle only says "UUID assigned at creation" (unversioned) and Job ID is v7. Review phase should confirm whether v4 is the right choice or if the spec should just say "UUID" to stay consistent with existing language. |
| 6 | execute | Item #5 done: extended Participation in Jobs table with "Auto Compact" column (Option A). ChannelJob → subject; HeartbeatJob → not subject. Also corrected item #4's "UUID v4" → "UUID" to match existing Session Lifecycle language. Commit ea6db3a. | Option A (column) over Option B (parallel table) — keeps Job-variant comparison in a single scan target. UUID X over Y/Z — consistency with existing open-implementation wording. | Next cycle: item #6 (Prompt Structure table — reflect that post-compaction history seen by Shrimp Agent is a single role:"system" Conversation Summary entry, followed later by normal turns). Keep the change terse — existing Prompt Structure table in Design § Job is narrow; just add a line or short note under ChannelJob's history row. |
| 7 | execute | Item #6 done: added one new bullet under Prompt rules describing compacted history shape for ChannelJob (first entry role:"system" Conversation Summary; subsequent user/assistant entries appended). Shrimp Agent treats `history` opaquely. Commit f10cd5d. | New bullet preferred over mutating the existing ChannelJob sentence — preserves blame continuity on the original rule. | Next cycle: item #7 (Design § add SummarizePort contract). Must cover: role (summarize prior history), input (list of ConversationMessage), output (Conversation Summary string), provider/model usage (may reuse OPENAI_* or accept override from env — leave concrete env decisions to item #8), independence from ShrimpAgent (no tools, no agent loop — single-call). Telemetry posture: inherit ambient OTel context; keep minimal — no scope expansion. |
| 8 | execute | Item #7 done: added ### SummarizePort Design subsection after ### Shrimp Agent. Covers intro, Role contract table (Input/Output/Invocation/Completion/Failure), Provider abstraction, Inside/outside table, and Independence from Shrimp Agent bullets. Commit ce7cc0a. | SubAgent retargeted broken `#heartbeatjob` cross-refs to `#session-lifecycle` since no such anchor exists — correct call. | Next cycle: item #8 (Deployment & Configuration env vars). Add: compaction threshold token count (required when CHANNELS_ENABLED; or optional with default?), optional summarization model override. Follow existing env table style with Required column. IMPORTANT: while doing item #8, also scrub the phrase referring to "item #8" that item #7's SubAgent accidentally left inside SPEC.md — powerloop-internal references must not appear in SPEC. Replace with a simple forward cross-reference like "see Deployment & Configuration" or remove the forward-reference entirely. |
| 9 | execute | Item #8 done: added AUTO_COMPACT_TOKEN_THRESHOLD (required when CHANNELS_ENABLED; no default; fails fast on missing/non-positive-integer) and AUTO_COMPACT_MODEL (optional; falls back to AI_MODEL) env vars, grouped with other Channel-gated vars after SHRIMP_STATE_DIR. Added 3 Rules bullets. Scrubbed "item #8" leak in SummarizePort subsection — replaced with concrete `via AUTO_COMPACT_MODEL`. Commit 677cb69. | Required-when-gated pattern matches existing TELEGRAM_* style. No default for threshold forces operators to make an informed choice per model context window. | Next cycle: item #9 (Failure Handling rules). Add: SummarizePort call failure → skip compaction, retain current Session, keep the just-appended entries; new Session file write failure → fail-open (what exactly?); missing token-usage telemetry → skip compaction, log warning, continue. Must follow existing Failure Handling table style + Fail-Open Recovery terminology. Do not add any new failure modes beyond the three listed. |
| 10 | execute | Item #9 done: added 4 failure rows to Session Lifecycle § Failure handling table: SummarizePort fails → skip compaction; new Session JSONL write fails → abort, leave partial file for inspection; state.json update fails → orphan new file, old pointer holds; missing token count → skip evaluation. All use Fail-Open Recovery except missing-token-count (which is a skip, not a recovery). Commit 113ccf8. | Kept all 4 rows in the Session Lifecycle table rather than splitting to Design § Failure Handling — failure conditions belong with Session state rules that already live there. Explicit differentiation: "Fail-Open Recovery" applies to errors; missing-data cases get "skipped" without the FOR label. | Next cycle: item #10 (final cross-reference sweep). Verify HeartbeatJob-unaffected is explicit in Telemetry section (`gen_ai.conversation.id` note), Session Lifecycle participation, and anywhere else relevant. Verify /new Slash Command section still correctly references shared rotation semantics. Verify internal links (#auto-compact, #session-lifecycle, etc.) all resolve. After item #10: current_phase → review. |
| 11 | execute | Item #10 done: added post-append compaction steps to ChannelJob lifecycle tables in both Behavior § Event-Driven Trigger Flow and Design § Job; fixed 2 #failure-handling anchors that were pointing at the MCP-only Design § Failure Handling instead of Session Lifecycle's own failure table. No term drift, no leaked powerloop refs, no HeartbeatJob collateral damage. Commit dac7e72. | Anchor fix was the highest-impact issue caught — would have led implementers to the wrong failure list. All other checks clean. | ALL 10 Execute items done. current_phase → review. Next cycle: Review phase picks 2-3 items for Stage 1 Haiku broad scan against /spec:spec-review criteria (clarity, completeness, IS/IS-NOT distinction, no implementer ambiguity). Stage 2 Sonnet verify any SUSPECTs. Prioritize reviewing items with the most behavioral content: #4 (core Auto Compact subsection), #7 (SummarizePort contract), #9 (failure rules). Items #1–#3 are smaller and lower-risk; can be batched later. |
| 12 | review | Stage 1 Haiku broad scan on items #4 (core Auto Compact behavior), #7 (SummarizePort Design), #9 (Failure Handling rows). ALL THREE PASS — no SUSPECTs, no Stage 2 verification triggered. All 4 goal requirements verified clear, unambiguous, correctly scoped. | No fixer dispatch needed. Stage 1 Haiku scan proved sufficient for these items. review_cycles→1. | Next cycle: Review remaining items. Batch the small ones together: #1 (Glossary), #2 (Scope IS), #3 (Scope IS-NOT). Batch 2: #5 (Participation table), #6 (Prompt Structure), #8 (env vars), #10 (cross-ref sweep) — split across 2 cycles. |
