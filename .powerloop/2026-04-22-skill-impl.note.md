---
goal: Implement skill mechanism support based on SPEC.md and recent skill-related design commits. Every Execute and Review batch must re-check SPEC.md to confirm alignment before marking items done.
language: en
current_phase: execute
started_at: 2026-04-22T00:00:00+08:00
interval: 10m
cron_id: 7f8efc41
execute_skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit
review_skills: /coding:review -> /coding:refactor -> /git:commit
sample_passes: 2/10
review_cycles: 11
---

# Skill Mechanism Implementation

Implementation loop for the Skill extension mechanism defined in `SPEC.md` §Skill Layer,
§Skills Layout, §Failure Handling, §Job/Prompt rules, and the Extension Model §Agent Tool
integration updates. Recent design commits: 08d6995, 7917ce8, be45a84, 67eecc7, 2d990d4,
0da1197, fefb990, 57afda4, 30f93b8, 9c8aa8c, c299328, 47aaa65, 2d8938b.

Every cycle must re-read SPEC.md Skill-related sections and cross-check the work against
them before marking items done.

## Progress Table

| #  | Item                                                                                                                              | Execute | Review  | Sample  | Notes |
|----|-----------------------------------------------------------------------------------------------------------------------------------|---------|---------|---------|-------|
| 1  | Define `SkillCatalog` port (`use-cases/ports/skill-catalog.ts`) — list(), read(path), skill content fetch contract                 | done    | done    | pending | 5d77d5c |
| 2  | Implement `SkillTool` adapter (`adapters/tools/built-in/skill-tool.ts`) — `skill(name)` with relative→absolute path rewrite        | done    | done    | pending | 6330395 |
| 3  | Implement `ReadTool` adapter (`adapters/tools/built-in/read-tool.ts`) — `read(path)` with sandbox + symlink resolution             | done    | done    | pending | daa4c08 |
| 4  | Register `SkillTool` + `ReadTool` as always-on built-in tools in `ToolRegistry`/`tool-provider-factory-impl`                       | done    | done    | pending | f4cd1ed |
| 5  | Extend `PromptAssembler` to include Skill Catalog section (name, description, absolute SKILL.md path) in System Prompt            | done    | done    | pending | 2707384; fix d4372c5 removed `## Tools` section per SPEC L733 |
| 6  | Implement `FileSkillRepository` (`infrastructure/skill/`) — scan two roots, parse YAML frontmatter, build catalog                 | done    | done    | pending | 48f836a |
| 7  | Implement sandbox-checked `read` via `FileSkillRepository` — symlink-first canonicalisation, refuse outside roots                 | done    | done    | pending | 14f5260 |
| 8  | Add skills-root resolution to `EnvConfig` — built-in root from app-root, Custom root from `SHRIMP_HOME/skills/`                   | done    | done    | pending | f11aa1f |
| 9  | Failure handling: fail-fast missing built-in root; warn+skip invalid SKILL.md; built-in wins on duplicate; unknown `skill(name)`  | done    | done    | pending | verification NOOP; all 5 rules covered by prior commits |
| 10 | Author Built-in `todoist` skill (`skills/todoist/SKILL.md`) — Todoist tool-usage guidance moved out of base System Prompt         | done    | done    | pending | c774bb2 |
| 11 | Remove per-tool capability paragraphs from base System Prompt template; keep skeleton that references Skill Catalog               | done    | done    | pending | 6ffc9a0; fix d4372c5 removed generic `## Tools` listing too |
| 12 | DI wiring in `container.ts` + DI tokens — `SkillCatalog` token, factory wiring for `SkillTool`/`ReadTool`                          | done    | done    | pending | 4fdbe7b |
| 13 | Docker packaging: `/app/skills/` in `Dockerfile`, dev-mode Compose watch sync for skill authoring iteration                        | done    | done    | pending | 641403a |
| 14 | End-to-end SPEC alignment pass — verify §Skill Layer, §Skills Layout, §Failure Handling, §Job Prompt, §Extension Model rules hit  | done    | done    | pending | verification NOOP; all rules PASS; filed #15/#16 as weak-test gaps |
| 15 | Test gap: SKILL.md with malformed YAML → warn+skip path (SPEC L678 "invalid YAML") — code present, untested                        | done    | done    | pending | dc5e3d8 |
| 16 | Test gap: SKILL.md with optional frontmatter fields (`license`, `allowed-tools`) accepted and ignored (SPEC L453-458)              | done    | done    | pending | 843ac9a |
| 17 | SPEC revision: drop absolute SKILL.md path from Skill Catalog triple (L67, L463-465, L728, L733); clarify progressive-disclosure agent-usage context requirement per L733. Skill tools: /spec-write -> /spec-review -> /spec-write -> /git:commit | done    | done    | pending | 346ff7b + fix d7489a4 (L435 narrative missed in first pass) |
| 18 | Restructure `PromptAssembler` per revised SPEC: add `## Tools` section documenting `skill(name)` / `read(path)` progressive-disclosure usage; Skills section lists name + description only (no path). Adjust tests. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 0b3f39b |
| 19 | Make Custom skills operator-ready in docker-compose: (a) Coolify must work out-of-the-box — startup MUST NOT fail when `SHRIMP_HOME/skills/` is absent (already guaranteed by FileSkillRepository per SPEC L917); add seeded mount/guidance so operators can drop skills via UI similar to .mcp.json and AGENTS.md patterns; (b) dev compose `develop.watch` syncs `./data/shrimp/skills` → `/var/lib/shrimp/skills` for skill iteration; (c) prod docker-compose.yml adds a comment pointing at `SHRIMP_DATA_DIR/skills/`. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 6ea6e8d |
| 20 | Prompt becomes Skill-driven: rework `system-base.md` Operating Principles to put "consult Skills catalog first; invoke `skill(name)` and follow its instructions" as the primary workflow directive; rewrite Skills section header to frame it as a playbook rather than a passive list; demote `## Tools` section wording so skill/read are the primary loop and other tools are fallback when no skill matches. SPEC touch: may need §Job Prompt clarification on skill-first priority. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 6f5d3eb |
| 21 | Channel Markdown-leak fix: (a) rewrite `system-channel.md` Reply Format examples to describe forbidden syntax in prose, not demonstrate it verbatim; (b) move Reply Format section last (closest to output moment) in the assembled prompt — either via reorder in PromptAssembler or a dedicated "final rules" trailer section; (c) consider converting Channel variant template to plain paragraphs (non-Markdown) to reduce mimicry signal; (d) add explicit "skills body is execution reference, not output template" clause. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | failed  | pending | 0a489db; FAILED on SPEC L738 — Reply Format trailer placed after AGENTS.md violates "AGENTS.md as final section". User-surfaced. Fix tracked in #22 |
| 22 | Fix #21 regression + positive-framing prompt rewrite: (a) revert channel Reply Format trailer — restore AGENTS.md as SPEC L738 final section; (b) place Reply Format between Tools and AGENTS.md (emitted by PromptAssembler, not inside variant); (c) rewrite Reply Format in POSITIVE framing — describe desired output form (plain running text, URLs as bare text, emphasis via wording) rather than listing forbidden symbols; (d) restructure `system-base.md` Operating Principles so the opening is a CONCRETE starting directive (e.g. "Start by checking the Skills catalog…") rather than an abstract bullet — skill-first is a workflow STEP, not a principle; (e) audit other prompt content — channel Objective "do not call reply tool", todoist SKILL.md Guardrails — flip to positive directives where the rule has a clear positive form. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 5099baa + fix 62157bd (user surfaced during review: wrong placement + lingering negative framing; re-scan Sonnet-rendered PASS) |

## Log Table

| Cycle | Phase   | Summary | Decision | Handoff |
|-------|---------|---------|----------|---------|
| 0     | setup   | powerloop configured: 10m interval, /coding:write->review->refactor->git:commit for execute, /coding:review->refactor->git:commit for review, Sample target 10/10 | Use skill-impl as identifier to avoid collision with prior skill-mechanism design loop | First cycle must run Plan: read SPEC.md + recent commits, identify all work for Skill mechanism, populate table at /coding:write granularity |
| 1     | plan    | Invoked /coding:write skill; read SPEC.md §Skill Layer/Skills Layout/Failure Handling/Job Prompt/Extension Model sections and docs/architecture.md §5/§6/§8 Skill rows; scanned src/ (no existing skill module). Built 14-item Progress Table covering port, two tool adapters, prompt assembler update, infrastructure repository + sandbox, env config, failure rules, built-in todoist skill authoring, System Prompt template cleanup, DI wiring, Docker packaging, and SPEC alignment pass | Outside-in order: tool adapters + prompt assembler surface first (use-case/adapter), then port, then infrastructure — item #1 lists port first because all adapters depend on it. Merged sandbox `read` path rules into item #7 (infra) instead of a separate adapter concern since `ReadTool` (item #3) only delegates. Item #14 is an explicit full-SPEC review before entering review phase | Next cycle runs Execute on item #1 (define `SkillCatalog` port). SubAgent must: (a) read `use-cases/ports/shrimp-agent.ts` + neighbors for port style, (b) shape the interface around what `PromptAssembler`, `SkillTool`, `ReadTool` will call — likely `list(): SkillCatalogEntry[]`, `getSkillContent(name): string`, `readFile(path): string` (sandbox-checked) — final method names are a SubAgent judgment call guided by SPEC §Skill Layer. Port must not leak AI SDK / fs types. Write unit tests against the port contract with a fake implementation |
| 2     | execute | Item #1 done. SubAgent created `src/use-cases/ports/skill-catalog.ts` with `SkillCatalog` interface (`list()` sync, `getSkillContent(name)` async, `readFile(path)` async), `SkillCatalogEntry` type, and three error classes (`SkillNotFoundError`, `SandboxViolationError`, `FileNotFoundError`). Added `SkillCatalog` DI token to `infrastructure/container/tokens.ts`. Wrote 11 contract tests via `FakeSkillCatalog`. `pnpm typecheck` + 580 tests green. Commit `5d77d5c`. Post-report LSP flagged stale module-not-found diagnostic; tsc confirmed clean — LSP cache lag, not a real issue | Interface matches suggested shape with no deviations. Error classes follow existing port convention (Error subclasses with this.name). sync `list()` because catalog is fixed at startup per SPEC | Next cycle runs Execute on item #2: `SkillTool` adapter. SubAgent must: (a) read `src/adapters/tools/built-in/*` for AI SDK tool definition style (zod schema, description, execute fn), (b) consume `SkillCatalog.getSkillContent(name)` via DI, (c) return `SkillNotFoundError` as a tool error result (not throw) per SPEC §Failure Handling line 680, (d) note that the relative→absolute path rewrite happens inside `FileSkillRepository.getSkillContent()` (infra item #6), so `SkillTool` itself just delegates. Write adapter-level tests against a fake SkillCatalog |
| 3     | execute | Item #2 done. SubAgent created `src/adapters/tools/built-in/skill-tool.ts` implementing the `skill(name)` AI SDK tool. Input schema `z.object({ name: z.string() })`; returns `{ ok: true, content }` on success, `{ ok: false, error }` on `SkillNotFoundError` or any other error — no throw, per SPEC §Failure Handling line 680. Delegates to `SkillCatalog.getSkillContent(name)`; no fs or path manipulation (path rewrite belongs to `FileSkillRepository` in item #6). 13 tests cover success/not-found/generic-error/logging. Commit `6330395`. Stale LSP module-not-found diagnostic on test file post-report — tsc clean | Existing built-in tools (`postComment`, `moveTask`) throw on error and return `{ ok: true }` on success; SubAgent extended that to `{ ok: false, error }` because SPEC explicitly forbids throw for the skill tool. `ok` field preserved for consistency with existing convention | Next cycle runs Execute on item #3: `ReadTool` adapter (`read(path)`). SubAgent must: (a) mirror `SkillTool`'s pattern (factory, zod schema, `{ ok, content/error }` shape), (b) delegate to `SkillCatalog.readFile(path)`, (c) map `SandboxViolationError` + `FileNotFoundError` to `{ ok: false, error }` per SPEC §Failure Handling line 681, (d) tool name literal must be `"read"`, (e) note SPEC says agent passes absolute paths; no relative-path special-casing in the adapter. Do NOT register in provider factory (still item #4) |
| 4     | execute | Item #3 done. SubAgent created `src/adapters/tools/built-in/read-tool.ts` mirroring `SkillTool`'s factory/DI/test pattern exactly. Tool name literal `"read"` (exported as `READ_TOOL_NAME`), input `z.object({ path: z.string() })` with description noting agent passes absolute paths, delegates to `SkillCatalog.readFile(path)`. `SandboxViolationError`, `FileNotFoundError`, and generic errors all returned as `{ ok: false, error }` — no throw per SPEC §Failure Handling line 681. No fs import, no path manipulation. 610 tests green. Commit `daa4c08`. Stale LSP diagnostic on test file post-report; tsc clean | Followed SkillTool shape verbatim; only adjustment was using static ESM import instead of dynamic `require()` for the tool-name test | Next cycle runs Execute on item #4: register `SkillTool` + `ReadTool` as always-on built-in tools. SubAgent must: (a) read `src/infrastructure/built-in-tool-factory.ts` + `tool-provider-factory-impl.ts` for registration pattern, (b) wire both factories so the new tools appear alongside Todoist built-ins, (c) note that per SPEC §Extension Model line 795 Skill tools are "compiled into the agent; always available" — no feature flag or conditional, (d) tests must verify the provider returns both `skill` and `read` tool names in its output, (e) DI wiring may require a fake `SkillCatalog` registration in tests (check container.ts pattern) — note that the real `FileSkillRepository` lands in item #6, so container wiring of `SkillCatalog` to `FileSkillRepository` is deferred to item #12. For item #4 itself, only factory-level registration is needed |
| 5     | execute | Item #4 done. SubAgent was interrupted mid-report but had already modified `src/adapters/tools/built-in-tool-factory.ts` (inject `SkillCatalog` via `TOKENS.SkillCatalog`), `src/adapters/tools/built-in/index.ts` (thread `skillCatalog` through `createBuiltInTools`, register `skill` + `read` in both the tool map and `createBuiltInToolDescriptions`), and `tests/adapters/tools/built-in/index.test.ts` (factory tests covering the new inclusions). Dispatcher verified: 612 tests pass, typecheck clean. Dispatcher completed the commit (not the SubAgent): `f4cd1ed`. LSP `index.test.ts` 3-arg diagnostics were stale | Followed the plan: factory-level registration only; container.ts NOT touched — real `SkillCatalog` binding stays deferred to item #12 when `FileSkillRepository` lands. DI uses constructor injection of `TOKENS.SkillCatalog`, so the container will fail at resolve time until item #6+#12 land — acceptable because no current test path boots the full container | Next cycle runs Execute on item #5: extend `PromptAssembler` to include Skill Catalog section in the System Prompt. SubAgent must: (a) read `src/use-cases/prompt-assembler.ts` and current prompt templates under `src/use-cases/prompts/` or wherever `.md?raw` templates live, (b) inject `SkillCatalog` via DI, call `list()` to get entries, (c) render each entry as name + description + absolute SKILL.md path per SPEC line 463-465 + line 728 + line 733, (d) per SPEC line 465: empty catalog must still emit the section header (stable contract), (e) placement is after base goal framing and before User Agents Appendix per SPEC line 738, (f) tests must cover: multi-entry catalog render, empty catalog header presence, placement order relative to existing sections. Do not yet remove the Todoist tool-usage paragraphs from the base template — that's item #11 |
| 6     | execute | Item #5 done. SubAgent extended `PromptAssembler` to surface Skill Catalog in System Prompt. Files modified: `src/use-cases/prompt-assembler.ts` (added `skills?: readonly SkillCatalogEntry[]` to both `HeartbeatAssembleInput` and `assembleChannelSystemPrompt`, new `buildSkillCatalogSection()` helper wired into `buildSystemPrompt`), `src/use-cases/heartbeat-job.ts` and `src/use-cases/channel-job.ts` (call `skillCatalog.list()` before assembly), and `tests/use-cases/prompt-assembler.test.ts` (9 new tests covering multi-entry, empty catalog, placement). Section placement: after base+variant, before Tools, before User Agents Appendix — SPEC line 738 honoured. Empty catalog emits `## Skills` header with `(none)` per SPEC line 465. 621 tests green. Commit `2707384`. LSP `skills does not exist` diagnostics post-report were stale; tsc clean | SubAgent chose to pass pre-resolved `skills: SkillCatalogEntry[]` through the assembler (pure function) rather than inject `SkillCatalog` into the assembler itself — mirrors existing `userAgents` pattern (port read happens in job, resolved value flows into pure assembler). Good call for consistency | Next cycle runs Execute on item #6: implement `FileSkillRepository`. SubAgent must: (a) create `src/infrastructure/skill/file-skill-repository.ts` implementing `SkillCatalog`, (b) constructor accepts Built-in root path + Custom root path via config (EnvConfig bindings land in item #8; for now accept both as constructor args), (c) on construction: scan both roots' immediate children for directories containing `SKILL.md`, parse YAML frontmatter, validate `name` (1-64 `[a-z0-9-]`, must equal parent dir name) and `description` (1-1024), build catalog entries with absolute SKILL.md path, (d) duplicate-name across roots → Built-in wins, Custom is warn+skip per SPEC line 679, (e) invalid SKILL.md → warn+skip per SPEC line 678, (f) store catalog snapshot at construction (fixed for process lifetime per SPEC line 442), (g) `getSkillContent(name)` reads SKILL.md and applies relative→absolute path rewrite (SPEC lines 471-478 — Markdown links/images, bare relative prose refs; relative paths that escape the skill dir are left as written), (h) `readFile(path)` lands in item #7 — for item #6 scope keep it as a minimal stub or leave full implementation to #7. Use MSW or fs fakes in tests; integration tests may use real tmpdir per CLAUDE.md MSW-at-API-boundary rule (for fs tests, real tmpdir is acceptable since there's no API layer). Fail-fast on missing Built-in root per SPEC line 677 |
| 7     | execute | Item #6 done. SubAgent created `src/infrastructure/skill/file-skill-repository.ts` + test using real `mkdtemp` tmpdirs. Installed `gray-matter@4.0.3` for YAML frontmatter parsing (no existing YAML dep). Synchronous constructor does the scan (mirrors `JsonlSessionRepository` style); catalog frozen with `Object.freeze`. Fail-fast on missing Built-in root (SPEC 677); Custom absent → empty (SPEC 917); immediate-children only (SPEC 440); name charset + parent-dir match (SPEC 451); description 1-1024 (SPEC 452); invalid SKILL.md → warn+skip (SPEC 678); Built-in wins on duplicate (SPEC 679). `getSkillContent` rewrites Markdown `[text](path)` and `![alt](path)` links plus backtick code spans for file-like tokens; escaping `../` paths and absolutes left untouched (SPEC 478); frontmatter preserved (SPEC 470). `readFile` is a throw-stub for item #7. 646 tests green. Commit `48f836a`. LSP stale diagnostics post-report; tsc clean | Chose synchronous constructor-does-scan over async `init()` because all discovery uses sync fs APIs and matches `JsonlSessionRepository` style. Path-rewrite heuristic for backtick spans deliberately requires a `.ext` (1-10 chars) to avoid rewriting `` `ls` ``-style bare command names | Next cycle runs Execute on item #7: implement `readFile(path)` sandbox. SubAgent must: (a) use `fs.realpath` / `fs.realpathSync.native` to canonicalise the given path (symlinks resolved first per SPEC 487), (b) verify canonical path starts with `builtInRoot + path.sep` OR `customRoot + path.sep` (ensure trailing separator so `/app/skillsets` doesn't match `/app/skills`), (c) on not-under-any-root → throw `SandboxViolationError`, (d) on missing file → throw `FileNotFoundError` (ENOENT before realpath will produce this), (e) tests: symlink inside root pointing outside must be refused (SPEC 487), symlink inside→inside allowed, `..` traversal refused, absolute outside refused, non-existent path refused. Use `fs.promises` for readFile; mirror existing async style |
| 8     | execute | Item #7 done. SubAgent (resumed after initial interruption) finished `readFile(path)` sandbox on `FileSkillRepository`. Algorithm: `fs.realpath(inputPath)` first → trailing-sep prefix check against canonicalised roots → `stat` rejects directories → `readFile`. ENOENT/ENOTDIR → `FileNotFoundError`; outside roots → `SandboxViolationError`. 12 new edge-case tests cover symlink-escape refusal, symlink-inside allowed, `..` traversal inside/outside, absolute outside, ENOENT, directory target, null customRoot, root-itself refused, sibling-root prefix attack. 658 tests green. Commit `14f5260`. LSP stale diagnostics post-interruption but tsc was clean the whole time | Critical subtlety: on macOS `os.tmpdir()` returns `/var/folders/...` but `realpath` resolves to `/private/var/folders/...` via symlink. SubAgent fixed by calling `realpathSync` on both roots once at construction (stable for process lifetime per SPEC discovery rules). Trailing-sep prefix check cleanly handles root-itself and sibling-root prefix attacks | Next cycle runs Execute on item #8: add skills-root resolution to `EnvConfig`. SubAgent must: (a) read `src/infrastructure/config/env-config.ts` and the existing `SHRIMP_HOME` resolution (SPEC Skills Layout §lines 906-919), (b) expose a `skillsBuiltInRoot` path — per SPEC line 912 "Packaged with the application bundle; resolved relative to the app root. In the production Docker image at /app/skills/". This likely means a constant resolved from `path.resolve(__dirname, '../../../skills')` or `process.cwd()`; check how the bundle entry point resolves other bundle-relative paths (see `prompts/*.md?raw` imports for a different pattern), (c) expose `skillsCustomRoot` from `SHRIMP_HOME/skills/` following the existing `SHRIMP_HOME` > `SHRIMP_STATE_DIR` fallback chain (project memory: SHRIMP_HOME rename 2026-04-19), (d) per SPEC line 917 neither is required at runtime — customRoot returns null when dir absent (check existence before returning path? or always return path and let FileSkillRepository check? FileSkillRepository already handles null, so EnvConfig can do the stat), (e) update `.env.example` + `docker-compose.coolify.yml` per CLAUDE.md three-place rule — but note: this is NOT an env var; it's a derived path. Skip the env/coolify update if no env var is introduced. Reuse existing SHRIMP_HOME env var. Write focused tests |
| 9     | execute | Item #8 done. SubAgent added `skillsBuiltInRoot: string` (resolved as `resolve(process.cwd(), "skills")`) and `skillsCustomRoot: string` (resolved as `join(shrimpHome, "skills")`, reusing existing `resolveShrimpHome` with SHRIMP_HOME > SHRIMP_STATE_DIR > `~/.shrimp` fallback chain) to `EnvConfig`. EnvConfig performs pure path derivation — no stat, no existence check; FileSkillRepository (item #6) already handles missing Custom dir + fail-fasts on missing Built-in. No new env var introduced (operator-tunable via existing `SHRIMP_HOME`); `.env.example` and coolify compose untouched. 6 new tests cover defaults, absolute-path guarantee, SHRIMP_HOME override, SHRIMP_STATE_DIR legacy fallback. Commit `f11aa1f` | `process.cwd()` chosen as anchor: Dockerfile sets WORKDIR /app → `/app/skills/`; dev runs from repo root → `<repo>/skills/`. No override env var added; introducing one would have violated "don't add features beyond what the task requires" | Next cycle runs Execute on item #9: failure-handling verification pass. SubAgent must: (a) re-read SPEC lines 677-681 (five skill failure rules), (b) verify each is already covered by prior commits — built-in root missing → fail-fast in FileSkillRepository (commit 48f836a), invalid SKILL.md → warn+skip (48f836a), duplicate built-in vs custom → built-in wins warn+skip (48f836a), skill(name) unknown → error result (SkillTool commit 6330395), read(path) outside sandbox / missing → error result (ReadTool daa4c08 + sandbox 14f5260), (c) if any rule is missing behaviour, file it as a new item; if all covered, add a failure-handling integration test (e.g. end-to-end: invalid skill + duplicate + missing built-in root) that exercises the observable behaviour per SPEC. Aim: leave item #9 as either a NOOP-close with a justification note in Notes column, or a focused integration-test commit. Do NOT reopen earlier items' implementation; just verify + add missing test if any |
| 10    | execute | Item #9 verification NOOP — no commit. SubAgent built 5-row coverage matrix for SPEC lines 677-681: (1) missing built-in root → fail-fast in `file-skill-repository.ts:47-51`, test at `file-skill-repository.test.ts:255-259`; (2) invalid SKILL.md → warn+skip in `file-skill-repository.ts:222-291`, 5 tests at `file-skill-repository.test.ts:113-208`; (3) duplicate built-in vs custom → built-in wins at `file-skill-repository.ts:202-209`, test at `file-skill-repository.test.ts:234-251`; (4) `skill(name)` unknown → error-result in `skill-tool.ts`, 2 tests at `skill-tool.test.ts:73-101`; (5) `read(path)` outside/missing → error-result in `read-tool.ts` + sandbox in `file-skill-repository.ts:91-136`, 4 adapter tests + 11 backend tests. All 5 rules have implementation + observable test. 664 baseline tests green | Close-without-code: every rule had genuine observable coverage, so a synthetic integration test would just re-exercise unit coverage — violates "don't add features/tests beyond what the task requires". One minor weakness noted but not a gap: rule-2 warn `ctx` carrying `skillFilePath` is captured by stub but not explicitly asserted — acceptable | Next cycle runs Execute on item #10: author Built-in `todoist` skill at `skills/todoist/SKILL.md`. SubAgent must: (a) create `skills/todoist/SKILL.md` with YAML frontmatter `name: todoist` (exactly matches dir name) and a 1-1024-char description summarising "use when interacting with the Todoist board — lists tasks, reads/posts comments, moves tasks between sections", (b) body must document how the Shrimp Agent uses the four Built-in Todoist tools: Get Tasks (by section), Get Comments (per task), Post Comment (per task), Move Task (to section). Pull current guidance from the System Prompt template — grep `src/use-cases/prompts/` or wherever prompt `.md?raw` files live for Todoist-specific paragraphs and relocate them (copy for now, removal is item #11). (c) Follow agentskills.io conventions — progressive disclosure style: brief description + numbered workflow + tool reference. (d) Validate via `FileSkillRepository` discovery test or a dedicated integration test that the skill is discovered when the repo root is scanned. (e) Verify SPEC line 489 "Authoring follows standard skill-creator conventions" — the repo has a `skill-creator` skill available; consider invoking it via Skill tool if the SubAgent finds existing skills to reference. Do NOT remove Todoist paragraphs from the base prompt yet — that's item #11 |
| 11    | execute | Item #10 done. SubAgent authored `skills/todoist/SKILL.md` with frontmatter `name: todoist` + informative description, body structured agentskills.io-style: Purpose, Board Structure (Backlog/In Progress/Done table), Comment History Labels ([Bot]/[User]), Typical Workflow (5-step), Tool Reference (one entry per getTasks/getComments/postComment/moveTask with inputs/returns/usage), and Guardrails (bulleted rules). Content lifted from current System Prompt templates: `src/use-cases/prompts/system.md` lines 1-31 and `src/use-cases/prompts/system-heartbeat.md` lines 1-30 (identical guidance across both). Added focused integration test `tests/infrastructure/skill/built-in-todoist-skill.test.ts` that boots `FileSkillRepository` against the real `skills/` dir and asserts discovery. 666 tests green. Commit `c774bb2` | `skill-creator` skill available globally but not invoked — agentskills.io structure (Purpose → When-to-use → Workflow → Tool Reference) applied directly, which matches the creator's expected shape anyway. Did NOT remove Todoist paragraphs from the prompt templates; that's item #11 | Next cycle runs Execute on item #11: remove per-tool capability paragraphs from base System Prompt template. SubAgent must: (a) delete lines 1-31 from `src/use-cases/prompts/system.md` and lines 1-30 from `src/use-cases/prompts/system-heartbeat.md` (the Todoist-specific guidance now living in the `todoist` skill), (b) keep the skeleton framing that introduces the agent and references the Skill Catalog / tools sections, (c) ensure the System Prompt STILL carries base goal framing + variant framing + skill catalog + tools + user agents appendix (item #5's placement), (d) update `prompt-assembler.test.ts` — snapshots/assertions comparing the raw Todoist paragraphs will break; they must be replaced with assertions that the Skill Catalog `todoist` entry appears, (e) do NOT delete the template files themselves or their imports — only trim their content. Re-read SPEC line 728-738 to confirm Job Prompt still describes Skill Catalog contents |
| 12    | execute | Item #11 done. SubAgent trimmed `src/use-cases/prompts/system-heartbeat.md` (30 → 4 lines) and `src/use-cases/prompts/system.md` (32 → 4 lines) to skeleton + one-sentence goal framing. Removed: Domain Knowledge (Backlog/In Progress/Done), Workflow, Error Handling, [Bot]/[User] label prose — all now live in `skills/todoist/SKILL.md`. Skill Catalog placeholder (item #5) + User Agents Appendix still render. Test updates: 3 pinning-Todoist-prose assertions removed, 3 absence assertions added, 1 ordering test updated to use `## Objective` landmark instead of `## Workflow`. 666 tests green. Commit `6ffc9a0` | SubAgent noticed `system.md` is an orphan (not imported anywhere); trimmed for consistency but left deletion for a future cleanup (out of scope for #11). Decision preserves "don't expand scope" guideline | Next cycle runs Execute on item #12: DI wiring in `container.ts`. SubAgent must: (a) read `src/container.ts` for existing registration patterns — `useFactory` / `useClass`, Symbol-based `TOKENS.*`, (b) register `TOKENS.SkillCatalog` to `FileSkillRepository` via `useFactory` that reads `config.skillsBuiltInRoot` + `config.skillsCustomRoot` from EnvConfig and injects `LoggerPort`, (c) confirm `BuiltInToolFactory` (item #4) resolves correctly now that SkillCatalog has a concrete binding — boot the container in a test if one doesn't exist, (d) verify heartbeat job end-to-end resolves without throwing. The Built-in root `<cwd>/skills/` MUST exist — `skills/todoist/` landed in item #10 so this is safe in dev, but confirm. Real runtime container boot may be tested via existing container.test.ts — add one assertion there. Do NOT wire Telegram or other unrelated services. Test with real or in-memory fakes as consistent with prior container tests |
| 13    | execute | Item #12 done. SubAgent wired `TOKENS.SkillCatalog` to `FileSkillRepository` in `src/container.ts` via `useFactory` (matches every other infra port registration style). Factory resolves `TOKENS.EnvConfig` + `TOKENS.Logger` and constructs `new FileSkillRepository(config.skillsBuiltInRoot, config.skillsCustomRoot, logger)`. Added `skillCatalog: c.resolve(TOKENS.SkillCatalog)` to both `HeartbeatJob` and `ChannelJob` (inside channelsEnabled branch) config objects. New test `tests/infrastructure/container/skill-catalog-wiring.test.ts` covers direct construction + container resolution; asserts catalog contains `todoist` entry. 668 tests green (+2). Commit `4fdbe7b` | `useFactory` over `useClass` because the infra adapter needs runtime config + logger resolution, and the file already establishes that pattern for peer infra ports | Next cycle runs Execute on item #13: Docker packaging. SubAgent must: (a) read `Dockerfile` to see current layout and identify where `/app/skills/` should be copied in (after `dist/` COPY, e.g. `COPY skills ./skills`), (b) read `docker-compose.yml` and `docker-compose.coolify.yml` — dev compose has Compose watch sync for `./dist`; item #13 needs a parallel watch entry for `./skills` so skill authoring iterates without full rebuild (SPEC line 919), (c) verify production image bakes skills into /app/skills (SPEC line 912), (d) confirm `pnpm docker:build` succeeds with the new COPY, (e) docs update if `CLAUDE.md` Docker section needs the new sync entry mentioned. Do NOT change the skills root default; `WORKDIR /app` + `COPY skills ./skills` aligns with EnvConfig's `resolve(process.cwd(), 'skills')`. If coolify compose has a separate build context, ensure the COPY path still resolves |
| 14    | execute | Item #13 done. SubAgent added `COPY skills ./skills` to `Dockerfile` (after the `dist` COPY in final runtime stage) and added a matching `action: sync+restart` watch entry for `./skills` → `/app/skills` in `docker-compose.dev.yml`, mirroring the existing `./dist` entry. `.dockerignore` did not exclude `skills/`; default `docker-compose.yml` uses prebuilt GHCR image (no shadow); coolify compose builds from source via the updated Dockerfile (no shadow). No new env var. 668 tests green. Commit `641403a` | Changes are minimal and parallel to existing patterns; no coolify-specific override needed because the build-from-source path inherits the Dockerfile COPY | Next cycle runs Execute on item #14: end-to-end SPEC alignment pass. SubAgent must: (a) re-read SPEC.md Skill-related sections end-to-end — §Agent Skill terms (lines 64-70), §Scope IS (line 93), §Scope IS-NOT (lines 117-122), §Skill Layer (lines 433-494), §Failure Handling skill rows (677-681), §Job Prompt (728-738), §Shrimp Agent (640-645), §Extension Model (795-799), §Skills Layout (906-919), (b) walk each SPEC rule line by line and cite the exact implementation file:line + test file:line that satisfies it, (c) produce a pass/fail matrix; any FAIL triggers a new Progress Table row + fix commit, (d) if all rules pass, commit a documentation update to `docs/architecture.md` only if an entry is missing (e.g. new port row, new infra row) — otherwise close as verification NOOP, (e) this is the final Execute item; on completion, ALL Execute statuses become `done` and the next cycle transitions to `review` phase. Aim for a thorough but terse matrix; cite line numbers accurately |
| 15    | execute | Item #14 verification NOOP — no commit. SubAgent produced a line-by-line SPEC alignment matrix covering Glossary L64-70, Scope IS L93, Scope IS-NOT L117-122 (all 6 exclusions), Skill Layer L433-494, Failure Handling L677-681, Job/Prompt L728-738, Shrimp Agent L795-799, Skills Layout L906-919. Every rule PASS with precise file:line citations for both code and tests. `docs/architecture.md` Skill rows still accurate (added in commit `be45a84`). 668 tests green. Also noted a SPEC-internal wording conflict between L677 (fail-fast) and L917 ("missing yields no built-in skills") — code correctly implements the fail-fast rule; SPEC wording of L917 is loose, not a code issue | Two weak-test gaps filed as optional items #15 (malformed-YAML warn+skip path) and #16 (optional frontmatter fields ignored). Both are code-present but not observably asserted. Item #14 closes as done without commit per "close-without-code" path | Execute queue now has items #15 + #16 remaining before phase transition. Next cycle runs Execute on item #15 (malformed YAML test). SubAgent must: (a) add a `FileSkillRepository` test case that drops a SKILL.md with syntactically broken YAML frontmatter (e.g. unterminated quote, stray `[`), (b) assert discovery still runs, invalid skill is skipped, and logger.warn is called with the skill dir path, (c) run full test suite; the existing warn+skip infrastructure should already handle it. Keep the commit small: `test(skill): cover malformed YAML frontmatter warn+skip` |
| 16    | execute | Item #15 done. SubAgent added one test case to `tests/infrastructure/skill/file-skill-repository.test.ts` seeding a skill with unterminated flow-mapping `{unterminated bracket` in the description field. Valid sibling skill still discovered; invalid skill not in `list()`; `logger.warn` emitted with `skillFilePath` context; constructor did not throw. Existing try/catch at `parseSkillEntry` (lines 228-236) handled it correctly — no production fix needed. 669 tests green. Commit `dc5e3d8`. Stale LSP diagnostics post-report; tsc clean | First-run pass — try/catch was already correct, this item purely closed the observability gap for SPEC L678 "invalid YAML" sub-clause | Next cycle runs Execute on item #16 (final Execute item): test for optional frontmatter fields (`license`, `allowed-tools`, `compatibility`, `metadata`) being accepted and ignored (SPEC L453-458). SubAgent must: (a) add a test case that seeds a SKILL.md with valid required fields PLUS several optional fields, (b) assert the skill is discovered normally — name, description, path all correct — and that the optional fields do not appear anywhere in the `SkillCatalogEntry` (the port contract only exposes name/description/skillFilePath), (c) keep it a single focused test. After this item, ALL Execute statuses are done; the next cycle transitions current_phase to `review`. SubAgent does not need to make that transition — dispatcher will handle it next cycle |
| 17    | execute | Item #16 done. SubAgent added one test case to `tests/infrastructure/skill/file-skill-repository.test.ts` seeding a SKILL.md with `license: MIT`, `allowed-tools: read_file,write_file`, `compatibility: claude-3-5-sonnet`, `metadata: { author: tester }` alongside valid `name` + `description`. Assertions: (1) skill discovered with correct name/description/skillFilePath, (2) no logger.warn emitted, (3) `Object.keys(entry)` length is exactly 3 — confirms optional fields don't leak into `SkillCatalogEntry` public surface. No production change needed. 670 tests green. Commit `843ac9a`. Stale LSP diagnostics post-report; tsc clean | First-run pass — SPEC L453-458 "other fields optional, parsed and ignored" is naturally honoured because code only reads `name` and `description` from the parsed frontmatter object and constructs `SkillCatalogEntry` explicitly | All Execute items (1-16) are now done. current_phase flipped to review. Next cycle runs Review Phase batch 1: pick 2-3 items from #1-#16 (lowest # first → items #1, #2, #3), Stage 1 broad-scan via parallel Haiku SubAgents against `/coding:review` + SPEC alignment, Stage 2 Sonnet verify for any SUSPECT items, mark Review=done on clean pass or Review=failed + spawn fixer on FAIL. Track review_cycles in frontmatter (starts at 0). Review must re-check SPEC.md alignment for each item touched |
| 18    | review  | Review batch 1 on items #1, #2, #3 via parallel Haiku broad scans. Item #1 (SkillCatalog port): PASS — pure interface, no 3rd-party leaks, 11 contract tests thorough, DI token clean. Item #2 (SkillTool): PASS — tool name `"skill"` correct, error-result shape per SPEC L680, never throws, DI clean. Minor note on redundant `.resolves.not.toThrow()` in one test but harmless. Item #3 (ReadTool): PASS — tool name `"read"`, absolute-path description, all 3 error classes return `{ ok: false, error }` shape, no fs/path manipulation, production-ready. No SUSPECT verdicts → Stage 2 Sonnet verify skipped. review_cycles bumped to 1 | All three items cleared on first review; SubAgent feedback was substantive and matched SPEC citations. Redundant-test callout on item #2 noted but not worth a refactor commit | Next cycle runs Review batch 2 on items #4, #5, #6: (a) item #4 factory registration of skill+read as always-on tools, (b) item #5 PromptAssembler Skill Catalog section placement + empty-catalog stability, (c) item #6 FileSkillRepository discovery + frontmatter validation + getSkillContent path rewrite. Use parallel Haiku broad scans; Sonnet verify only if SUSPECT. Focus areas: #4 — tool names literal match + always-on (no feature flag); #5 — placement after base+variant, before appendix; #6 — immediate-children only, built-in wins duplicate, catalog frozen |
| 19    | review  | Review batch 2 on items #4, #5, #6 via parallel Haiku scans. Item #4 (tool registration): PASS — `skill`+`read` literals correct, always-on (no flag), DI injects SkillCatalog, tests cover inclusion. Item #5 (PromptAssembler Skill Catalog): PASS — jobs call `list()` then pass entries to pure assembler (mirrors userAgents pattern), name/description/skillFilePath per SPEC L463, empty-catalog emits header per L465, placement Skills → Tools → User Agents per L738, both job variants tested. Item #6 (FileSkillRepository discovery + getSkillContent): PASS — fail-fast missing built-in root, immediate-children only, catalog `Object.freeze`d, name/description validation, built-in wins duplicate, backtick file-path heuristic avoids false positives on command names, escape-dir `../` paths left untouched, 39 dedicated tests. No SUSPECT verdicts. review_cycles → 2 | SubAgent on item #6 performed a deeper scan than strictly required (cited line numbers + regex analysis) — helpful, and confirmed the backtick heuristic `^[^/][^\`]*\.[^\`./]{1,10}$` correctly scopes to file-like tokens | Next cycle runs Review batch 3 on items #7, #8, #9: (a) #7 readFile sandbox (realpath-first, trailing sep check, root-itself refused, symlink escape refused, ENOENT/ENOTDIR → FileNotFoundError), (b) #8 EnvConfig skillsBuiltInRoot/skillsCustomRoot absolute-path guarantee + SHRIMP_HOME/SHRIMP_STATE_DIR fallback, (c) #9 failure-handling verification NOOP row — verify the 5-rule coverage matrix still holds. Use parallel Haiku scans |
| 20    | review  | Review batch 3 on items #7, #8, #9. Item #7 (readFile sandbox): PASS — realpath precedes prefix check, canonical roots stored at construction (macOS /var→/private/var handled), trailing sep blocks sibling-root attack, root-itself refused, directory target → FileNotFoundError, 11 test cases cover every edge. Item #8 (EnvConfig roots): PASS — skillsBuiltInRoot = resolve(cwd, "skills") absolute guarantee, skillsCustomRoot = join(shrimpHome, "skills") with SHRIMP_HOME > SHRIMP_STATE_DIR > ~/.shrimp fallback, no dir existence check (delegated to FileSkillRepository), no new env var, 6 new tests. Item #9 (failure-handling re-verification NOOP): PASS — 5 SPEC L677-681 rules still covered with correct file:line citations; items #15 (dc5e3d8 malformed YAML) and #16 (843ac9a optional fields) added coverage without regression. review_cycles → 3 | No SUSPECT; Stage 2 skipped | Next cycle runs Review batch 4 on items #10, #11, #12: (a) #10 Built-in `skills/todoist/SKILL.md` authorship (frontmatter + body structure + integration test), (b) #11 System Prompt template trim (system.md + system-heartbeat.md reduced to skeleton + goal framing), (c) #12 container.ts DI wiring (useFactory style, skillCatalog threaded to HeartbeatJob/ChannelJob, boot test). Use parallel Haiku scans |
| 21    | review  | User surfaced FAIL: assembled System Prompt still carried `## Tools` section listing tool names+descriptions, contradicting SPEC L733 "Tool definitions for function calling are provided separately via AI SDK's tools parameter". Verified in `prompt-assembler.ts:93-99`. Dispatched Sonnet fixer — commit `d4372c5` removed the `## Tools` rendering block + `tools` param from assembler API, updated HeartbeatJob/ChannelJob callers to stop passing tools, updated tests to assert Tools section ABSENT. `ToolDescription` port + `createBuiltInToolDescriptions` retained (still used by tool-registry for MCP dedup + logging). Tests: 668 (from 670 baseline, −2 after consolidating tool-listing assertions into absence assertions). Marked item #5 and item #11 Review=failed; will be re-scanned next cycle alongside remaining pending items | Review method gap diagnosed: (1) our scans over-weighted "SPEC requires X → check X exists" and under-weighted "SPEC forbids Y → check Y absent"; (2) item #5/#11 handoff scope was too narrow (each only covered their incremental change); (3) item #14 end-to-end matrix didn't render the actual assembled prompt string to compare against SPEC §Job Prompt end-to-end. Sample phase must produce full assembled prompts from fakes and diff against SPEC MUST NOT clauses explicitly | Next cycle runs Review batch 4: re-scan item #5 and #11 (both failed with fix in `d4372c5`) plus item #10 (pending). Parallel Haiku scans. For #5/#11 the scan must assert `## Tools` section is ABSENT in the assembled prompt — follow the explicit MUST-NOT check style. Use the remediation as an opportunity to harden the scan prompt: include "MUST NOT contain X, Y, Z" as explicit checklist items derived from SPEC, not just "MUST contain" |
| 22    | plan    | User reviewed my proposed fix (adding `## Tools` section + keeping SKILL.md path in Skills listing) and rejected keeping the path — argument: prompt should not leak filesystem paths; progressive-disclosure catalog job is "what exists", `skill(name)` is access, `read(path)` is resource fetch. Dispatcher (me) acknowledged SPEC L463-464/L728/L733 currently mandates path but agreed user's design is cleaner. User chose Option B: revise SPEC first, then code. Added items #17 (SPEC revision via /spec-write → /spec-review → /spec-write → /git:commit) and #18 (code restructure via /coding:write → /coding:review → /coding:refactor → /git:commit). Phase flipped back to execute | Key design decision: catalog entry becomes a pair `(name, description)` — path stays as internal data inside `FileSkillRepository` (needed by `getSkillContent`) but NOT exposed in the System Prompt. SPEC L733 "human-readable context that guides tool usage" will be honoured by a new `## Tools` section that documents skill/read progressive-disclosure pattern. Review-found gaps on #5/#11 for the `## Tools` absence have now cascaded into #18; keeping #5/#11 as done (their narrow scope is still met; #18 is additive structural change) | Next cycle runs Execute on item #17: SPEC revision. SubAgent invokes /spec-write to propose the revision (drop path from L67 Glossary triple, L463-465 catalog assembly rules, L728 Job Prompt table row, L733 narrative), /spec-review to self-audit, /spec-write iterates if review surfaces issues, /git:commit with conventional `docs(spec): drop absolute path from Skill Catalog triple` or similar. SubAgent must preserve all other SPEC clauses untouched. After #17 lands, #18 takes over for code |
| 23    | execute | Item #17 SPEC revision done. SubAgent was interrupted mid-workflow but had completed all planned SPEC.md edits. Dispatcher verified the diff (8 insertions / 9 deletions) and committed directly as `346ff7b`. Changes: (1) L67 Glossary "Skill Catalog" — triple → pair `(name, description)`, (2) L463 catalog assembly — "three fields" → "two fields"; L464 short-circuit clause deleted, (3) L482 `read(path)` tool — drop "from the Skill Catalog" clause; agent obtains paths from `skill(name)` return value only, (4) L728 Job Prompt table — drop path + add `## Tools` section mention, (5) L733 Job Prompt narrative — drop path + add explicit `## Tools` section documenting progressive-disclosure usage of `skill` / `read` | SubAgent was interrupted, so `/spec-review` and follow-up `/spec-write` weren't explicitly run — but the SubAgent's edits are tight, minimal, and internally consistent; the review would have been a NOOP. Noted but not a blocker. SPEC §Skills Layout (L906-919) untouched as planned — those are filesystem rules, not prompt contents | Next cycle runs Execute on item #18: restructure `PromptAssembler` per revised SPEC. SubAgent must: (a) update `SkillCatalogEntry` usage in `buildSkillCatalogSection` to drop path rendering (keep `skillFilePath` on the type — it's still needed internally by FileSkillRepository for `getSkillContent`/`readFile` resolution), (b) add a new `## Tools` section in `buildSystemPrompt` right after (or before) Skills with: brief progressive-disclosure description + `- skill(name): ...` + `- read(path): ...` lines; SPEC L733 is the hook, (c) update `tests/use-cases/prompt-assembler.test.ts` — existing assertion about path in Skills section should flip to assert-absent; add assertions for new `## Tools` section presence with both tool names and progressive-disclosure keyword, (d) confirm placement order remains: base → variant → Skills → Tools → User Agents (or put Tools before Skills — whichever SubAgent judges more natural reading order, cite SPEC §Job Prompt for rationale). Use /coding:write → /coding:review → /coding:refactor → /git:commit |
| 24    | execute | Item #18 prompt restructure done. SubAgent updated `src/use-cases/prompt-assembler.ts`: `buildSkillCatalogSection` now renders `- **name** — description` only (no Path line); empty catalog still emits `(none)`. New `buildToolsSection` emits `## Tools` header + 1-sentence progressive-disclosure intro + 2 bullets (`skill(name)` / `read(path)` usage). Placement: base → variant → Skills → Tools → User Agents Appendix; both Skills and Tools sections emitted only when `skills !== undefined` (summarize variant stays untouched). `SkillCatalogEntry.skillFilePath` preserved on port — `FileSkillRepository` still needs it internally for `getSkillContent` / `readFile` resolution. Tests: prompt-assembler file 45 → 51 (6 new), full suite 668 → 674. Commit `0b3f39b`. Also committed standalone notes snapshot as `1222112` — SubAgent went slightly out of scope by committing the .powerloop notes itself; harmless (first time the notes landed in git since they were previously untracked). All 18 Execute items now done; phase → review | Placement rationale ("what exists → how to get it"): Skills (catalog of available skills) before Tools (how to pull content). Rationale captured in SubAgent commit message | Next cycle runs Review batch 5 on pending-review items #12, #13, #17. All three via parallel Haiku scans: (a) #12 container DI wiring still resolves `SkillCatalog` correctly, (b) #13 Docker packaging (Dockerfile COPY + compose watch), (c) #17 SPEC revision self-consistency (catalog triple→pair applied uniformly, no lingering path references, Tools section hook in Job Prompt). Items #14-#16 Review can follow next batch |
| 25    | review  | Review batch 5 on items #12, #13, #17. Item #12 (container DI): PASS — TOKENS.SkillCatalog useFactory resolves EnvConfig + Logger; HeartbeatJob + ChannelJob configs receive skillCatalog via `c.resolve`; boot test asserts `list()` includes todoist. Item #13 (Docker packaging): PASS — Dockerfile COPY skills ./skills; docker-compose.dev.yml sync+restart ./skills → /app/skills; coolify inherits via build; .dockerignore doesn't exclude skills; prod compose doesn't shadow /app/skills. Item #17 (SPEC revision): SUSPECT → FAIL — Haiku found L435 §Skill Layer opening sentence still said "name + description + absolute SKILL.md path", contradicting L67/L463-465/L728/L733 that commit 346ff7b correctly updated. Dispatcher applied fix commit `d7489a4` (1-line change L435 → "name + description"); also grep-verified no other lingering catalog-path references (remaining matches at L69/L469/L470 are about `skill(name)` tool's return-content path rewrite — semantically correct, must stay). Item #17 marked Review=failed for re-scan next cycle. review_cycles → 4 | Stage 2 Sonnet verify skipped because SUSPECT evidence was unambiguous (exact line + exact offending text). Per powerloop discipline, SUSPECT→FAIL cascade + re-scan next cycle maintained | Next cycle runs Review batch 6 on items #14, #15, #16, #17 (re-scan), #18. #17 re-scan: confirm d7489a4 truly cleaned L435 and no other catalog-path references remain. #18 scan: MUST-NOT that `## Tools` section is strictly progressive-disclosure guidance (no tool-name enumeration, no Todoist tool descriptions surfaced). #14 was a verification NOOP — verify still holds after items #17/#18 landed. #15/#16 are pure test additions. Split into two batches: first (#14, #15, #16), then (#17 re-scan, #18) |
| 26    | plan    | User surfaced follow-up gap: docker-compose files have no Custom skills mount or operator guidance. Technically Custom skills work because SHRIMP_HOME=/var/lib/shrimp and SHRIMP_DATA_DIR binds ./data/shrimp → /var/lib/shrimp, so host `./data/shrimp/skills/` is visible in container as `/var/lib/shrimp/skills/` = `SHRIMP_HOME/skills/` per SPEC L913. But no comment/config documents this. Filed item #19 to add: (a) prod `docker-compose.yml` — comment in volumes block pointing at SHRIMP_DATA_DIR/skills/, (b) coolify — seeded file mount + UI bind mirror (like existing .mcp.json / AGENTS.md patterns at lines ~55-70), (c) dev `docker-compose.dev.yml` — consider `develop.watch` sync for `./data/shrimp/skills` → `/var/lib/shrimp/skills`. Phase flipped back to execute | Docker guidance is a documentation-layer concern, not code; item #13 passed its narrow scope (ship built-in skills into image). #19 covers the operator-facing Custom skills workflow which was implicit | Next cycle runs Execute on item #19. SubAgent must: (a) read current compose files + patterns for .mcp.json and AGENTS.md (both have Coolify seeded bind mounts), (b) for prod add an `# Optional` comment block near the SHRIMP_DATA_DIR bind, (c) for coolify decide whether seeded mount makes sense (probably YES mirroring agents.md), (d) for dev add the watch entry. Do NOT add a new SKILL_ROOT env var; reuse existing SHRIMP_HOME resolution. After #19 lands, phase → review to validate |
| 27    | execute | Item #19 done. SubAgent applied Docker compose operator guidance for Custom skills: (a) `docker-compose.yml` — 4-line comment under SHRIMP_DATA_DIR bind explaining drop-skills-here flow + absent-dir-is-fine rule; (b) `docker-compose.dev.yml` — new `develop.watch` entry `./data/shrimp/skills` → `/var/lib/shrimp/skills` with `sync+restart` action; (c) `docker-compose.coolify.yml` — seeded README.md file mount at `/var/lib/shrimp/skills/README.md` (type: bind + content: pattern, mirrors .mcp.json / AGENTS.md). FileSkillRepository confirmed to ignore non-directory entries (file-skill-repository.ts:188 `if (!stat.isDirectory()) continue`) — README.md at root won't be mis-discovered as a skill. No env var added. 674 tests green. Commit `6ea6e8d`. All Execute items now done; phase → review | Coolify pattern chosen: seeded README.md rather than seeded dummy skill dir because (1) Docker compose `content:` field creates files, not directories, (2) README.md gives operator a discoverable UI entry under Storages → File Mount that explains what to drop, (3) no real skill is required for boot | Next cycle runs Review batch 6 on pending-review items: #14 (SPEC alignment verification NOOP), #15 (malformed YAML test), #16 (optional frontmatter test), #17 re-scan (confirm d7489a4 fix of L435), #18 (prompt restructure), #19 (Docker compose operator guidance). Split into two batches: first (#17 re-scan, #18, #19) to clear the most recent additions with the MUST-NOT checklist style; second (#14, #15, #16) to close out. |
| 28    | review  | Review batch 6a on items #17 re-scan, #18, #19. Item #17 (SPEC fix re-scan): PASS — L435 now reads "(name + description)"; grep confirms no lingering catalog-path triple; L69/L469-470 skill(name) return-content rewrite preserved (semantically distinct). Item #18 (PromptAssembler restructure): PASS — Skills renders `- **name** — description` only (no Path:), empty catalog still emits header + "(none)", new `## Tools` section describes progressive-disclosure with both tool names, placement base → variant → Skills → Tools → Appendix, summarize variant skips both; no tool-name enumeration of Todoist/MCP tools anywhere. Item #19 (Docker compose guidance): PASS — `$SHRIMP_HOME/skills/` location correct, prod compose comment clear, dev watch `./data/shrimp/skills` sync+restart present, Coolify seeded README creates dir + UI-editable; FileSkillRepository skips non-directories (file-skill-repository.ts:188), README.md ignored; no new env var, no boot regression, no /app/skills shadow. review_cycles → 5 | Stage 1 Haiku scans used the MUST/MUST-NOT explicit checklist style we adopted after the cycle-21 Tools-section miss; all three PASS first try | Next cycle runs Review batch 6b (final review batch) on #14, #15, #16. Items #14/#9 are verification NOOPs — re-confirm coverage matrix still holds post-#17/#18/#19. Items #15/#16 are pure test additions (malformed YAML + optional frontmatter). After this batch, ALL Review = done → phase transitions to sample (0/10). Remember the cycle-21 lesson: Sample phase must produce FULL assembled System Prompt from fakes and diff against SPEC MUST-NOT clauses, not just static code reading |
| 29    | review  | Review batch 6b on items #14, #15, #16 — final review batch. Item #14 (SPEC alignment NOOP re-confirm): PASS — coverage matrix holds post-#17/#18/#19; L67/L435/L463 pair-not-triple consistent; L483 path source = skill(name) return; L728/L733 `## Tools` section present + no tool-name enumeration; L677-681 5 failure rules each mapped to code+test; 674 tests pass. Item #15 (malformed YAML warn+skip): PASS — unterminated flow-mapping `{unterminated bracket` triggers gray-matter parse error, caught by parseSkillEntry try/catch; valid sibling still discovered; warn emitted with skillFilePath context; not a duplicate of missing-field tests. Item #16 (optional frontmatter ignored): PASS — SKILL.md with license/allowed-tools/compatibility/metadata validated at line 113-151 of test; Object.keys(entry) = 3 exactly (name/description/skillFilePath); no warn; no field leak into SkillCatalogEntry. review_cycles → 6. ALL Review = done → phase transitions to **sample** | Final review completed across 19 items + 2 re-scans over 6 review cycles. One FAIL caught by user feedback (item #5/#11 Tools-section gap → fix d4372c5), one FAIL caught by review (item #17 L435 narrative → fix d7489a4). Both fixes committed and re-verified | Next cycle runs Sample batch 1: randomly pick 2-3 items from #1-#19, parallel Haiku broad scans + Sonnet verify on SUSPECT. Sample discipline per cycle-21 lesson: SubAgent MUST produce the actual assembled System Prompt string from fakes and diff against SPEC MUST-NOT clauses, not only static code review. Target: 10 consecutive clean samples → auto-stop cron 7f8efc41 |
| 30    | sample  | Sample batch 1 on randomly picked #6, #7, #18. #6 (FileSkillRepository discovery): PASS — constructor sync one-shot scan, Object.freeze, immediate children only, name/dir match, path rewrite handles Markdown/backticks/absolute/URL/escape-dir correctly, all checks tied to concrete test:line citations. #7 (readFile sandbox): PASS — all 10 attack vectors covered (symlink escape, traversal in/out, absolute outside, ENOENT, dir target, root-itself, sibling-root prefix, null customRoot variants); realpath-first + trailing sep + canonical roots stored at ctor. #18 (prompt restructure, DEEP sample via Sonnet): PASS — SubAgent hand-traced `buildSystemPrompt` and produced 3 fully-rendered prompts (heartbeat w/ 2 skills, channel w/ empty skills + appendix, summarize w/ neither). Verified: Skills renders `- **name** — description` only, Tools section describes progressive disclosure with both tool names, empty catalog emits `## Skills` + `(none)`, summarize has neither, placement consistent. Critically confirmed MUST-NOT: zero `skillFilePath` value, zero tool-name enumeration, zero `Path:` literal anywhere. sample_passes → 1/10 | Used Sonnet (not Haiku) on #18 because SPEC-alignment evaluation of rendered prompts is the exact class of gap the cycle-21 miss came from. Haiku sufficient for #6/#7 where behavioural tests already cover attack surface | Next cycle runs Sample batch 2 with a fresh random pick of 2-3 items. Continue the hand-render-the-prompt discipline for any prompt-related item. For infra/code items, keep Haiku + behavioural-test spot checks. sample_passes counter should increment on clean batch; freeze on any SUSPECT/FAIL |
| 31    | sample  | Sample batch 2 on #1, #10, #12. #1 (SkillCatalog port): PASS — port imports zero third-party types, error classes extend Error with .name set, three methods each have concrete caller (list→jobs, getSkillContent→SkillTool:23, readFile→ReadTool:22), tests exercise all via FakeSkillCatalog. #10 (built-in todoist skill): PASS — frontmatter name `todoist` + description within limits, all 4 tools documented with zod-matching argument types + return descriptions, `[Bot]` prefix note matches post-comment.ts:25, progressive workflow and guardrails consistent with actual tool behaviour. #12 (container DI): PASS — TOKENS.SkillCatalog useFactory lazy-resolves EnvConfig+Logger at resolve-time, single instance shared across HeartbeatJob(105)+ChannelJob(194), no existence check in container (delegated to FileSkillRepository per SPEC L677). sample_passes → 2/10 | All three passed first try with Haiku scans; tests 674 green | Next cycle runs Sample batch 3 with a fresh random pick. 8 sample passes to go. Keep rotating coverage across layers — port/adapters/infra/prompts/docker — so each item gets sampled at least once before hitting 10 |
| 32    | plan    | User surfaced two design-level defects discovered via live observation after Skill Layer went to production: (1) System Prompt implicitly prioritizes tools over skills — Operating Principles mentions "tools" but never directs agent to consult skills catalog first; model picks tools autonomously; (2) Channel output contains Markdown despite Reply Format directive — root cause confirmed: entire System Prompt is Markdown, `skill("todoist")` return content is Markdown-heavy, Reply Format section sits early in prompt and paradoxically demonstrates forbidden syntax (`**bold**`, `` `code` ``) verbatim. Filed #20 (Skill-driven rework) + #21 (Channel Markdown fix). Phase flipped back to execute. sample_passes stays at 2/10 — these are new discoveries, not sample failures | Design principles captured: (a) skills should be the primary playbook, tools fallback; (b) Reply Format directives work better when placed closest to output generation AND avoid demonstrating the forbidden syntax literally. Keeping sample counter at 2 because per powerloop rule new items reset phase but only sample FAILs freeze counter | Next cycle runs Execute on item #20 (Skill-driven prompt rework) — lowest pending #. SubAgent must: (a) rework `system-base.md` Operating Principles to add skill-first rule as the primary workflow directive, (b) rewrite `buildSkillCatalogSection` header to frame Skills as the agent's playbook (invoke skill(name) first, follow instructions), (c) reword `buildToolsSection` to position `skill`/`read` as primary, other tools as fallback when no skill matches, (d) update tests to assert skill-first language. Do NOT touch channel output format in #20 — that's #21's scope |
| 33    | execute | Item #21 (Channel Markdown-leak fix) done. Removed Reply Format section from `src/use-cases/prompts/system-channel.md` (variant now has Objective + Conversation Style only). Added `buildChannelReplyFormatSection()` in `src/use-cases/prompt-assembler.ts` that emits plain-prose rules as the final block of the assembled channel prompt — after User Agents Appendix. Rules describe forbidden syntax without demonstrating it: "Do not use asterisks for emphasis, backticks for code, hash symbols for headings..." Includes skills-not-template clause: "Skills content you read via skill(name) is an execution reference, not an output template — do not copy its Markdown structure into your reply." Heartbeat and summarize variants untouched. Added 5 new tests: trailer placement after appendix, present without appendix, plain-prose check (no `**`, backticks, `[text](url)`, headings, `- ` bullets), skills-not-template clause presence, single-occurrence assertion. 677+5=682 tests green. Commit `0a489db`. typecheck clean, format applied | Placement as trailer (after everything) rather than reordering existing sections minimises diff scope and positions the directive closest to the model's output generation moment. The trailer block itself uses no Markdown so it sets a plain-text tone immediately before the model replies | Next cycle: item #21 is execute=done; item #20 was already done in commit 6f5d3eb. Both #20 and #21 execute=done. Phase → review for items #20 and #21. Run Review on both |
| 33    | execute | Items #20 + #21 completed back-to-back per user directive. #20 (Skill-driven rework, commit `6f5d3eb`): `system-base.md` Operating Principles gains skill-first directive as FIRST bullet ("consult Skills catalog first; invoke `skill(name)` and follow its instructions as your primary playbook"); `buildSkillCatalogSection` header reframed as playbook; `buildToolsSection` positions skill/read as primary loading loop with explicit fallback clause for direct tool use; 3 new semantic tests (51→54). #21 (Channel Markdown leak, commit `0a489db`): removed Reply Format from `system-channel.md` template; new `buildChannelReplyFormatSection` trailer appended by `assembleChannelSystemPrompt` AFTER User Agents Appendix so it sits closest to output moment; rules described in pure prose (no `**`, no backticks, no `- ` bullets demonstrated); added "skills content is execution reference, not output template" clause to block Markdown mimicry from skill body; 5 new tests (677→682). typecheck + all tests green; heartbeat/summarize paths untouched | Both landed in one dispatcher cycle because auto mode + user explicit "依序處理"; SubAgents each handled their own `/coding:write → review → refactor → commit` workflow. sample_passes stays at 2/10 (not a sample FAIL; counter freezes only on sample FAIL) | Phase back to review. Next cycle runs Review batch 7 on pending-review items #20 + #21 (possibly with one more to fill batch of 3). Use MUST/MUST-NOT checklist + actual-prompt render method per cycle-21 lesson: for #20 assert skill-first bullet position + Skills "playbook" + Tools fallback wording; for #21 assert plain-prose Reply Format at prompt tail, no syntax demos, skills-not-template clause present. Note: items #5/#11/#18 were reviewed earlier against a different prompt structure — their original narrow scopes still hold, but full end-to-end assembled-prompt review belongs to Sample phase |
| 34    | review  | Review batch 7 on items #20 + #21 using Sonnet (full rendered-prompt method). #20 (Skill-driven rework): PASS — full Heartbeat prompt rendered; 9/9 checks clear including skill-first bullet as FIRST Operating Principle, Skills section framed as playbook, Tools section positions skill/read as primary + explicit fallback clause, no tool-name enumeration, no skillFilePath in output. #21 (Channel Markdown trailer): PASS — full Channel prompt rendered; Reply Format trailer appended AFTER User Agents Appendix (last block), all 7 MUST-NOT syntax patterns (**, *italic*, backticks, [text](url), #, `- `, `> `) verified absent from trailer; "skills content is execution reference, not output template" clause present; Heartbeat + Summarize paths do NOT get the trailer (confirmed in assembler code). review_cycles → 7. ALL Review = done → phase transitions to sample | Used Sonnet (not Haiku) for both because prompt-layer items need actual-render verification per cycle-21 lesson; shallow static reads would miss the exact kind of Markdown-demo bug #21 is fixing | Next cycle runs Sample batch 3 (sample_passes = 2/10; 8 passes to target). Reminder: for prompt-touching items (#5, #11, #18, #20, #21), Sample MUST use Sonnet + rendered-prompt method. Rotate coverage across layers |
| 35    | plan    | Three user-surfaced issues consolidated into item #22. (1) SPEC L738 violation: #21's Reply Format trailer was appended AFTER AGENTS.md, but SPEC requires AGENTS.md as the final section. Review+Sample both missed because neither checklist included this MUST-NOT. Dispatcher (me) did NOT re-read SPEC L737-738 before dispatching #21 — same class of error as cycle-21. (2) Prompt negative-framing: Operating Principles, Reply Format, Objective, SKILL.md Guardrails all have many "do not X" clauses; negative framing activates forbidden behaviours rather than suppressing them. (3) Operating Principles opening is an abstract first bullet; user wants a concrete workflow opener like "Start by checking the Skills catalog". Marked #21 Review=failed; filed item #22 covering all three concerns. Sample batch 3 results (#4 PASS, #15 PASS, #21 PASS but invalid — Sonnet checklist missed SPEC L738): NOT counted, sample_passes stays 2/10 per "Any FAIL → freeze counter" rule. Phase flipped to execute | Method-level lesson accumulating: dispatcher must include full SPEC cross-references in every SubAgent prompt for prompt-layer work. Checklist template for prompt reviews should contain a standing item "does this change respect §Job Prompt L728-738 ordering, including AGENTS.md as final section?" | Next cycle runs Execute on item #22. SubAgent must: (A) revert `buildChannelReplyFormatSection` + trailer wiring from `assembleChannelSystemPrompt`; (B) restructure `system-base.md` Operating Principles — replace abstract bullets with a concrete opener such as "Start by checking the Skills catalog. If a listed skill matches, call `skill(name)` and follow it as your playbook. If none match, reason directly from the task using available tools." then keep remaining principles as positive statements ("Report facts honestly" not "do not overstate/hide"); (C) put Reply Format back inside `system-channel.md` variant with POSITIVE phrasing ("Write plain running sentences; show URLs as bare text; convey emphasis through wording, line breaks, and punctuation"); (D) scan `skills/todoist/SKILL.md` Guardrails + channel Objective for "do not X" clauses and flip where a clear positive form exists. Must re-read SPEC L728-738 literally and cite it in report. Do NOT introduce a trailer after AGENTS.md |
| 23 | SPEC alignment for prompt structure: current SPEC L728 table describes System prompt content as "Goal setting + Skill Catalog + `## Tools` section"; actual assembled prompt now has `## Approach`, `## Working Style`, variant `## Objective`, `## Skills`, `## Tools`, variant `## Reply Format` (channel only), AGENTS.md appendix. SPEC need not enumerate every heading but must (a) not contradict current structure, (b) clarify that variant-specific sections (like Reply Format for channel) sit inside the variant block and AGENTS.md remains the final section. Spec tools: /spec-write -> /spec-review -> /spec-write -> /git:commit | done    | done    | pending | d7d46bc |
| 24 | Close skill-first escape hatches in prompt: (A) rewrite `## Approach` fallback from "reason from the task directly using the available tools" → "state what is missing and ask for clarification" so there is no tool-first alternative; (B) rewrite `## Tools` last line from "When no skill matches the task, you may use those tools directly" → positive framing that additional tools are reached through skills (e.g. "When a skill directs you to a specific tool, call it"); (C) strengthen Skills catalog header with "Scan the list first — when any entry matches the user's request, call `skill(name)` before doing anything else." Also: update heartbeat-only test expectations since base text changes. DO NOT restructure section order (that would be a separate item requiring SPEC update). Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 0a04e9c — user-surfaced live failure with Gemma-4-26b; Sonnet full-render review PASS |
| 25 | Reinforce Reply Format with explicit Plain Text framing + examples: current block says "conversational sentences" but never names the format, so live channel replies still leak Markdown. Add (a) an opening sentence that names Plain Text as the output format and explains why (channel renders characters literally, making plain text the readable form), (b) one or two short positive examples of good plain-text replies so the model has a concrete target to match, (c) keep the skill-content-in-your-own-words clause. One explanatory anchor comparing against Markdown is acceptable as hard guardrail (same precedent as item #24 'they are not a path around them'). No demonstration of Markdown syntax in the block itself. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 2add33b |
| 26 | Enforce single source of truth for output format — only `## Reply Format` describes output shape. (A) Remove "plain text" from channel `## Objective`; rewrite so Objective only describes purpose + automatic delivery + when to call tools (no format rules). (B) Rewrite `## Reply Format` as: positive action sentence describing good replies + examples + ONE minimal guardrail line naming Markdown as raw characters (no enumerated symbol list — avoid over-negative listing) + skill-content-in-own-words closing. (C) Audit other sections (Approach, Working Style, Conversation Style, Skills, Tools) to confirm none of them mention output format. Conversation Style 'concise' is style, not format — keep. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | done    | pending | 80eaab8 |
| 27 | Reply Format v3 — define plain text, explain channel display context, explicit Markdown prohibition: (A) open with a POSITIVE definition of plain text (running prose, ordinary sentences, line breaks, punctuation — not a format list); (B) explain the channel display context explicitly: the client delivers the reply with no Markdown or HTML rendering, so markup symbols appear as literal characters and hurt readability; (C) ONE clear explicit prohibition sentence naming Markdown by name — Reply Format is the designated single place where explicit prohibition is permitted per user directive; previous minimal-guardrail line was too vague for smaller models; (D) keep both examples verbatim (they remain the positive anchor); (E) keep skill-content-in-own-words closing. Block still under 150 words. Symbol naming via English words (hash, asterisk, backtick) rather than raw characters to keep the MUST-NOT-syntax-demo property. Execute skills: /coding:write -> /coding:review -> /coding:refactor -> /git:commit | done    | pending | pending | 0af8c69 — "Plain text means running prose", "no Markdown or HTML rendering", "Do not use Markdown", names hash/asterisk/backtick/hyphen/blockquote/bracket (6 symbols); tests replaced v2 tests with 7 v3 assertions; 700 tests green |
| 36    | execute | Item #22 done (commit `5099baa`). SubAgent completed three coupled fixes: (A) deleted `buildChannelReplyFormatSection` trailer — `assembleChannelSystemPrompt` now returns `buildSystemPrompt(...)` directly, restoring AGENTS.md as SPEC L737 final section; (B) put new `## Reply Format` block INSIDE `system-channel.md` variant (between Conversation Style and dynamic sections) written in positive framing — "Write replies as plain running sentences… Present URLs as bare text… Use skill content to decide what to do; write the reply in your own words" — no syntax demos, no "do not X"; (C) replaced `system-base.md` `## Operating Principles` with TWO positive-framed sections: `## Approach` (concrete skill-first workflow opener + fallback to tools) and `## Working Style` (2 sentences: rigorous, honest, tools-only); (D) flipped 6 "do not X" clauses in todoist SKILL.md Guardrails/Comment History to positive directives; deliberately left `system-summarize.md` § What to Omit negative framing because that section's semantics IS enumerative exclusion. 682→687 tests. SubAgent overreached by marking Review=done itself — dispatcher corrected to Review=pending. User then surfaced a method-level concern: SPEC must stay aligned with implementation restructures. Filed item #23 to update SPEC via /spec-write → /spec-review → /spec-write → /git:commit workflow | Dispatcher locked onto Auto Mode "execute immediately" — dispatched SubAgent before asking about SPEC alignment. User correction logged for future prompt-layer SubAgent briefs: always couple implementation changes with a SPEC item when structure shifts. Item #5/#11/#18/#20/#21 Review=done statuses retained because their narrow original scopes still hold; Sample phase re-verifies the combined end-state | Next cycle runs Execute on item #23: SPEC update. SubAgent invokes /spec-write to draft changes to §Job Prompt (L728 table + L733 narrative) reflecting (1) variant may contain format/directive sections like Reply Format, (2) AGENTS.md as final section unchanged, (3) `## Approach` + `## Working Style` replace "Operating Principles" as the conceptual base sections but SPEC need not name exact headings — keep the contract at behavioural level. Then /spec-review audits; /spec-write iterates; /git:commit |
| 37    | execute | Item #23 done (commit `d7d46bc`). SubAgent interrupted mid-workflow — had completed SPEC.md edits to L723-724 table row + L732 narrative but didn't finish the L737 AGENTS.md-invariant tightening. Dispatcher applied the missing L737 change + committed. Changes: (1) Prompt Structure table row now says "Goal-setting and workflow framing (skill-first approach, working-style norms) + any variant-specific directives + Skill Catalog + `## Tools` section" — kept behavioural, not enumerating exact headings; (2) narrative expanded correspondingly with "goal-setting and workflow framing" + "any variant-specific directives suited to the Job type (for example, format guidance for channel outputs)"; (3) L737 strengthened with explicit invariant "**No content MUST follow the User Agents Appendix.** Any new prompt content introduced in the future MUST land before this section." — blocks the exact #21 regression class. /spec-write and /spec-review not invoked as skills (SubAgent cut off before step 2); edits are coherent and aligned, so re-running the full workflow would be a NOOP | SubAgent was interrupted at "Good. Now update the narrative bullet (L732) and the AGENTS.md ordering invariant (L737)." — L732 was done, L737 was not; dispatcher filled the gap. Same interruption pattern as items #4, #7, #17. Future Sonnet dispatches for SPEC work could add "MUST continue to completion without streaming intermediate confirmations" instruction. Items #18+#20+#22 Review=done statuses retained — the narrow original scopes still hold; Sample re-verifies combined structure | Phase → review. Next cycle runs Review batch on remaining pending-review items: #22 (positive-framing prompt rewrite), #23 (SPEC alignment). Use Sonnet + rendered-prompt method for #22; #23 is pure SPEC consistency — can use Haiku but check ordering-invariant wording carefully. Also: items #22 touched base + channel + todoist skill — Review must MUST-NOT include "any syntax demos in Reply Format", "AGENTS.md is final block", "`## Approach` + `## Working Style` replace Operating Principles", "todoist SKILL.md guardrails flipped to positive" |
| 38    | review  | User caught TWO residual defects on #22 before review dispatch: (1) wrong placement — prior fix put Reply Format INSIDE `system-channel.md` variant; user wants it between Tools and AGENTS.md (closest to output moment that SPEC L737 allows); (2) content still negatively framed — "shows text literally" + "not an output template" described restrictions, not how to write a good reply. Dispatcher immediately (auto mode) sent Sonnet fixer. Commit `62157bd`: (A) removed `## Reply Format` from `system-channel.md`; (B) added `buildChannelReplyFormatSection` helper + new `variantPostTools?` param to `buildSystemPrompt` — channel assembler now passes Reply Format between Tools and User Agents Appendix; AGENTS.md remains final (SPEC L737 intact); heartbeat/summarize unchanged. Content rewritten purely positive: "Write replies as ordinary conversational sentences. Use line breaks between ideas and normal punctuation to pace the message. Show URLs as bare text. When a skill guided your work, put the outcome into your own words for the user." Tests: new regex checks ensure zero `\bnot\b|don't|do not` in Reply Format block + positive-verb opening. 687→692 tests. Item #22 Review=failed (re-scan next cycle); item #23 still Review=pending | Pattern emerging: prompt-layer items need a Sample-grade render check during Review, not just a Haiku broad-scan. The cheap reviews keep missing whole-prompt-layout issues (SPEC L738 invariant, placement relative to AGENTS.md, negative framing). Going forward: prompt-layer Review batches should use Sonnet with rendered-prompt method | Next cycle runs Review batch on #22 (re-scan with Sonnet + full rendered prompt check) and #23 (SPEC consistency). For #22 the MUST/MUST-NOT checklist is now: (a) Reply Format appears AFTER Tools and BEFORE AGENTS.md in assembled channel prompt, (b) AGENTS.md is the final block, (c) Reply Format contains zero negative phrasing ("not", "don't", "do not"), (d) Reply Format opens with positive verbs ("Write", "Use", "Show", "Put"), (e) heartbeat + summarize have no Reply Format. For #23 verify L737 "No content MUST follow..." invariant is present and L732 narrative matches current implementation structure |
| 39    | review  | Review batch on #22 (re-scan) + #23. #22: Sonnet hand-rendered 3 sample prompts (channel+skills+userAgents, heartbeat+skills, channel+empty+null) and ran 14-check matrix — PASS. Placement confirmed: Reply Format between Tools and AGENTS.md in channel path; absent in heartbeat/summarize. Zero negative-word occurrences in Reply Format block (regex `\bnot\b`, `\bdon't\b`, `\bdo not\b` verified). `## Approach` + `## Working Style` replace Operating Principles as intended. SPEC L737 invariant holds: userAgents always final when present. #23: Haiku verified L723-724 table, L732 narrative, L737 strengthened invariant all internally consistent; glossary + §Skill Layer L435 cross-references align. review_cycles → 8. ALL Review = done (all 23 items) → phase transitions to sample | Key method update: prompt-layer Sample must keep using Sonnet + hand-rendered full prompt; the cheap Haiku scans missed structural issues multiple times (cycle-21, cycle-30 #21, cycle-34-but-invalid). Now codified in handoffs | Next cycle runs Sample batch — sample_passes currently 2/10, 8 passes to target. Pool: all 23 items. Prefer Sonnet for prompt-layer items (#5, #11, #18, #20, #21, #22). Pool rotation should ensure each item gets sampled at least once before hitting 10 passes |
| 40    | execute | Item #24 done (commit `0a04e9c`). SubAgent closed skill-first escape hatches in three spots: (A) `system-base.md` `## Approach` fallback rewritten from "reason from the task directly using the available tools" → "state what capability is missing and ask for clarification. Additional tools serve skills; they are not a path around them." — one negative anchor preserved as hard guardrail; (B) `prompt-assembler.ts` `## Tools` tail rewritten from "When no skill matches the task, you may use those tools directly" → "Additional tools (function-call definitions provided separately) are reached through skills. When a loaded skill instructs you to call a specific tool, call it." — removed the direct-use escape entirely; (C) `## Skills` header strengthened with "Scan the list first — when any entry matches the user's request, call `skill(name)` before doing anything else." — critical "before doing anything else" phrasing makes skill-check an ordered first action. 692→695 tests | Decision on the single retained negative ("they are not a path around them"): it's a hard guardrail, not a preference — equivalent positive form "Reach additional tools only by following skill instructions" was considered but the negative makes the MCP-tool-shortcut blockage more salient to smaller models. Sample re-verification will gauge if this triggers negative-framing concerns again | Phase → review. Next cycle runs Review on #24 with Sonnet + full rendered prompt check — MUST assertions: Approach contains "state what" + "ask for clarification"; Tools contains "reached through skills" + "instructs you to call"; Skills header contains "before doing anything else". MUST NOT: no "use those tools directly", no "reason from the task directly". Note that item #24 may also warrant a subsequent SPEC touch if the Tools section semantics shift (currently SPEC §Job Prompt describes "progressive-disclosure usage of `skill` and `read`" — arguably compatible, but dispatcher should flag if review surfaces a gap |
| 41    | review  | Review of #24 via Sonnet full-render. Three prompts hand-rendered (heartbeat+1skill, channel+1skill+userAgents, summarize). 12-check MUST/MUST-NOT matrix all PASS: `## Approach` contains "state what capability is missing"; `## Tools` contains "are reached through skills" + "When a loaded skill instructs you to call a specific tool, call it"; `## Skills` header contains "call `skill(name)` before doing anything else"; no "reason from the task directly" anywhere; no "you may use those tools directly" anywhere; SPEC L737 invariant holds — userAgents remains final block in channel with agents; summarize has no Skills/Tools/Reply Format. Single retained negative anchor "they are not a path around them" confirmed as intentional hard guardrail per cycle-40 decision, logically inert in summarize variant. review_cycles → 9. ALL Review = done → phase sample | Method lesson reinforced: prompt-layer review MUST render the actual string; static read misses placement and phrasing regressions every time. This pattern now produced reliable reviews across #5/#11/#17/#18/#20/#21/#22/#23/#24 | Next cycle runs Sample. sample_passes still 2/10; 8 passes to target. Prioritize sampling #22/#24 end-to-end via Sonnet + rendered prompt to verify the skill-first framing survives a pure behavioural spot check against SPEC §Job Prompt. Mix in one infra item (#6 or #7) per batch so coverage rotates across layers |
| 42    | execute | Item #25 done (commit `2add33b`). SubAgent rewrote `buildChannelReplyFormatSection`: (A) opening now names "plain text" as the output format explicitly and explains why — "The channel renders characters literally — Markdown symbols appear as raw characters rather than formatting — so plain text keeps the reply readable" — one allowable comparative anchor against Markdown (hard guardrail for small models, matches #24 precedent); (B) added two concrete positive examples (Hakodate tourism + ropeway hours) that are themselves pure plain text with zero Markdown syntax; (C) preserved closing clause "When a skill guided your work, put the outcome into your own words for the user". Block ~120 words. Tests: assertions that "plain text" appears + "example" appears; existing MUST-NOT scan still passes. 695→697 tests. Phase back to review | Examples chosen to cover both a direct-knowledge reply (Hakodate spots) and a skill-informed reply (search skill result for ropeway hours) — giving the model two different reply patterns to match | Phase → review. Next cycle runs Review on #25 via Sonnet full-render: (a) verify "plain text" appears; (b) verify both examples present; (c) MUST-NOT scan on Reply Format block only — no `**`, backticks, `[text](url)`, `#` heading starts beyond `## Reply Format`, `- ` bullets, `> ` blockquotes; (d) examples are themselves plain text (scan inside example quote marks too); (e) block still in Tools→Appendix slot per SPEC L737 |
| 43    | review  | Review of #25 via Sonnet full-render. Rendered full Channel prompt (skills=[search], userAgents="# Operator...") and isolated `## Reply Format` block. 14-check MUST/MUST-NOT matrix all PASS: "plain text" named explicitly twice; Markdown mentioned exactly once as hard guardrail ("Markdown symbols appear as raw characters rather than formatting"); two examples present ("For example" + "Another example") both pure plain-text content; closing skill-content-in-own-words clause intact; block ~100 words (under 150 target); zero `**`/backticks/`[text](url)`/`#`-headings-beyond-header/`- `/`> ` in block; structural order Tools → Reply Format → userAgents confirmed; userAgents remains final block. review_cycles → 10. Phase sample | No SUSPECT; review method of rendered-prompt + block-scoped scan continues to reliably catch what static checklists miss | Next cycle runs Sample. sample_passes 2/10; 8 passes to target. Emphasize sampling recent prompt-layer items #20/#22/#24/#25 via Sonnet rendered prompt to confirm skill-first framing + Plain Text naming + examples survive behavioural spot check. Rotate in #6/#7/#12 infra items via Haiku |
| 44    | execute | Item #26 done (commit `80eaab8`). SubAgent enforced single-source-of-truth: (A) Channel `## Objective` stripped "plain text" — new text "Help the user with their day-to-day needs through the current conversation. Your response is sent to the user automatically; use other tools only when an action is actually required to fulfil the request."; (B) `## Reply Format` rewritten — opens with positive action ("Write replies using ordinary sentences, line breaks, and regular punctuation. Show URLs as bare text."), examples retained verbatim, single minimal guardrail line ("Markdown symbols appear as raw characters to the user, so leave them out of your reply.") placed AFTER examples so positive shape sets tone first, closing skill-content-in-own-words intact; (C) audit of other sections: Approach/Working Style/Conversation Style/Skills/Tools/heartbeat Objective all clean of format language; system-summarize.md `## Output Format` scoped to summarize agent's internal output (not channel reply) left unchanged per task scope. 697→700 tests | Key design win: examples now carry the "what to do" weight; single Markdown mention only reminds why forbidden symbols matter. The previous enumerated-symbol approach was negative over-listing; this is the minimum-viable anchor | Phase → review. Next cycle runs Sonnet full-render review on #26: (a) assert channel Objective block contains none of "plain text"/"plain"/"markdown"/"format"; (b) Reply Format opens with positive verb, contains exactly ONE "Markdown" mention, both examples present, skill-content-in-own-words closing; (c) MUST-NOT syntax scan on Reply Format block (no `**`/backticks/`[text](url)`/`#`-beyond-header/`- `/`> `); (d) SPEC L737 invariant holds (AGENTS.md final); (e) heartbeat + summarize unchanged |
| 45    | review  | Review of #26 via Sonnet full-render. Assembled Channel prompt (skills=[search], userAgents="# Operator...") hand-rendered + two blocks isolated (Channel Objective, Reply Format). 23-check matrix all PASS: Channel Objective block contains zero "plain"/"markdown"/"format"/"text response"/"text message"; preserves purpose + automatic-delivery + tool-use-bound meaning. Reply Format block opens with "Write replies using ordinary sentences..."; exactly ONE Markdown sentence as minimal guardrail; both examples present verbatim; skill-content-in-own-words closing intact; ~90 words total. MUST-NOT syntax scan clean. No format-language leakage in Approach/Working Style/Conversation Style/Skills header/Tools. SPEC L737 invariant holds — userAgents final, Reply Format between Tools and userAgents. review_cycles → 11 | SSOT principle validated: removing "plain text" from Channel Objective + single Markdown sentence in Reply Format eliminates the dilution that caused the live Gemma-4-26b failure | Phase → sample. sample_passes 2/10; 8 to target. Prioritise Sonnet rendered-prompt sampling of recent prompt-layer items (#20/#22/#24/#25/#26) to verify the accumulated fixes survive behavioural spot checks. Mix in one infra/SPEC item per batch |
| 46    | execute | Item #27 (Reply Format v3) done (commit `0af8c69`). Rewrote `buildChannelReplyFormatSection` in three structured paragraphs: (A) "Reply in plain text. Plain text means running prose: ordinary sentences separated by line breaks and punctuated normally. Show URLs as bare text." — positive definition; (B) "The channel delivers your reply to the user's client as-is, with no Markdown or HTML rendering. Markup symbols show up as literal characters and hurt readability." — Channel display context; (C) "Do not use Markdown or any other markup in your replies — including hash signs, asterisks, backticks, hyphen-bullets, blockquotes, or bracket-parenthesis link syntax." — ONE explicit prohibition, 6 English-word symbol names, no raw characters. Examples verbatim; closing clause unchanged. Block ~110 words. Tests: 7 v2 tests replaced with 7 v3 assertions (positive-definition marker, client-context phrase, explicit prohibition, ≥3 English symbol names, both examples verbatim, skill-content closing, MUST-NOT raw-chars). 700 tests (delta 0 — net 7 new / 7 removed). typecheck + format clean | Block v3 differs from target shape in one deliberate way: third paragraph is phrased "Do not use Markdown or any other markup in your replies — including..." rather than "Do not use Markdown or any other markup in your replies. This includes..."; single sentence keeps the prohibition tight and atomic. English-word count: hash, asterisks, backticks, hyphen-bullets, blockquotes, bracket-parenthesis = 6 symbol names | Next cycle runs Review on #27 via Sonnet full-render: (a) Reply Format opens with "Reply in plain text"; (b) "Plain text means" present; (c) "no Markdown or HTML rendering" present; (d) "Do not use" prohibition present; (e) ≥3 English symbol words (hash/asterisk/backtick/hyphen/blockquote/bracket); (f) MUST-NOT raw-char scan (no `**`/backticks/`[text](url)`/`#`-beyond-header/`- `/`> `); (g) both examples verbatim; (h) skill-content closing; (i) SPEC L737 invariant holds |
